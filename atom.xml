<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title><![CDATA[Gosuke Miyashita]]></title>
    <link href="https://mizzy.org/atom.xml" rel="self"/>
    <link href="https://mizzy.org/"/>
    <updated>2026-08-05T20:13:04+09:00</updated>
    <id>https://mizzy.org/</id>
    <author>
        <name><![CDATA[Gosuke Miyashita]]></name>
    </author>
    <generator uri="https://github.com/mizzy/nebel/">Nebel</generator>

    <entry>
        <title type="html"><![CDATA[Peithoにツイートやリンクの埋め込み機能を入れた]]></title>
        <link href="https://mizzy.org/blog/2026/08/05/1/"/>
        <updated>2026-08-05T20:13:04+09:00</updated>
        <id>https://mizzy.org/blog/2026/08/05/1/</id>
        <content type="html"><![CDATA[
<p>自作プレゼンツール<a href="https://github.com/mizzy/peitho">Peitho</a>の話の続き。ツイートやYouTubeの動画のURLを書くと、スライドにその中身を貼れるようにした（<a href="https://github.com/mizzy/peitho/pull/401">PR #401</a>、<a href="https://github.com/mizzy/peitho/pull/403">#403</a>、<a href="https://github.com/mizzy/peitho/pull/404">#404</a>）。貼られるのはスクリーンショットかカードで、動画が再生できるわけではない。</p>
<p>書き方はこう。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="s">```embed
</span></span></span><span class="line"><span class="cl">https://x.com/gosukenator/status/2074821309259973046
</span></span><span class="line"><span class="cl"><span class="s">```</span>
</span></span></code></pre><p><code>embed</code>というタグのコードフェンスにURLを1行書くだけ。ビルド時に解決されて、present、preview、PDFエクスポート、公開用の<code>dist/</code>のどれでも同じものが出る。</p>
<hr />
<h2>出力にスクリプトを入れない</h2>
<p>まず前提として、Peithoでは本文にHTMLタグを書くとビルドエラーになる。CommonMarkはHTMLをそのまま出力に通す仕様だけど、Peithoでは怒られて止まる（コメントだけは例外で、ページ設定と発表者ノートに使っている）。なので、Xが配っている公式の埋め込みコードを貼る、という選択肢は最初からない。</p>
<p>仮に許したとしても動かない。公式の埋め込みは、blockquoteと<code>widgets.js</code>の読み込みをセットで貼る形になっていて、スクリプトがページ全体からそのblockquoteを探し出して中身を差し替える、という段取りで動く。ところがPeithoはスライドを1枚ずつShadow DOMの中に入れて、デッキのCSSもその中に流し込んでいる。デッキのスタイルが発表者ツールなどのシェル側のUIに漏れ出さないように、また逆にシェル側のスタイルがスライドに効かないようにするため。Shadow DOMの中身は外から普通に検索しても引っかからないので、<code>widgets.js</code>は差し替える相手を見つけられないまま終わる。</p>
<p>そもそも、スライドを表示するたびにスクリプトが走って埋め込みを組み立てる、という作りには無理がある。ネットワークのない会場では<code>widgets.js</code>をXから取ってくるところで止まるし、Xの側で表示のされ方が変われば、過去のスライドの見た目も変わる。PDFエクスポートも、スクリプトの実行を待ちきれるかどうかというタイミングの問題を抱え込むことになる。発表の当日に埋め込みが出るかどうかが、その場の環境次第になってしまう。</p>
<p>なので全部ビルド時に解決して、出力には静的な成果物だけが入るようにした。</p>
<hr />
<h2>埋め込みは3種類</h2>
<p>同じ<code>embed</code>と書いても、出来上がるものは3種類ある。書いたURLと、オプションを付けたかどうかで決まる。</p>
<h3>ツイートのスクリーンショット（デフォルト）</h3>
<p>X（Twitter）のステータスURLをオプションなしで書くと、ビルド時にヘッドレスChromeで公式のウィジェットを描画して、そのPNGを撮る。撮った画像はキャッシュされて、以降のビルドではネットワークに出ない。</p>
<p><img src="/images/2026/08/peitho-embed-screenshot.png" alt="公式のXウィジェットのスクリーンショットが貼られたスライド" /></p>
<p>アイコンも動画のサムネイルも、いいねの数や「Read 1 reply」まで、公式の埋め込みの見た目そのまま入る。</p>
<p>自分でスクリーンショットを撮っていたのを、機械にやらせるようにしただけとも言える。</p>
<p>ツイートが消されていたり非公開だったりして、ウィジェットがいつまでも描画されない場合は、行番号つきのビルドエラーにしている。空白の画像が黙って出るよりは、ビルドが落ちたほうがいい。</p>
<h3>カードモード</h3>
<p><code>mode=card</code>を付けると、スクリーンショットではなくHTMLのカードになる。</p>
<pre class="chroma"><code><span class="line"><span class="cl">``<span class="sb">`embed mode=card
</span></span></span><span class="line"><span class="cl"><span class="sb">https://x.com/gosukenator/status/2074821309259973046
</span></span></span><span class="line"><span class="cl"><span class="sb">`</span>``
</span></span></code></pre><p>こっちはoEmbedのAPIを叩いてデータを取ってきて、Peitho側でカードのHTMLを組み立てる。画像ではなく本文なので、テキストが選択できるし、デッキのテーマの色とフォントに従う。Chromeを起動しないので、カードだけのデッキならChromeなしでビルドできる。</p>
<p><img src="/images/2026/08/peitho-embed-card.png" alt="カードモードで描画された同じツイート。デッキのフォントと色になっている" /></p>
<p>同じツイートだけど、さっきのスクリーンショットとは見た目が違う。アイコンもXのロゴも入っていないし、フォントはデッキのもの。本物そっくりではないかわりに、スライドの中で浮かない。</p>
<p>oEmbedのAPIが返してくるJSONには、投稿者名や日付といったデータに混じって、<code>html</code>というフィールドが入っている。中身は結局あの公式の埋め込みコードで、blockquoteがそのまま文字列で入っている。これをスライドに貼れば手っ取り早いけど、そうはしていない。</p>
<p>このHTMLは表示するためのものとしては扱わず、データとして読むだけにしている。本文とリンクだけを抜き出して、あとは投稿者名などのJSONのフィールドと組み合わせ、カードのHTMLはPeitho側で組み立て直す。抜き出した文字列はすべてエスケープしてから埋める。Xから受け取ったHTMLが、そのままの形で出力に出ていくことはない。自分で書いたHTMLすらビルドエラーで弾いているのに、外から取ってきたHTMLを素通しするのでは筋が通らない。</p>
<h3>汎用のoEmbedカード</h3>
<p>X以外のURLをオプションなしで書くと、汎用のカードになる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="s">```embed
</span></span></span><span class="line"><span class="cl">https://www.youtube.com/watch?v=dQw4w9WgXcQ
</span></span><span class="line"><span class="cl"><span class="s">```</span>
</span></span></code></pre><p>ビルド時にまずそのページのHTMLを取ってきて、<code>&lt;link rel=&quot;alternate&quot; type=&quot;application/json+oembed&quot;&gt;</code>という行を探す。oEmbedに対応しているサイトは、埋め込み用のデータをどこから取れるかを、この形でページ内に書いている。あとはそこに書かれているURLを叩けば、カードに必要なデータが返ってくる。</p>
<p>なので、Peithoの側に「対応サービス一覧」のようなものはない。YouTubeやMastodonを名指しで書いてあるわけではなく、この行が置いてあるページなら何でも通る。</p>
<p>返ってきたデータにサムネイル画像が入っていればそれを使ったカードに、入っていなければタイトルと投稿者とサービス名（「YouTube」など）だけのテキストカードになる。</p>
<p><img src="/images/2026/08/peitho-embed-generic-card.png" alt="YouTubeの動画がサムネイル付きのカードとして描画されたスライド" /></p>
<p>サムネイルはビルド時にダウンロードして、自分で<code>![]()</code>で貼った画像と同じ扱いで、ハッシュ名のローカルアセットとして公開される。リンク先は常にデッキに書いたURLで、oEmbedのレスポンスに入っているURLで上書きはしない。</p>
<p>こっちはスクリーンショットを撮らない。返ってくる<code>html</code>も使っていない。YouTubeのそれは動画プレイヤーのiframeで、Mastodonのそれは「View on Mastodon」と書かれた枠と、それを実際の投稿に置き換えるスクリプトのセットになっている。どちらも表示にはネットワークかスクリプトが要るので、ビルド時に静的な成果物を作る、という方針とは噛み合わない。かといって、Xと同じようにスクリーンショットを撮る方向にも行きにくい。Xの公式ウィジェットは描画が終わったことを自分で教えてくれるので、それを待ってから撮ればいい。ところが一般のiframeにそういう仕組みはない。読み込みが終わったことは分かっても、中身が描画し終えたかどうかは外から知りようがないので、適当に何秒か待って撮る、みたいな作りになってしまう。なので汎用側は常にPeithoが組み立てたカード、と割り切った。</p>
<hr />
<h2>URLを1つ書くだけの記法にした</h2>
<p>記法をどうするかは少し考えた。</p>
<p>本文中にURLをそのまま書いたら自動で埋め込みになる、という案は却下した。「ここはただのリンクにしたい」と区別がつかないし、書いていないことが黙って起きるのはPeithoの方針に合わない。</p>
<p><code>![](https://...)</code>の画像記法を拡張する案も却下。Peithoの<code>![]()</code>はリモートURLを受け付けないし、拡張子もpngやjpgといった画像のものしか受け付けない。ツイートのURLを書く場所としては、どちらの条件にも合っていない。</p>
<p>結果として<code>embed</code>という専用のフェンスになった。既に<code>mermaid</code>や<code>math</code>がフェンスのタグで組み込みのレンダラに振り分けられる仕組みがあるので、そこに<code>embed</code>を足す形で収まっている。<code>code_images.embed:</code>で外部コマンドに差し替えるのも、他のタグと同じようにできる。</p>
<p>オプションをどこに書くかも、実装の途中で変えた。最初は本文の、URLの次の行に書く案だった。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="s">```embed
</span></span></span><span class="line"><span class="cl">https://x.com/gosukenator/status/2074821309259973046
</span></span><span class="line"><span class="cl">mode: card
</span></span><span class="line"><span class="cl"><span class="s">```</span>
</span></span></code></pre><p>これだと本文の中に「URLの行」と「オプションの行」という2つの文法が混ざる。今は<code>embed</code>と書いている行に、続けて書くようにしている。</p>
<pre class="chroma"><code><span class="line"><span class="cl">``<span class="sb">`embed mode=card
</span></span></span><span class="line"><span class="cl"><span class="sb">https://x.com/gosukenator/status/2074821309259973046
</span></span></span><span class="line"><span class="cl"><span class="sb">`</span>``
</span></span></code></pre><p>本文は常にURLが1行だけ、という状態を保てるし、後から<code>theme</code>や<code>width</code>を足すときも文法を作り直さなくて済む。</p>
<hr />
<h2>キャッシュの置き場所</h2>
<p>取ってきたものは<code>.peitho/embeds-cache/</code>に置いている。スクリーンショットのPNG、oEmbedのレスポンス、ダウンロードしたサムネイル画像がここに入る。全部揃っていればオフラインでビルドできるし、足りないものだけを取りに行く。</p>
<p>有効期限のようなものはなく、勝手に取り直したりもしない。更新したいときは消す。何かに失敗したときのエラーには、消すべきキャッシュファイルのパスをそのまま出すようにしてある。</p>
<hr />
<p>サンプルデッキは<a href="https://peitho.gosu.ke/examples/tweet-embed/">Tweet Embedの例</a>に置いてある。3枚のスライドで、スクリーンショット、カードモード、汎用のoEmbedカードがそれぞれ出る。</p>
<p>参考:</p>
<ul>
<li><a href="https://github.com/mizzy/peitho">mizzy/peitho</a></li>
<li><a href="https://peitho.gosu.ke/">デモサイト peitho.gosu.ke</a></li>
<li><a href="https://peitho.gosu.ke/examples/tweet-embed/">Tweet Embedのサンプルデッキ</a></li>
<li><a href="/blog/2026/08/02/1/">前回の記事: peitho lintに検査を2つ足した</a></li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Qwen3のトークナイザーは日本語をどう扱うか]]></title>
        <link href="https://mizzy.org/blog/2026/08/02/3/"/>
        <updated>2026-08-02T11:59:12+09:00</updated>
        <id>https://mizzy.org/blog/2026/08/02/3/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/08/02/2/">前の記事</a>でPyxisにトークナイザーを組み込んだ。その過程でQwen3の<code>tokenizer.json</code>の中身をあれこれ覗いていたのだが、日本語の扱いが思っていたのと違っていて面白かったので、そこだけ切り出して書く。</p>
<p>前提として、BPEやbyte-level BPE、pre-tokenizerといった用語は前の記事で説明しているので、そちらを読んでいる前提で進める。分割の実験にはHuggingFaceの<a href="https://github.com/huggingface/tokenizers">tokenizers</a>を使っている。</p>
<hr />
<h2>日本語ではほぼ区切られない</h2>
<p>トークナイザーはBPEでマージする前に、pre-tokenizerという処理で正規表現を使ってテキストを大まかに区切る。単語の境界をまたいだマージが起きないようにするための仕組みで、Qwen3では次の正規表現が使われている。</p>
<pre><code>(?i:'s|'t|'re|'ve|'m|'ll|'d)|[^\r\n\p{L}\p{N}]?\p{L}+|\p{N}| ?[^\s\p{L}\p{N}]+[\r\n]*|\s*[\r\n]+|\s+(?!\S)|\s+
</code></pre>
<p>これが日本語に対してどう働くのか、実際にかけてみた。</p>
<pre><code>'I like this' -&gt; ['I', ' like', ' this']
'こんにちは、世界' -&gt; ['こんにちは', '、世界']
'私はラーメンを食べた' -&gt; ['私はラーメンを食べた']
</code></pre>
<p>英語は3つに区切られるのに、日本語は文が丸ごと1つの塊になっている。</p>
<p>理由は主力パターンの<code>\p{L}+</code>にある。<code>\p{L}</code>はUnicodeの「文字」カテゴリで、漢字もひらがなもカタカナもすべて含まれる。英語は単語の間にスペースがあり、スペースは<code>\p{L}</code>ではないのでそこで連続が途切れて区切りになるが、日本語は分かち書きしないので全部つながったままマッチする。<code>こんにちは、世界</code>が2つに割れたのは読点<code>、</code>が文字ではないためで、つまり日本語で区切りが入るのは句読点や記号の位置だけ。</p>
<p>ということは、pre-tokenizerの「マージが単語境界をまたがないようにする」という働きが、日本語ではほとんど効いていない。文全体が1つの塊としてBPEに渡され、その中で自由にマージが起きる。実際にトークン化してみるとこうなる。</p>
<pre><code>'これはペンです' -&gt; ['これは', 'ペン', 'です']
'今日はいい天気ですね' -&gt; ['今日は', 'いい', '天', '気', 'ですね']
</code></pre>
<p><code>これは</code>や<code>今日は</code>のように名詞と助詞がくっついたトークン、<code>ですね</code>のような活用語尾のトークンができている。英語なら<code>like this</code>が1トークンにならないよう防いでいたのと同じ種類のマージが、日本語では普通に起きているということ。</p>
<hr />
<h2>文字の途中で切れるトークン</h2>
<p>もうひとつ面白い例。</p>
<pre><code>'私はラーメンを食べた' -&gt; ['私は', 'ラ', 'ー�', '�', 'ン', 'を', '食べた']
</code></pre>
<p><code>ラーメン</code>の部分が文字化けしている。これはトークンの境界が「メ」という1文字の<strong>途中</strong>で切れているため。「メ」はUTF-8で<code>E3 83 A1</code>の3バイトだが、前半<code>E3 83</code>が<code>ー</code>と一緒に1つのトークンになり、残りの<code>A1</code>が単独で次のトークンになっている。どちらも単体では正しいUTF-8にならないので、個別にデコードすると<code>�</code>（U+FFFD、不正なバイト列を表す置換文字）が出てくる。byte-level BPEはバイト単位でマージするので、こういう文字の途中で切れるトークンが生まれることがある。もちろん全部繋げてデコードすればちゃんと「私はラーメンを食べた」に戻る。</p>
<p>これは生成のときに効いてくる。トークンを1個ずつ生成しながら順次表示していく場合、<code>ー�</code>のような中途半端なトークンをその時点で表示すると文字化けする。生成が進んで次のトークンが来ればバイトが揃って正しい文字になるので、ストリーミング表示するにはデコードできないバイトを次まで持ち越す必要がある。<code>tokenizers</code>にはこのための<code>decode_stream</code>というAPIがある。</p>
<p>これは他人事ではなく、実は次回書く生成ループが<code>decode(&amp;[next_token_id])</code>とトークンを1個ずつ単独でデコードして表示していて、まさにこの問題を踏んでいる。英語のプロンプトで動かしている限りは表面化しないので、そのまま気づかず書いていた。</p>
<hr />
<h2>トークン効率</h2>
<p>ついでにトークン効率も測ってみた。同じ内容の文で比べるとこうなる。</p>
<table>
<thead>
<tr>
<th>文</th>
<th>文字数</th>
<th>トークン数</th>
<th>文字/トークン</th>
</tr>
</thead>
<tbody>
<tr>
<td>The quick brown fox jumps over the lazy dog.</td>
<td>44</td>
<td>10</td>
<td>4.40</td>
</tr>
<tr>
<td>素早い茶色の狐が怠けた犬を飛び越える。</td>
<td>19</td>
<td>16</td>
<td>1.19</td>
</tr>
</tbody>
</table>
<p>英語は1トークンで4文字ぶん運べるのに、日本語は1文字ちょっと。ここだけ見ると日本語がかなり不利に見える。</p>
<p>ただしこの指標はフェアではない。分母の「文字数」の重みが言語によって違うからだ。<code>kitchen</code>は7文字だが「台所」は2文字で、同じ意味を運ぶのに必要な文字数がそもそも3倍以上違う。日本語は少ない文字数で書ける分、文字数で割ると値が小さく出る。1.19という数字は「日本語は1トークンで1文字ぶんの意味しか運べない」という意味ではない。</p>
<p>そこで、意味の対応する単語同士で、何トークンになるかを比べてみた。</p>
<table>
<thead>
<tr>
<th>英語</th>
<th>トークン数</th>
<th>日本語</th>
<th>トークン数</th>
</tr>
</thead>
<tbody>
<tr>
<td>kitchen</td>
<td>2</td>
<td>台所</td>
<td>2</td>
</tr>
<tr>
<td>government</td>
<td>1</td>
<td>政府</td>
<td>1</td>
</tr>
<tr>
<td>yesterday</td>
<td>2</td>
<td>昨日</td>
<td>1</td>
</tr>
<tr>
<td>understand</td>
<td>2</td>
<td>理解</td>
<td>1</td>
</tr>
<tr>
<td>difficult</td>
<td>2</td>
<td>難しい</td>
<td>1</td>
</tr>
</tbody>
</table>
<p>意外なことに、日本語のほうが少ないくらいだった。<code>yesterday</code>や<code>difficult</code>が2トークンなのに「昨日」「難しい」は1トークンで収まっている。Qwen3は中国語圏のモデルということもあってか、漢字を含む語彙はかなり充実している。</p>
<p>一方で明確に弱いのがカタカナ語で、<code>トークナイザー</code>は4トークンに割れる（<code>tokenizer</code>は1トークン）。日本語のトークン数が増えるのは、語彙が貧弱だからというより、こうした外来語や、送り仮名・活用語尾のような細かい単位が積み重なるからだと思う。</p>
<p>文単位でも測ってみたが、こちらは文によって傾向がかなり違った。</p>
<table>
<thead>
<tr>
<th>日本語の文</th>
<th>英語</th>
<th>日本語</th>
</tr>
</thead>
<tbody>
<tr>
<td>私は駅に行って切符を買った。</td>
<td>10</td>
<td>9</td>
</tr>
<tr>
<td>ご質問があればお知らせください。</td>
<td>10</td>
<td>9</td>
</tr>
<tr>
<td>このライブラリは高速なBPEトークナイザーを提供する。</td>
<td>9</td>
<td>17</td>
</tr>
</tbody>
</table>
<p>素の和文なら英語より少ないこともあるが、カタカナ語が入ると一気に膨らむ。技術文書は外来語だらけなので、体感として日本語は不利、というのはこのあたりが効いていそう。APIの課金はトークン単位なので、そのままコスト差になる。</p>
<hr />
<h2>カタカナを漢語にすると縮む</h2>
<p>カタカナの弱さが分かりやすく出るのが「日本語のトークン化」という文字列。9文字で7トークンに割れる。</p>
<pre><code>日本 / 語 / の / トー / ク / ン / 化
</code></pre>
<p>「日本」は2文字で1トークンにまとまっているのに、「トークン」の4文字は<code>トー</code> <code>ク</code> <code>ン</code>と3トークンに分解されている。</p>
<p>試しに同じ意味を漢語で書き換えてみると、差がはっきりする。</p>
<pre><code>日本語の字句解析 -&gt; 日本 / 語 / の / 字 / 句 / 解析
</code></pre>
<p>8文字で6トークン。文字数はほとんど変わらないのに、カタカナを漢語にするだけでトークンが1つ減る。「解析」が2文字で1トークンに収まる一方、「トークン」は4文字使って3トークンかかっている。</p>
<p>LLMに投げるプロンプトを短くしたいとき、カタカナ語を漢語に言い換えると効く場面がありそうだが、そこまでして削る価値があるかは場合によりそう。</p>
<hr />
<p>トークナイザーの実装そのものについては<a href="/blog/2026/08/02/2/">前の記事</a>に書いた。次回は出力ヘッドとサンプリングを実装して、テキストからテキストへの推論をつなげる予定。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Pyxisの開発: トークナイザー]]></title>
        <link href="https://mizzy.org/blog/2026/08/02/2/"/>
        <updated>2026-08-02T11:59:11+09:00</updated>
        <id>https://mizzy.org/blog/2026/08/02/2/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/07/23/1/">前回の記事</a>でTransformer本体ができた。ただしこの時点でPyxisが受け取れるのはトークンIDの列だけで、テキストを渡すことはできない。今回はその入口にあたるトークナイザーを組み込んだ。対応するPRは<a href="https://github.com/mizzy/pyxis/pull/23">#23</a>。</p>
<p>BPEそのものについては<a href="/blog/2026/07/01/1/">embeddingの記事</a>で一度書いたので、今回はその先、実際のトークナイザーファイルの中に何が入っていて、テキストがどういう順序でトークンIDになるのかを見ていく。</p>
<hr />
<h2>トークナイザーは訓練済みのデータ</h2>
<p>トークナイザーというと文字列を分割するロジックを思い浮かべるが、モデルと一緒に配布される<code>tokenizer.json</code>の中身は<strong>全部データ</strong>だった。</p>
<p>BPEは「訓練データ中で隣り合う頻出ペアを繰り返しマージする」というアルゴリズムだが、これはトークナイザーを作るとき（訓練時）に済ませておく処理で、推論時に走らせるわけではない。推論時に必要なのは、その訓練の結果として得られた2つのテーブル。</p>
<ul>
<li><strong>vocab</strong>: トークン文字列 → トークンIDの対応表</li>
<li><strong>merges</strong>: どのペアをどの順番でマージするか、という規則の列</li>
</ul>
<p>Qwen3-1.7Bの<code>tokenizer.json</code>を実際に覗いてみると、vocabが151643エントリ、mergesが151387エントリ入っていた。ファイルサイズは11MBほどある。</p>
<p>このファイルには他に<code>normalizer</code>、<code>pre_tokenizer</code>、<code>decoder</code>といった項目もあるが、中身はこんな調子。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="s2">&#34;normalizer&#34;</span><span class="err">:</span> <span class="p">{</span><span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;NFC&#34;</span><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="s2">&#34;decoder&#34;</span><span class="err">:</span> <span class="p">{</span><span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;ByteLevel&#34;</span><span class="p">,</span> <span class="nt">&#34;add_prefix_space&#34;</span><span class="p">:</span> <span class="kc">false</span><span class="p">,</span> <span class="nt">&#34;trim_offsets&#34;</span><span class="p">:</span> <span class="kc">false</span><span class="p">,</span> <span class="nt">&#34;use_regex&#34;</span><span class="p">:</span> <span class="kc">false</span><span class="p">}</span>
</span></span></code></pre><p>処理の名前と、必要ならそのパラメータが書いてあるだけで、処理そのものは書かれていない。<code>{&quot;type&quot;: &quot;NFC&quot;}</code>は「Unicode正規化のNFCをかけろ」という指示であって、NFCが何をするかはこのファイルの外にある。つまり<code>tokenizer.json</code>が持っているのは、どの処理を使えという指示と、訓練で得られたテーブルだけ。処理の中身は、これを読む側が実装しておく必要がある。</p>
<p>mergesの先頭はこうなっている。</p>
<pre><code>[&quot;Ġ&quot;, &quot;Ġ&quot;], [&quot;ĠĠ&quot;, &quot;ĠĠ&quot;], [&quot;i&quot;, &quot;n&quot;], [&quot;Ġ&quot;, &quot;t&quot;], [&quot;ĠĠĠĠ&quot;, &quot;ĠĠĠĠ&quot;], [&quot;e&quot;, &quot;r&quot;]
</code></pre>
<p><code>Ġ</code>は半角スペースを表す記号（後述）。スペース2個、スペース4個、<code>in</code>、<code> t</code>、スペース8個、<code>er</code>という順に並んでいる。訓練データ中でこの順に頻出だった、ということがそのまま規則の順序になっている。インデント由来と思われるスペースの連結が上位を占めているのが、いかにも実データという感じで面白い。</p>
<hr />
<h2>マージを追ってみる</h2>
<p>「マージ」というのは、隣り合った2つの要素をくっつけて1つにする操作のこと。訓練時は「データ中で最も頻出するペアをマージする」を繰り返し、マージしたペアを順にmergesへ書き足していく。Qwen3では151387回繰り返した結果があのテーブル、ということになる。</p>
<p>一方、推論時はこのテーブルを引く側になる。入力に対して<strong>mergesに載っているマージだけを適用していく</strong>、という手順になる。</p>
<ol>
<li>入力をバラバラの要素に分解する</li>
<li>隣り合うペアのうち、mergesに載っているものを探す</li>
<li>複数見つかったら、mergesの中で順位が最も早いものをマージする</li>
<li>2に戻る。どのペアも載っていなければ終了</li>
</ol>
<p><code>like</code>で追ってみる。Qwen3のmergesを実際に引くと、<code>(l,i)</code>が486番目、<code>(i,k)</code>が1323番目、<code>(k,e)</code>が184番目、<code>(li,ke)</code>が4547番目だった。</p>
<pre><code>l i k e
  ↓ (k,e) が184番目で最も早い
l i ke
  ↓ (l,i) が486番目
li ke
  ↓ (li,ke) が4547番目
like
</code></pre>
<p>3回のマージで1トークンになった。ここで注意したいのは、左から順に処理しているわけではないこと。最初にくっつくのは<code>li</code>ではなく<code>ke</code>で、あくまでmergesの順位が早い順に適用される。<code>(i,k)</code>も1323番目に載ってはいるが、先に<code>(k,e)</code>と<code>(l,i)</code>が適用されて<code>i</code>と<code>k</code>が隣り合わなくなるので、出番が来ないまま終わる。</p>
<p>ここで<code>like</code>が1トークンまで到達できたのは、必要なマージが3つとも載っていたから。載っていないペアに当たれば、そこでマージが尽きて複数トークンのまま終わる。</p>
<hr />
<h2>byte-level BPE</h2>
<p>embeddingの記事でBPEを説明したときに「最初は1文字ずつの辞書から始めて」と書いたが、現代のLLMのトークナイザーは正確には文字単位ではなく<strong>バイト単位</strong>から始める。byte-level BPEと呼ばれる方式で、GPT-2で採用されて以降、広く使われている。</p>
<p>まず前提として、トークンの種類は無制限に増やせるわけではない。トークナイザーを作るときに「何種類まで」という上限を決めていて、これを<code>vocab_size</code>と呼ぶ。Qwen3-1.7Bなら151936で、トークンIDは0から151935の範囲に収まる（先ほどのvocabの151643に特殊トークン26個を足しても151669にしかならないが、残りは128の倍数に切り上げるための余白で、実際には使われていない）。</p>
<p>なぜ上限が要るかというと、この数がそのままモデルのサイズに効いてくるため。モデルはトークンIDを受け取ると、<a href="/blog/2026/07/01/1/">embeddingテーブル</a>からそのIDに対応するベクトルを引いて計算を始める。このテーブルはトークン1つにつき1行を持つ<code>vocab_size × hidden_dim</code>の行列なので、トークンの種類が増えればその分だけ行が増える。Qwen3-1.7Bは<code>hidden_dim</code>が2048なので、151936行 × 2048列で約3.1億個の数値がこのテーブルに入っていることになる。この「訓練で学習された数値ひとつひとつ」がパラメータで、モデル名の「1.7B」もパラメータの総数（17億）を指している。</p>
<p>3.1億がどれくらいかというと、Transformerブロック1層分（アテンション+FFNで約5000万）の6層分に相当する。ブロックは全部で28層なので、embeddingテーブル1枚でその2割強を別に抱えている勘定。トークンの種類を増やすコストはそれなりに重い。</p>
<p>その前提で文字単位のBPEを考えると、初期辞書にUnicodeの全文字を入れる必要がある。Unicodeで割り当て済みのコードポイントは29万個以上あり、そのうち文字（Letter）だけでも14万個ある。15万の枠のうち14万を初期辞書が占めてしまい、マージして語彙を育てる余地がほとんど残らない。かといって一部の文字だけを入れると、辞書にない文字が来たときに表現できず、<code>&lt;unk&gt;</code>（unknown）という「知らない文字」トークンに潰すしかない。</p>
<p>byte-level BPEでは、テキストをUTF-8のバイト列に変換してから、その256種類のバイトを初期辞書にする。どんな文字列も最終的にはバイトの列なので、初期辞書が256個あればあらゆる入力を表現できる。絵文字だろうが未知の言語だろうが、必ず何らかのトークン列になり、<code>&lt;unk&gt;</code>が原理的に発生しない。</p>
<p>ここでひとつ問題がある。バイト値の中には制御文字（改行、タブなど）や、そもそも単体では正しいUTF-8にならないバイトが含まれていて、これらをそのまま辞書のキーとしてJSONに書き出すのは扱いにくい。そこでGPT-2では、256個のバイトそれぞれを「印字可能なUnicode文字」に1対1で対応づけてから辞書に載せる、という方式を取っている。</p>
<p><code>tokenizer.json</code>の中で見かける<code>Ġ</code>はこれ。半角スペース（バイト値0x20）に対応づけられた文字で、スペースそのものではない。同様に<code>Ċ</code>が改行に対応する。日本語のトークンが<code>ãģĵãĤĵãģ«ãģ¡ãģ¯</code>のような文字化けした見た目になっているのも同じ理由で、これは「こんにちは」のUTF-8バイト列を1バイトずつこの対応表で変換した結果。文字化けしているのではなく、そういう表記法になっている。</p>
<hr />
<h2>前処理の正規表現</h2>
<p>もうひとつ、BPEの前段に<strong>pre-tokenizer</strong>という処理が入る。</p>
<p>先ほどのマージの手順を、テキスト全体に対して無制限にかけると困ったことが起きる。<code>I like this</code>をバラすと<code>I ␣ l i k e ␣ t h i s</code>となるが、ここでは<code>like</code>の<code>e</code>と次の<code>␣</code>も「隣り合うペア」になってしまう。訓練データに<code>like this</code>が頻出していればこのペアがマージされ、単語の境界をまたいだ塊が1トークンになりうる。</p>
<p>これを防ぐのがpre-tokenizer。BPEにかける前に、正規表現でテキストを大まかに区切っておく。</p>
<pre><code>[I] [␣like] [␣this]
</code></pre>
<p>そしてマージは、この区切られた塊の中だけで行う。塊が違えば隣り合っているとみなさないので、<code>e</code>と<code>␣</code>のペアはそもそも候補に上がらず、<code>like this</code>という1トークンは原理的に生成されない。</p>
<p>区切っておく利点は他にもある。<code>like this</code>や<code>like that</code>のような組み合わせを辞書に入れ始めると際限がなく、15万しかない語彙の枠を食い潰してしまう。また、<code>like</code>が単独では1トークン、<code>like this</code>の中では別トークンの一部、となるとモデルから見た単語の同一性も崩れる。</p>
<p>Qwen3の<code>tokenizer.json</code>に入っている正規表現がこれ。</p>
<pre><code>(?i:'s|'t|'re|'ve|'m|'ll|'d)|[^\r\n\p{L}\p{N}]?\p{L}+|\p{N}| ?[^\s\p{L}\p{N}]+[\r\n]*|\s*[\r\n]+|\s+(?!\S)|\s+
</code></pre>
<p>パイプ区切りで、先頭から順に:</p>
<ol>
<li><code>'s</code>や<code>'ll</code>などの英語の短縮形</li>
<li>直前に記号を1つ許した文字の連続（<code>\p{L}</code>はUnicodeの「文字」カテゴリ）— スペース+単語がここで1つの塊になる</li>
<li>数字1桁（<code>\p{N}</code>）</li>
<li>スペースを1つ許した記号の連続</li>
<li>改行、末尾のスペース、その他の空白</li>
</ol>
<p>3番目が「数字1桁」なのがポイントで、これによって数字は必ず1桁ずつのトークンに分割される。<code>2026</code>は<code>2</code>, <code>0</code>, <code>2</code>, <code>6</code>の4トークン、<code>1234567</code>なら7トークンになる。<a href="https://arxiv.org/abs/2309.16609">Qwen Technical Report</a>にある「数字を1桁ずつに分割するようにした」というのは、辞書側ではなくこの正規表現で実現されている。</p>
<p>2番目のパターンで単語の直前のスペースが単語側に取り込まれるので、英語では「スペース+単語」が1トークンになりやすい。<code>Ġworld</code>のようなトークンが辞書にあるのはそのため。</p>
<hr />
<h2>実際に分割してみる</h2>
<p>実際のQwen3のトークナイザーで試してみた結果。</p>
<pre><code>'Hello'
  → ['Hello']  → [9707]

'Hello, Pyxis!'
  → ['Hello', ',', 'ĠPy', 'xis', '!']  → [9707, 11, 5355, 7191, 0]

'こんにちは'
  → ['ãģĵãĤĵãģ«ãģ¡ãģ¯']  → [89015]

'日本語のトークン化'
  → ['æĹ¥æľ¬', 'èªŀ', 'ãģ®', 'ãĥĪãĥ¼', 'ãĤ¯', 'ãĥ³', 'åĮĸ']
  → [101059, 102819, 15767, 137084, 28120, 15698, 32108]
</code></pre>
<p><code>Hello</code>は1トークンだが、<code>Pyxis</code>は<code>ĠPy</code> + <code>xis</code>の2トークンに割れている。<code>(ĠPy, xis)</code>というペアがmergesに載っていないためで、そこでマージが尽きて止まっている。頻出する単語ほど訓練時に深くマージされてテーブルを辿りきり1トークンに収まるが、珍しい単語は途中で尽きて複数トークンのまま残る、というBPEの性質がそのまま出ている。<code>ĠPy</code>の先頭に<code>Ġ</code>が付いているのは、前段の正規表現で直前のスペースが取り込まれたため。</p>
<p>日本語は「こんにちは」が1トークンなのに対して、「日本語のトークン化」は9文字で7トークンに割れている。トークンごとに区切るとこうなる。</p>
<pre><code>日本 / 語 / の / トー / ク / ン / 化
</code></pre>
<p>「日本」は2文字で1トークンにまとまっているのに、「トークン」の4文字は<code>トー</code> <code>ク</code> <code>ン</code>と3トークンに分解されている。日本語の扱いはこれ以外にも調べていて面白かったので、<a href="/blog/2026/08/02/3/">別記事</a>にまとめた。</p>
<hr />
<h2>特殊トークン</h2>
<p>BPEのvocabは151643エントリだが、その後ろに特殊トークンが26個、<code>tokenizer.json</code>の<code>added_tokens</code>として別途定義されている。IDは151643から151668まで。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;id&#34;</span><span class="p">:</span> <span class="mi">151643</span><span class="p">,</span> <span class="nt">&#34;content&#34;</span><span class="p">:</span> <span class="s2">&#34;&lt;|endoftext|&gt;&#34;</span><span class="p">,</span> <span class="nt">&#34;special&#34;</span><span class="p">:</span> <span class="kc">true</span><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;id&#34;</span><span class="p">:</span> <span class="mi">151644</span><span class="p">,</span> <span class="nt">&#34;content&#34;</span><span class="p">:</span> <span class="s2">&#34;&lt;|im_start|&gt;&#34;</span><span class="p">,</span> <span class="nt">&#34;special&#34;</span><span class="p">:</span> <span class="kc">true</span><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;id&#34;</span><span class="p">:</span> <span class="mi">151645</span><span class="p">,</span> <span class="nt">&#34;content&#34;</span><span class="p">:</span> <span class="s2">&#34;&lt;|im_end|&gt;&#34;</span><span class="p">,</span> <span class="nt">&#34;special&#34;</span><span class="p">:</span> <span class="kc">true</span><span class="p">}</span>
</span></span></code></pre><p><code>&lt;|im_start|&gt;</code>と<code>&lt;|im_end|&gt;</code>はチャット形式のマークアップで、会話のロール（system/user/assistant）の区切りを表す。OpenAIのChatMLに由来する形式で、Qwen3のchat templateもこれを使っている。このうち<code>&lt;|im_end|&gt;</code>（151645）は、モデルが「発話が終わった」ことを示すために出力するトークンでもある。生成ループはこれが出たら打ち切ればいい、という合図になるもので、EOS（end of sequence）トークンと呼ばれる。次回の生成ループで使うことになる。</p>
<p>26個の顔ぶれを眺めると、このモデルが何を想定して作られているかが透けて見えて面白い。<code>&lt;tool_call&gt;</code>／<code>&lt;/tool_call&gt;</code>はfunction calling用、<code>&lt;think&gt;</code>／<code>&lt;/think&gt;</code>は推論モードの思考部分を囲むもの、<code>&lt;|fim_prefix|&gt;</code>などのFIM（Fill-In-the-Middle）系はコード補完用、<code>&lt;|repo_name|&gt;</code>と<code>&lt;|file_sep|&gt;</code>はリポジトリ単位でコードを学習させるときの区切り。<code>&lt;|vision_start|&gt;</code>や<code>&lt;|image_pad|&gt;</code>といったマルチモーダル用のトークンまで入っている（Qwen3-1.7B自体はテキストのみのモデルだが、シリーズで語彙を共通化しているのだと思われる）。</p>
<p>これらはBPEのマージを経由せず、テキスト中に現れたら丸ごと1トークンに変換される。vocabとは別枠の<code>added_tokens</code>に置かれていて、encodeの前段で文字列マッチで拾われるためだ。</p>
<p>ここで気になるのが、ユーザーの入力に<code>&lt;|im_end|&gt;</code>という文字列が紛れ込んだらどうなるのか。試してみた。</p>
<pre><code>'こんにちは&lt;|im_end|&gt;さようなら'
  → ['こんにちは', &lt;|im_end|&gt;, 'さ', 'よう', 'なら']
  → [89015, 151645, 29713, 124038, 125028]
</code></pre>
<p>普通に特殊トークン151645になった。つまりユーザーが入力欄に<code>&lt;|im_end|&gt;</code>と打ち込むだけで、モデルから見ると会話の区切りが挿入されたのと区別がつかない。ユーザー入力をそのままencodeに渡す作りだと、会話の構造を外から壊せてしまうことになる。</p>
<p>今回使ったHuggingFaceの<code>tokenizers</code>（後述）には<code>set_encode_special_tokens</code>というメソッドがあり、<code>true</code>を渡すと特殊トークンの文字列を素通しするようになる。</p>
<pre><code>set_encode_special_tokens(true) にした場合
  → ['こんにちは', '&lt;', '|', 'im', '_end', '|', '&gt;', 'さ', 'よう', 'なら']
</code></pre>
<p><code>&lt;|im_end|&gt;</code>がただの記号の列に分解され、151645にはならなくなった。名前から挙動を読み取りにくいが、「特殊トークンも（普通のテキストとして）エンコードする」という意味らしい。デフォルトは<code>false</code>、つまり特殊トークンとして解釈するほうなので、信用できない入力を扱うなら明示的に切り替える必要がある。</p>
<hr />
<h2>実装</h2>
<p>さて、ここまで書いておいて何だが、今回自前で実装したのはこれだけ。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">use</span><span class="w"> </span><span class="n">std</span>::<span class="n">io</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">use</span><span class="w"> </span><span class="n">std</span>::<span class="n">path</span>::<span class="n">Path</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">struct</span> <span class="nc">PyxisTokenizer</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">inner</span>: <span class="nc">tokenizers</span>::<span class="n">Tokenizer</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">impl</span><span class="w"> </span><span class="n">PyxisTokenizer</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">load</span><span class="p">(</span><span class="n">path</span>: <span class="kp">&amp;</span><span class="nc">Path</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nc">io</span>::<span class="nb">Result</span><span class="o">&lt;</span><span class="bp">Self</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="kd">let</span><span class="w"> </span><span class="n">inner</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">tokenizers</span>::<span class="n">Tokenizer</span>::<span class="n">from_file</span><span class="p">(</span><span class="n">path</span><span class="p">).</span><span class="n">map_err</span><span class="p">(</span><span class="n">io</span>::<span class="n">Error</span>::<span class="n">other</span><span class="p">)</span><span class="o">?</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nb">Ok</span><span class="p">(</span><span class="bp">Self</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="n">inner</span><span class="w"> </span><span class="p">})</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">encode</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">text</span>: <span class="kp">&amp;</span><span class="kt">str</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nb">Vec</span><span class="o">&lt;</span><span class="kt">u32</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="bp">self</span><span class="p">.</span><span class="n">inner</span><span class="p">.</span><span class="n">encode</span><span class="p">(</span><span class="n">text</span><span class="p">,</span><span class="w"> </span><span class="kc">false</span><span class="p">).</span><span class="n">unwrap</span><span class="p">().</span><span class="n">get_ids</span><span class="p">().</span><span class="n">to_vec</span><span class="p">()</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">decode</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">token_ids</span>: <span class="kp">&amp;</span><span class="p">[</span><span class="kt">u32</span><span class="p">])</span><span class="w"> </span>-&gt; <span class="nb">String</span> <span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="bp">self</span><span class="p">.</span><span class="n">inner</span><span class="p">.</span><span class="n">decode</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span><span class="w"> </span><span class="kc">true</span><span class="p">).</span><span class="n">unwrap</span><span class="p">()</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>HuggingFaceの<a href="https://github.com/huggingface/tokenizers">tokenizers</a> crateの薄いラッパー。21行。</p>
<p>Pyxisは「LLMの推論を自分で実装して理解する」というのが目的なので、本来ならBPEも自分で書くべきところだけど、ここは既存のcrateに任せることにした。理由は、トークナイザーは推論の本体ではなく前処理・後処理であること、そして<code>tokenizer.json</code>のフォーマット（normalizer、pre_tokenizer、decoderの組み合わせ）を正確に再現するのが、得られる理解に対して手間が大きすぎると判断したため。ここでの1バイトのズレは全く違うトークンIDになって、モデル本体が正しくても出力が壊れる。デバッグしたい対象はそこじゃない、という気持ちがある。</p>
<p><code>encode</code>の第2引数<code>false</code>は<code>add_special_tokens</code>で、<code>&lt;|im_start|&gt;</code>などを自動で付けないという指定。今回は素のテキストをそのまま推論に流したいので付けない。チャット形式で使うときはテンプレートを別途組み立てる必要があるが、それは後回し。</p>
<p><code>decode</code>の第2引数<code>true</code>は<code>skip_special_tokens</code>で、こちらは逆に特殊トークンを出力から除去する指定。生成結果に<code>&lt;|im_end|&gt;</code>のような文字列がそのまま混ざるのを防ぐ。</p>
<p><code>unwrap()</code>しているのは、<code>tokenizers</code>のエラー型が<code>Box&lt;dyn Error&gt;</code>で<code>io::Error</code>に素直に変換できないため。ここは後で見直すかもしれない。</p>
<p>依存は<code>default-features = false</code>にして<code>onig</code>だけを有効にしている。デフォルトでは<code>onig</code>に加えて<code>progressbar</code>（indicatif）と<code>esaxx_fast</code>（C++実装のsuffix array、トークナイザーの訓練で使う）が有効になるが、どちらも推論には要らないので切った。</p>
<hr />
<h2>テスト</h2>
<p>テストはこう書いた。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="cp">#[test]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">fn</span> <span class="nf">decode_roundtrips_with_encode</span><span class="p">()</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="nb">Some</span><span class="p">(</span><span class="n">tokenizer</span><span class="p">)</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">load_test_tokenizer</span><span class="p">()</span><span class="w"> </span><span class="k">else</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="k">return</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">};</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">input</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s">&#34;Hello, Pyxis!&#34;</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">token_ids</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">tokenizer</span><span class="p">.</span><span class="n">encode</span><span class="p">(</span><span class="n">input</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">output</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">tokenizer</span><span class="p">.</span><span class="n">decode</span><span class="p">(</span><span class="o">&amp;</span><span class="n">token_ids</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="fm">assert_eq!</span><span class="p">(</span><span class="n">output</span><span class="p">,</span><span class="w"> </span><span class="n">input</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>実際のQwen3-1.7Bの<code>tokenizer.json</code>を読み込んで、encodeしてdecodeすると元に戻ることを確認している。byte-level BPEは情報を落とさない変換なので、往復すれば必ず元通りになるはず、という性質のテスト。</p>
<p><code>load_test_tokenizer()</code>がOptionを返していて、ファイルが無ければテストを素通りさせている。11MBのモデルファイルをリポジトリに置くわけにもいかず、手元にモデルをダウンロードしていない環境ではスキップしたいため。ただしこれは「テストが通っている」のか「テストが走っていない」のかが区別できない書き方なので、あまり良くはない。CIを整備するときに考え直す予定。</p>
<hr />
<p>これで入口（テキスト → トークンID）ができた。あとは出口、つまりTransformerの出力ベクトルからトークンIDを選ぶ処理を書けば、テキストからテキストへの推論がつながる。次回は出力ヘッドとサンプリングを実装する。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[peitho lintに検査を2つ足した]]></title>
        <link href="https://mizzy.org/blog/2026/08/02/1/"/>
        <updated>2026-08-02T00:19:16+09:00</updated>
        <id>https://mizzy.org/blog/2026/08/02/1/</id>
        <content type="html"><![CDATA[
<p>自作プレゼンツール<a href="https://github.com/mizzy/peitho">Peitho</a>の話の続き。<a href="/blog/2026/07/18/2/">以前入れた</a><code>peitho lint</code>に検査を2つ足した。ひとつは文字が小さすぎないかの検査（<a href="https://github.com/mizzy/peitho/pull/386">PR #386</a>）、もうひとつはスロット単位のはみ出し検査（<a href="https://github.com/mizzy/peitho/pull/389">PR #389</a>）。</p>
<hr />
<h2>文字サイズの検査</h2>
<p>24ptを下回るテキストがあるスライドに警告を出すようにした。24ptという数字は、学会や大学が出している発表のアクセシビリティガイドラインでよく見る下限を採ったもの（<a href="https://www.arl.org/accessibility-guidelines-for-powerpoint-presentations/">ARLのガイドライン</a>は「All slides should use a minimum font size of 24 points.」と書いている）。とはいえ普遍的な基準というわけでもなくて、用途別に12pt以上でいいとする資料もあれば、会場が大きいなら30pt以上と言う資料もある。閾値は固定にしていて、今のところ設定で変えることはできない。必要になったらfrontmatterで指定できるようにすればいいので、まずは設定を増やさない側に倒している。</p>
<pre><code>warning: slide 3 has text at 18pt, below the recommended 24pt: &quot;Some long caption that was shrunk to f…&quot;
  help: raise the font size in the layout CSS, or move content to another slide instead of shrinking it
</code></pre>
<p>既存のはみ出し検査は「枠から出ている」という見た目の破綻を捕まえるので、手元で見れば分かる。一方こっちは、手元のディスプレイでは普通に読めてしまう。会場の後ろから読めないことに気づくのは本番中、というやつ。</p>
<p>小さくすること自体が悪いわけではなくて、意図的に縮めている場面はいくらでもある。ただ、意図的かどうかとは無関係に「後ろの席から読めるか」は決まるので、閾値を切って機械に見てもらう価値がある。</p>
<h3>CSSを読むのではなくブラウザに測らせる</h3>
<p>実装は、CSSの静的解析ではなく、既存のlintのヘッドレスChrome実行に相乗りする形にした。スライドごとにテキストノードを走査して、その親要素の計算済み<code>font-size</code>を読む。</p>
<p>CSSを解析する方向は最初から捨てた。原理的にはできなくはないんだろうけど、カスケードや継承を自前で解決して、<code>em</code>や<code>%</code>や<code>clamp()</code>を実効値まで畳んで……とやっていくと、結局ブラウザのやっていることを再実装することになる。しかもlintはもともとヘッドレスChromeを起動しているので、<code>getComputedStyle</code>を呼べば答えが返ってくる。</p>
<hr />
<h2>スロット単位のはみ出し検査</h2>
<p>もうひとつは、既存のはみ出し検査に空いていた穴を塞ぐもの。既存のものはスライドの枠からはみ出したかを見る検査で、今回足したのはその内側、スロットからはみ出したかを見る検査になる。</p>
<p>Peithoのスロットのコンテナには<code>overflow: hidden</code>が効いている。すると、あふれた中身は描画されないので、要素の大きさをブラウザに問い合わせても、あふれたぶんまで含んだ大きさは返ってこない。既存の検査はスライドの枠を基準に測っていたので、スロットの中で静かに切れているぶんには何も検出できていなかった。</p>
<p>どのくらい検出できていなかったかというと、こう。12項目の箇条書きを1枚に置いただけのデッキ（デフォルトのテーマとレイアウト）で、</p>
<p><img src="/images/2026/08/peitho-lint-slot-overflow.png" alt="12項目の箇条書きで最後の項目が下端で切れているスライド" /></p>
<p>12個目が半分に切れている。この状態で、以前の<code>peitho lint</code>は「はみ出しなし」と言っていた。スライドの枠自体は超えていないので、当時の測り方では何も起きない。</p>
<p>今は、要素ごとに「中身の本来の大きさ」（<code>scrollHeight</code>/<code>scrollWidth</code>）と「見えている領域の大きさ」（<code>clientHeight</code>/<code>clientWidth</code>）を比べて、実際にクリップが起きている要素を探している。差が出ていれば、そのぶんが隠れている。同じデッキがこう出る。</p>
<pre><code>warning: slide 1 content overflows the `body` slot vertically by 17px
  help: shrink or split the slide content, or adjust the layout CSS
</code></pre>
<h3>警告であってエラーではない</h3>
<p>これはビルドエラーにはしていない。Peithoは「黙って何かが起きる」経路を潰す方針で、パーサが未知の構造を飲み込むとか、スロット契約の違反を無視するとかはビルドエラーにしている。ただしそれらはPeitho自身が決められる、答えの決まっている話。</p>
<p>スロットのはみ出しはブラウザのレンダリング結果で、フォントやテーマCSSや環境で変わるし、意図的なこともある（長いログを枠に収めて見せる、とか）。判断が要るものは警告、というのが既存のはみ出し検査や文字サイズ検査と揃った扱いになる。</p>
<p>実際、これを入れたらPeitho自身の紹介に使っているサンプルデッキで、コードブロックが14px切れているスライドが見つかった。CSSを調整して直した。</p>
<hr />
<h2>報告されるpx数は多めに出る</h2>
<p>「17px切れている」と報告していても、実際に見えなくなっているテキストがちょうど17px分あるとは限らない。多めに出る。</p>
<p>はみ出した量として測っているものに、最後の段落の下マージンが混ざるため。マージンは何も描画しないので、そのぶんは「切れているテキスト」ではない。手元で試した例だと、47pxと報告されたうち、本当に見えなくなっていたのは24px分だった。</p>
<p>極端な話、テキストは全部収まっていてマージンだけが枠を越えている場合でも警告が出る。実際に描画されているものだけを測ろうとすると機構がだいぶ増えるので、今のところは受け入れている。</p>
<hr />
<p>参考:</p>
<ul>
<li><a href="https://github.com/mizzy/peitho">mizzy/peitho</a></li>
<li><a href="https://peitho.gosu.ke/">デモサイト peitho.gosu.ke</a></li>
<li><a href="https://www.arl.org/accessibility-guidelines-for-powerpoint-presentations/">ARL: PowerPoint Guidelines for Presenters</a> — 24ptの根拠にしたガイドライン</li>
<li><a href="/blog/2026/07/18/2/">前回のlintの記事: Peithoにスライドのはみ出し検査（peitho lint）を入れた</a></li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Peithoに段階表示（reveal）とコード行の強調を入れた]]></title>
        <link href="https://mizzy.org/blog/2026/08/01/1/"/>
        <updated>2026-08-01T23:36:55+09:00</updated>
        <id>https://mizzy.org/blog/2026/08/01/1/</id>
        <content type="html"><![CDATA[
<p>自作プレゼンツール<a href="https://github.com/mizzy/peitho">Peitho</a>の話の続き。発表中にスライドの内容を段階的に見せる機能をふたつ入れた。ひとつは箇条書きなどを1ステップずつ出す「段階表示」（<a href="https://github.com/mizzy/peitho/pull/341">PR #341</a>ほか）、もうひとつはコードブロックの特定の行を強調する機能（<a href="https://github.com/mizzy/peitho/pull/381">PR #381</a>）。</p>
<hr />
<h2>段階表示: <code>::: {reveal}</code></h2>
<p>箇条書きを一気に全部見せると、聞いている側は先の項目を読み始めてしまって、話している内容と視線がずれる。よくあるやつなので、<code>peitho present</code>で1ステップずつ出せるようにした。</p>
<p>書き方は<code>::: {reveal}</code>で囲むだけ。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="gh"># Agenda
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">::: {reveal}
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">-</span> Background
</span></span><span class="line"><span class="cl"><span class="k">-</span> Current problems
</span></span><span class="line"><span class="cl"><span class="k">-</span> Proposal
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">:::
</span></span></code></pre><p>グループの直下のブロックがそれぞれ1ステップになる。リストだけは例外で、トップレベルの項目1つが1ステップ、ネストした子項目は親と一緒に出る。グループの外に書いたものは常に見えている状態で、1枚のスライドに複数のグループを置いた場合はソース順にステップ番号が続く。</p>
<p>囲む対象は箇条書きに限らない。段落もコードブロックも画像も対象になるし、グループの中のブロックが別々のスロットに割り当たっても構わない（2カラムレイアウトで、テキストの後に画像を出す、みたいなことができる）。</p>
<p><code>:::</code>という記法は、既にPeithoにあった明示的スロット指定（<code>::: {slot=left}</code>）と同じもの。「これらのブロックはひとまとまりの並び」という構造の宣言であって、いつどう出すかはシェル側の仕事、という切り分けにしてある。</p>
<h3>配布物には出ない</h3>
<p>段階表示が効くのは<code>peitho present</code>だけで、<code>peitho preview</code>、PDFエクスポート、<code>peitho lint</code>、公開用の<code>dist/</code>は全部最終状態（全部見えている状態）になる。</p>
<p>これは実装上そうなる、というのが正しい。レンダラが吐くのは<code>data-reveal-step=&quot;N&quot;</code>という属性だけで、それを見て何かを隠すCSSは出力に入っていない。つまり生成されたHTMLは、そのまま開けば全部見えている状態になる。発表用のシェルだけがこの属性を見て<code>data-reveal-hidden</code>をトグルし、隠す処理を行う（<code>display</code>ではなく<code>visibility</code>で隠しているので、レイアウトは動かない）。</p>
<hr />
<h2>コード行の強調</h2>
<p>もうひとつはコードブロック。長いコードを「今この行の話をしています」と示したい、というやつ。</p>
<p>コードフェンスの言語タグの後ろに、波括弧で行番号を書く。</p>
<pre class="chroma"><code><span class="line"><span class="cl">``<span class="sb">`rust {3}
</span></span></span><span class="line"><span class="cl"><span class="sb">pub struct Deck&lt;Phase&gt; {
</span></span></span><span class="line"><span class="cl"><span class="sb">    slides: Vec&lt;Slide&gt;,
</span></span></span><span class="line"><span class="cl"><span class="sb">    _phase: PhantomData&lt;Phase&gt;,
</span></span></span><span class="line"><span class="cl"><span class="sb">}
</span></span></span><span class="line"><span class="cl"><span class="sb">`</span>``
</span></span></code></pre><p><code>|</code>で区切ると、グループごとに1ステップになって、強調が移動していく。</p>
<pre class="chroma"><code><span class="line"><span class="cl">``<span class="sb">`rust {2|4-6|9}
</span></span></span><span class="line"><span class="cl"><span class="sb">`</span>``
</span></span></code></pre><p>これで、ステップ1で2行目、ステップ2で4〜6行目、ステップ3で9行目、という具合。</p>
<p>実際の見た目はこんな感じ。強調されている行に背景色と左端のマーカーが付いて、それ以外の行は薄くなる。まずステップ1。</p>
<p><img src="/images/2026/08/peitho-code-emphasis-step1.png" alt="コード行強調のステップ1。2行目だけが強調されている" /></p>
<p>「次へ」を押すとステップ2。強調が4〜6行目に移り、さっきまで強調されていた2行目は他の行と同じように薄くなる。</p>
<p><img src="/images/2026/08/peitho-code-emphasis-step2.png" alt="コード行強調のステップ2。強調が4〜6行目に移動している" /></p>
<p>グループの中では<code>,</code>で列挙、<code>-</code>で範囲を指定できるので、<code>{2,5-7|9}</code>のような書き方もできる。言語タグは省略できて、<code>```{1,4-5}</code>のようにハイライトなしのブロックにも効く。</p>
<p><code>|</code>が無い静的な強調は「この行が重要な行です」というコンテンツの性質なので、PDFや公開出力にも入る。<code>|</code>付きの強調は話の進行を追うポインタなので、その任意の一瞬をPDFに焼き付けても、書いた本人が主張していないことを伝えてしまう。なので配布物では強調なしになる。</p>
<hr />
<p>参考:</p>
<ul>
<li><a href="https://github.com/mizzy/peitho">mizzy/peitho</a></li>
<li><a href="https://peitho.gosu.ke/examples/incremental-reveal/">段階表示のサンプルデッキ</a></li>
<li><a href="https://peitho.gosu.ke/examples/code-emphasis/">コード行強調のサンプルデッキ</a></li>
<li><a href="/blog/2026/07/31/2/">前回の記事: Peithoにデッキの言語指定（lang frontmatter）を入れた</a></li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Peithoにデッキの言語指定（lang frontmatter）を入れた]]></title>
        <link href="https://mizzy.org/blog/2026/07/31/2/"/>
        <updated>2026-07-31T19:11:44+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/31/2/</id>
        <content type="html"><![CDATA[
<p>自作プレゼンツール<a href="https://github.com/mizzy/peitho">Peitho</a>の話の続き。今回はデッキのfrontmatterに<code>lang</code>キーを追加して、生成されるHTMLの<code>&lt;html lang&gt;</code>属性を指定できるようにした話。対応するPRは<a href="https://github.com/mizzy/peitho/pull/339">#339</a>。</p>
<hr />
<h2><code>&lt;html lang=&quot;en&quot;&gt;</code>固定だった問題</h2>
<p>これまでPeithoが生成するHTMLは、すべて<code>&lt;html lang=&quot;en&quot;&gt;</code>で固定だった。日本語のデッキでも英語宣言のまま。セマンティクスとして正しくない、というだけならまだしも、実害もある。</p>
<p>Chromiumの<code>word-break: auto-phrase</code>は、テキストの言語に応じた解析を行い、自然な区切り（日本語なら文節）を優先して改行してくれるCSSプロパティ。解析には<a href="https://github.com/google/budoux">BudouX</a>（日本語・簡体字中国語・繁体字中国語・タイ語に対応した機械学習ベースの改行位置解析ライブラリ）のC++移植が使われている（<a href="https://developer.chrome.com/blog/css-i18n-features">Chrome 119の紹介記事</a>）。言語ごとの解析なので、効かせるには対象のテキストの言語が宣言されている必要があり、日本語のデッキなら要素かその祖先に<code>lang=&quot;ja&quot;</code>が要る。Peithoの出力は<code>&lt;html lang=&quot;en&quot;&gt;</code>で固定なので、テーマのCSSで<code>word-break: auto-phrase</code>を指定しても、そのままではどのスライドにも効かない。</p>
<p>逃げ道が全くないわけではない。<code>peitho build</code>の出力なら生成後のHTMLを書き換えて<code>lang=&quot;ja&quot;</code>にできるし、レイアウトHTMLは属性がそのまま出力されるので、カスタムレイアウトのルートの<code>&lt;section&gt;</code>に<code>lang=&quot;ja&quot;</code>を書けばスライド単位で言語を宣言することもできる。ただ、前者は<code>peitho preview</code>や<code>peitho present</code>には効かないし、後者は言語を伝えるためだけにレイアウトを自前で持つことになる。デッキの言語はレイアウトの都合ではなくデッキ自体の属性なので、frontmatterで宣言できるのが筋だろう、ということで今回の機能を入れた。</p>
<hr />
<h2>使い方</h2>
<p>デッキのfrontmatterに1行足すだけ。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="nn">---</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">lang</span><span class="p">:</span><span class="w"> </span><span class="l">ja</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nn">---</span><span class="w">
</span></span></span></code></pre><p>値はBCP 47スタイルの言語タグ（<code>en</code>、<code>ja</code>、<code>zh-Hans</code>など）。省略時のデフォルトは<code>en</code>で、既存のデッキの出力はバイト単位で変わらない。</p>
<p><code>lang</code>が付くのは、スライドのHTMLを本体として描画するページ全部。配布用の<code>index.html</code>、PDFエクスポート、<code>peitho lint</code>の検査用HTML、present、preview、発表者ツールの6種類。一方、スマホリモコンのページとpreviewのエラーページはPeitho自体のUIで中身が英語なので、<code>lang=&quot;en&quot;</code>のままにしている。</p>
<hr />
<p>参考:</p>
<ul>
<li><a href="https://github.com/mizzy/peitho">mizzy/peitho</a></li>
<li><a href="https://peitho.gosu.ke/">デモサイト peitho.gosu.ke</a></li>
<li><a href="/blog/2026/07/18/2/">前回の記事: Peithoにスライドのはみ出し検査（peitho lint）を入れた</a></li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[肘部管症候群の手術に向けてアレルギー科を受診した]]></title>
        <link href="https://mizzy.org/blog/2026/07/31/1/"/>
        <updated>2026-07-31T15:57:22+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/31/1/</id>
        <content type="html"><![CDATA[
<p>肘部管症候群シリーズの続き。<a href="/blog/2026/07/29/1/">以前の記事</a>で、入院前検査の肺機能検査で気になる数値があり、手術に支障はないレベルだけど、念のためアレルギー科で診てもらうことになっている、と書いた。その受診をしてきた話。</p>
<hr />
<h2>診察の内容</h2>
<p>受診したのは、手術を受けるのと同じ大病院のアレルギー科（呼吸器内科）。整形外科の医師からは、肺機能検査のどの数値が気になるのか、具体的に指摘されたわけではなかった。ただ、かなり前に肺活量の検査をしたときに、吐く力が弱い、と指摘されたことがあるのと、今回の肺機能検査でも、思いっきり一気に息を吐き出して1秒率（最初の1秒間で吐き出せる息の割合）を測る検査をやり直したりしていたので、そのあたりかな、という気がしていた。</p>
<p>アレルギー科での診察は、問診と確認のみで、追加の検査は特になし。肺機能検査の結果としては、閉塞性換気障害との指摘だった。閉塞性換気障害の原因にはたばこや喘息があるけど、たばこを吸わないのであれば、子供の頃の喘息が原因でしょうね、という話が医師からあった。</p>
<p>自分は子供の頃から喘息持ちで、中学生の時が一番ひどかったが、大人になってからは落ち着いていて、発作はもう10年以上出ていない。聴診でも音はきれいなので、問題はないだろう、とのこと。</p>
<p>手術中に発作が起きる可能性についての説明もあった。ただこれも、10年発作が出ていないなら、おそらく大丈夫でしょう、と。挿管まわりの説明もあった気がするが、あまり覚えていない。</p>
<hr />
<h2>手術までテリルジーを吸入</h2>
<p>問題はないだろう、とのことだったけど、念のため、テリルジー200という吸入薬を手術まで毎日吸入することになった。ステロイドと2種類の気管支拡張薬が配合された、1日1回タイプの吸入薬。</p>
<p>というわけで、呼吸器まわりは、手術に支障が出るような問題はなさそう。あとは手術まで、毎日テリルジーを吸入しながら備える。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[肘部管症候群の手術に向けた入院準備]]></title>
        <link href="https://mizzy.org/blog/2026/07/30/1/"/>
        <updated>2026-07-30T09:54:41+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/30/1/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/07/29/1/">前回の記事</a>の続き。筋電図検査の結果の説明を受けた日に、手術のリスクの説明や、入院に関する説明・手続きもまとめて行われたので、そのあたりの話。</p>
<hr />
<h2>手術のリスク説明</h2>
<p>手術の説明では、術式の話とあわせて、リスクの説明も受けた。神経損傷、再発、感染など、この手の手術の同意書にあるような一般的なリスクの説明。</p>
<p>肘部管症候群そのものについての説明リーフレットももらった。日本手の外科学会監修の「手の外科シリーズ」というもので、症状、原因、診断、治療が一通りまとまっている。内容は<a href="https://www.joa.or.jp/public/sick/condition/cubital_tunnel_syndrome.html">日本整形外科学会のサイトにある肘部管症候群のページ</a>と同じもの。原因としては、靱帯やガングリオンなどの腫瘤による圧迫、加齢に伴う肘の変形、子供のときの肘の骨折による変形、野球や柔道などのスポーツ、が挙げられている。<a href="/blog/2026/07/29/1/">前回の記事</a>で、3人の医師から骨折したことがあるか聞かれた、という話を書いたけど、子供の頃の肘の骨折による変形が原因のひとつとされているからか、と納得した。</p>
<hr />
<h2>入院に関する説明</h2>
<p>入院案内センターでは、入院日の流れや持ち物の説明を受けた。入院1週間前から毎日検温して記録するための紙も、このときにもらった。もらった資料がかなりの量なので、資料ごとにまとめておく。</p>
<h3>入院のご案内</h3>
<p>入院手続きや費用、病室、面会、入院中の生活、食事のことまで、一通りまとまっている冊子。</p>
<p>入院時に持参するものとしては、パジャマ、タオル・バスタオル、コップまたは湯飲み（割れ物は不可）、下着類、ティッシュペーパー、不織布マスク、洗面用具、筆記用具、現在使用中の薬とお薬手帳、などが挙げられている。履物は運動靴など滑らないもので、スリッパは禁止。</p>
<p>費用まわりでは、支払いにクレジットカードやデビットカードも使える。食事は1食あたり550円の自己負担。食事の時間は朝7時30分、昼12時、夕方18時。通常は昼食と夕食のうち週2回だけ、2種類のメニューから選べる選択食になっているけど、有料個室の場合は毎食選択食を利用できるらしい。</p>
<p>入院中のインターネット環境については、個室のインターネット通信代は無料だけど、パソコンと接続ケーブルは自分で用意する必要がある（ケーブルは売店でも売っている）。一般病室では、携帯電話のほか、パソコンでの無線LANや通信機器の使用も遠慮するように、となっている。</p>
<h3>有料の病室</h3>
<p>個室にはAタイプ、Bタイプ、Cタイプがあり、個室以外に窓側差額ベッド（4人室）というものも。もらった一覧表の内容をまとめると、こんな感じ。</p>
<ul>
<li>Aタイプ（個室）: 1日27,500円。約28㎡。浴槽・シャワー・トイレ・洗面台付きで、ソファーベッド・テーブル・チェアやアメニティグッズまである</li>
<li>Bタイプ（個室）: 1日16,500円。約18㎡。シャワー・トイレ・洗面台付き</li>
<li>Cタイプ（個室）: 1日13,200円。約18㎡。トイレ・洗面台付き</li>
<li>窓側差額ベッド（4人室）: 1日1,650円。トイレ・洗面台は共用</li>
</ul>
<p>テレビと冷蔵庫は、AタイプとBタイプは無料で、Cタイプと窓側差額ベッドはカード式の有料。インターネットは個室なら使えるけど有線のみで、Wi-Fiの提供はない。4人室はインターネット不可で、携帯電話の使用も控えることになっている。</p>
<p>別に大部屋でもいいか、と思ったけど、手術後の感染症リスクを考慮すると個室がおすすめ、と医師に言われたので、Bタイプで予約した。国立病院であまりお金がなさそうなので、売り上げに貢献しよう、という気持ちもある。</p>
<h3>安全な入院生活のための資料</h3>
<p>歩きやすい服装や履き物、ベッドからの転落への注意、夜間のトイレ（21時に消灯される）、点滴中の注意、車椅子・歩行器・杖を使うときの留意点、睡眠薬などを服用している場合の注意、さらには歩き方と体操まで、細かく書かれている。入院中は運動する機会が減って足腰の筋力が低下しがちなうえ、慣れない入院環境から転倒・転落につながることもあるようだ。</p>
<p>履き物は、滑り止めのない家庭用スリッパは滑りやすく危険なので不可。ゴム底で履きやすく脱ぎやすいものを用意する必要があって、ゴム底シューズや転倒防止シューズは院内の売店でも売っている。</p>
<h3>手術に必要な物</h3>
<p>腕・手の手術を受ける人向けの案内。T字帯、ストロー付きのコップか吸い飲み、三角巾、フラットタイプの紙オムツを、それぞれ1つずつ用意する。いずれも売店で購入できて、未使用の物品は購入から1週間以内なら返却できるので、レシートを保管しておくと良いようだ。パジャマの上は、半袖か袖口の広い、前開きのものを用意する（入院セットレンタルを使う場合は不要）。</p>
<p>入院前日には爪切りと髭剃りを済ませて、アクセサリー類は外してくる。入院翌日が手術の場合は入院当日の朝にシャワーを浴びて頭も洗ってくる、とあるけど、B個室にはシャワーが付いているので、その場合は不要だそうだ。</p>
<p>基本的に、レンタルで済むものはレンタルにしようと思っている。三角巾や紙オムツなど、レンタルの対象外のものは、入院当日に売店で買う予定。</p>
<h3>薬について</h3>
<p>案内が2枚あって、1枚は、サプリメントや健康食品、市販薬（医療機関で処方された薬以外はすべて対象）を使っている場合は、出血の危険性を高めたり、麻酔が効きすぎたりする恐れがあるため、手術・検査日の1週間前から使用を中止するように、というもの。</p>
<p>もう1枚は、入院当日のお薬確認について。普段使っている薬の現物を予定入院期間+3日分と、お薬手帳や薬の説明書を入院当日に持参して、薬剤師に種類と数を登録してもらう。飲み薬のシートはバラバラにせず、そのまま持っていく。</p>
<h3>事前歯科検診</h3>
<p>入院前に事前の歯科検診を受ける必要があって、かかりつけ歯科あての紹介状をもらった。また、歯科医療機関と連携していて、入院中に歯科受診を希望する場合は、連携歯科医療機関の訪問診療を受けられる。</p>
<h3>入院セットレンタル</h3>
<p>入院生活に必要な日用品をセットにして、クリーニング付きでレンタルできるサービス。衣類セットは日額550円（税込）で、甚平やニット患者衣などの衣類と、バスタオル・フェイスタオルが毎日1枚ずつ。申し込むと、歯ブラシやシャンプーなどの日用品もサービスでついてくる。肌着や靴下、院内シューズなどのオプションも選べる。支払いは入院費とは別で、利用月の翌月に請求書が郵送されてきて、コンビニで支払う方式。</p>
<h3>面会</h3>
<p>面会できるのは原則15歳以上で、面会時間は病室の種別によって異なる。4人室は13時から17時、BタイプとCタイプの個室は13時から20時、Aタイプの個室は終日面会可能。個室のグレードによって面会時間まで変わる。1回あたりの面会時間や人数に制限はない。発熱している人や、新型コロナやインフルエンザなどに罹患して一定期間経っていない人は面会できない。</p>
<h3>貴重品の保管</h3>
<p>床頭台（ベッド脇のキャビネット）の引き出しの中にセイフティーボックスがあり、現金や財布などの貴重品はその中に入れて、鍵は腕に身につけておく。検査やリハビリ、入浴や面会などで部屋を空けることがあるので、貴重品を持ち込んでいるときは必ず使うように、と書かれている。</p>
<h3>売店のサービス</h3>
<p>平日の午後には、売店の販売員が雑貨やミネラルウォーターをワゴンにのせて各病棟を回る、ワゴンサービスがある。病室備え付けの商品カタログから選んで注文用紙に書き、ナースステーションの注文BOXに入れておくと病室まで届けてくれる、お届けサービスも。毎朝の朝刊配達もやっていて、支払いはいずれも現金。</p>
<h3>医療総合相談室</h3>
<p>医療に関する相談や病院に対する苦情に対応する窓口。相談内容の秘密は保持されるし、相談したことで診療等で不利益を受けることはない、とのこと。</p>
<h3>地域医療情報連携ネットワーク</h3>
<p>地域の病院や診療所などの施設間で患者の診療情報を共有する仕組み。同意書を提出すると専用のIDが発行され、指定した施設間で診療情報が共有される。診療情報を閲覧できるのは患者が指定した施設のみ。閲覧すると利用記録が残り、いつ、誰が、どの情報を見たのか確認できるようになっている。利用するかどうかは自由意思で、同意しなくても診療に不利益はない。</p>
<h3>ポケさぽ</h3>
<p>入院準備や麻酔の説明をLINEで行うサービス。LINEで友だち登録すると、入院準備や持ち物、基本的な麻酔についての資料や動画を確認できるというもの。</p>
<h3>文書料金の改定</h3>
<p>物価高騰や人件費の上昇に伴い、2026年4月1日から診断書や証明書の料金が値上げされた、というお知らせ。普通診断書は3,300円から4,400円に、生命保険・損害保険などの診断書は7,700円から11,000円に、入院証明書は2,200円から3,300円に、といった具合。</p>
<hr />
<h2>手続き系</h2>
<h3>入院申込書</h3>
<p>署名と年月日を書くだけ。</p>
<h3>身元引受書兼診療費等支払保証書</h3>
<p>身元引受人（退院の指示があったときに本人を引き取る人）、支払義務者（診療費等を退院時に支払う人）、連帯保証人（支払ができなかった場合に連帯して支払う人）を記入するもの。支払義務者は患者本人と同一でもよいけど、連帯保証人は支払義務者とは別世帯で独立した生計を営む成年者にお願いする必要がある。保証の極度額は50万円。</p>
<h3>同意書</h3>
<p>3種類あった。肘部管手術に関する同意書、輸血に関する同意書、身体拘束に関する同意書。それぞれの同意書に対応する説明書もあわせて渡されていて、説明書の内容は事前に医師から口頭で説明を受けている。</p>
<h3>転倒・転落に関する入院時意識調査票</h3>
<p>入院という環境の変化により、日頃から歩行に自信がある人でも思いがけず転倒することがあり、特にトイレへの行き帰りに多く発生しているそうで、転倒・転落を予防するために患者の状況を把握するための調査票。めまいやふらつきがあるか、といった質問に、はい・いいえで答えていく形式。</p>
<h3>入院に際してお伺いしたいこと</h3>
<p>記入しておいて、入院時に看護師に渡す書類。緊急連絡先（必ず連絡が取れる人2名）、既往歴、体内に金属があるか、アレルギー、栄養状態、喫煙歴、介護保険の利用状況、といったことを記入する。緊急連絡先には、病院までの所要時間を書く欄もあった。</p>
<h3>もの忘れチェックシート</h3>
<p>患者本人ではなく、家族や患者をよく知る人が記載するもの。同じことを何回も話したり尋ねたりする、といった最近1ヶ月の状態について、普段からみられる行動に○をつける形式。認知症の検査ではなく、入院した患者に安全に生活してもらうための情報とのことで、多くの項目に該当した場合は、治療の説明や検査室への案内などを個別に対応してもらえるらしい。</p>
<h3>限度額適用認定証</h3>
<p>高額療養費の限度額適用認定証は、マイナ保険証を使うので手続き不要。以前は保険者に申請して認定証を取得する必要があったけど、マイナ保険証なら限度額情報が自動で連携されるので、何もしなくてよい。便利になったものだ。</p>
<hr />
<h2>入院までにやることと準備するもの</h2>
<p>ここまでの内容から、入院までにやることと準備するものを、自分用のチェックリストとしてまとめておく。</p>
<h3>やること</h3>
<ul>
<li>かかりつけ歯科で事前検診を受ける</li>
<li>連帯保証人を探して、身元引受書兼診療費等支払保証書に記入してもらう</li>
<li>もの忘れチェックシートを家族に書いてもらう</li>
<li>そのほかの書類の記入（入院申込書、同意書3種、転倒・転落の調査票、入院に際してお伺いしたいこと）</li>
<li>ポケさぽにLINE登録する</li>
<li>入院1週間前から毎日検温して記録する</li>
<li>サプリメントや市販薬を使っている場合は、手術・検査日の1週間前から中止する</li>
<li>入院前日に爪切りと髭剃りを済ませて、アクセサリー類を外す</li>
</ul>
<h3>準備するもの</h3>
<p>まずは、自宅から持っていく必要があるもの。</p>
<ul>
<li>診察券</li>
<li>マイナ保険証</li>
<li>記入済みの書類一式</li>
<li>入院のご案内の冊子と検温記録の紙</li>
<li>滑らない履物（スリッパ禁止）</li>
<li>下着類</li>
<li>不織布マスク</li>
<li>筆記用具</li>
<li>現在使用中の薬（入院期間+3日分）とお薬手帳</li>
<li>現金（売店のサービスやテレビカードなどは現金払い）</li>
</ul>
<p>次に、売店で買えるので、入院当日に病院で買えばいいもの。</p>
<ul>
<li>三角巾</li>
<li>フラットタイプの紙オムツ</li>
<li>T字帯（レンタルを使う場合は不要）</li>
<li>ストロー付きのコップか吸い飲み（レンタルを使う場合は不要）</li>
</ul>
<p>自分はレンタルを使う予定なので不要だけど、レンタルを使わない場合は、さらに以下も必要になる。</p>
<ul>
<li>パジャマ（上は半袖か袖口の広い前開き）</li>
<li>タオル・バスタオル</li>
<li>コップまたは湯飲み（割れ物不可）</li>
<li>ティッシュペーパー</li>
<li>洗面用具</li>
</ul>
<p>あとは、ノートPCと有線LANケーブル。個室のネットは有線のみで、ケーブルは売店でも売っている。</p>
<hr />
<p>というわけで、入院までにやることはまだいろいろある。準備を進めながら入院日を待つ。入院・手術の経過については、また記事にする予定。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[肘部管症候群の手術をすることになった]]></title>
        <link href="https://mizzy.org/blog/2026/07/29/1/"/>
        <updated>2026-07-29T12:40:13+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/29/1/</id>
        <content type="html"><![CDATA[
<p>4月末、右肘を曲げたときに、肘から小指にかけて痺れを感じるようになった。気になったのですぐに近所のクリニックを受診したのだけど、そこから手術が決まるまで、思ったより長くかかった。</p>
<hr />
<h2>クリニックを受診</h2>
<p>最初に行ったのは、近所の内科がメインのクリニック。決まった曜日と時間だけ整形外科の先生がいるところで、レントゲンを撮ってもらった。だけど「曲げたときに皮がつっぱって云々」といった説明と湿布が出ただけで、特にこれといった診断はなし。</p>
<p>自分では肘部管症候群を疑っていたのに、それに類する説明が一切なかったので不信感を持ち、別の整形外科専門のクリニックへ。ここでもレントゲンを撮り、触診もしてもらって、「肘部管症候群でしょう」ということで、タリージェという薬を2週間分処方された。</p>
<hr />
<h2>薬でも改善せず、大病院へ</h2>
<p>タリージェを2週間服用したけど、症状は改善しないどころか、徐々に悪化して、肘を曲げなくても常時小指のあたりに痺れを感じるようになった。さらに前腕に力が入りにくくなって、食事もとりづらくなってきた。小指も使いにくくなっているけど、脱Emacs済みなので仕事には支障がない。そもそも右手の小指なので、脱Emacsしていなくても関係なかったのだけど。同じクリニックで再度診てもらったところ、メコバラミンという薬が追加され、あわせて手術を前提とした専門医のいる大病院を紹介された。大病院の整形外科は専門ごとに科が分かれていて、上肢を専門とする科を受診することに。予約が必須とのことで、事前に電話で予約を入れてから向かった。</p>
<p>大病院での初診では、紹介元のクリニックから渡されたレントゲンの確認と触診をしてもらった。痺れは脳や首が原因のこともあり、肩から上が原因の場合は別の科の対応になるそうで、それを含めて確定させるため、筋電図検査を受けることになった。ただ予約が一杯で、検査は1ヶ月以上先に。薬は引き続き元のクリニックで処方してもらうよう指示された。</p>
<hr />
<h2>筋電図検査で確定診断</h2>
<p>筋電図検査は、皮膚の上から神経に電気刺激を与えて、その神経を伝わる信号の速さ（伝導速度）や筋肉の反応を測定する検査。神経が圧迫されて傷んでいると、信号の伝わる速度が遅くなるので、それが数値でわかる。</p>
<p>検査当日はその場で結果を聞けるわけではなく、後日あらためて医師から説明を受けることに。結果は、肘の部分で神経の伝導障害がはっきり確認された、というもの。これで手術を勧められたのだけど、あわせて「手首にも問題があるかもしれない」ということで、MRIも受けることに。</p>
<p>MRIの結果、手首側は特に問題なし。というわけで、肘部管症候群の手術で決まりということになった。</p>
<p>このタイミングで薬を継続すべきか確認したところ、効いていないなら飲まなくてもよいとのことだったので、服用はやめることに。</p>
<hr />
<h2>入院して手術へ</h2>
<p>検査結果の説明を受けたあと、入院前検査として採血、採尿、肺機能、レントゲン、心電図を受けた。肺機能検査で気になる数値があり、手術に支障はないレベルとのことだったけど、念のためアレルギー科でも診てもらうことになっている。</p>
<p>このあと入院して手術を受けることになった。特に問題がなければ3泊4日の想定。</p>
<p>症状に気づいてすぐに最初のクリニックを受診してから手術が決まるまで3ヶ月、クリニックを2軒と大病院を回って、レントゲン、投薬、筋電図、MRIと一通りの検査を経て、ようやくここまで来た。手術はさらに1ヶ月後になる予定。</p>
<p>余談だけど、整形外科クリニックの医師2人と大病院の医師1人、計3人から、レントゲンを見て「骨折したことありますか」と聞かれた。3人とも同じことを聞いてくるくらいなので、レントゲンに何かそれらしい痕跡があるのだと思うけど、自分にはまったく身に覚えがない。</p>

]]></content>
    </entry>

</feed>
