<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title><![CDATA[Gosuke Miyashita]]></title>
    <link href="https://mizzy.org/atom.xml" rel="self"/>
    <link href="https://mizzy.org/"/>
    <updated>2026-03-28T14:52:19+09:00</updated>
    <id>https://mizzy.org/</id>
    <author>
        <name><![CDATA[Gosuke Miyashita]]></name>
    </author>
    <generator uri="https://github.com/mizzy/nebel/">Nebel</generator>

    <entry>
        <title type="html"><![CDATA[Carinaのシークレット管理]]></title>
        <link href="https://mizzy.org/blog/2026/03/28/1/"/>
        <updated>2026-03-28T14:52:19+09:00</updated>
        <id>https://mizzy.org/blog/2026/03/28/1/</id>
        <content type="html"><![CDATA[
<p><a href="https://github.com/carina-rs/carina">Carina</a>にシークレット管理の仕組みを設計・実装しているので、Terraformとの設計の違いについて書いておく。</p>
<hr />
<h2>Terraformのシークレット問題</h2>
<p>Terraformにおけるシークレット管理は長年の課題だった。<a href="https://github.com/hashicorp/terraform/issues/516">GitHub issue #516</a>は2014年10月に作成され、250以上のコメントがついている。</p>
<p>問題の根本は、Terraformのstateにシークレットが平文で保存されることにある。<code>sensitive</code>フラグをつけても、plan出力がマスクされるだけで、stateには平文のまま残る。<code>aws_kms_secrets</code>データソースを使っても、復号結果はstateに平文で保存される。</p>
<p>この問題に対してTerraformは段階的に対処してきた。</p>
<ul>
<li><strong>sensitive</strong>（Terraform 0.14、2020年）: plan出力のマスク。stateは平文のまま</li>
<li><strong>ephemeral</strong>（Terraform 1.10、2024年11月）: stateに一切保存しない値。<code>ephemeral</code>リソースやephemeral変数として使う</li>
<li><strong>write-only</strong>（Terraform 1.11、2025年3月）: リソース属性の値をstateに保存しない仕組み</li>
</ul>
<p>問題提起から約10年かけて、ようやく「stateに保存しない」という本質的な解決策にたどり着いた形になる。</p>
<hr />
<h2>Terraformのwrite-only</h2>
<p>write-onlyはTerraform 1.11で導入された仕組みで、リソースの属性値をstateに一切保存しないようにする。例えばSSM Parameterにシークレットを保存する場合、従来は値がstateに平文で残っていたが、write-onlyを使うとこうなる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">data</span> <span class="s2">&#34;aws_kms_secrets&#34; &#34;example&#34;</span> {
</span></span><span class="line"><span class="cl">  <span class="k">secret</span> {
</span></span><span class="line"><span class="cl"><span class="n">    name</span>    <span class="o">=</span> <span class="s2">&#34;db_password&#34;</span>
</span></span><span class="line"><span class="cl"><span class="n">    payload</span> <span class="o">=</span> <span class="s2">&#34;AQICAHh...&#34;</span>
</span></span><span class="line"><span class="cl">  }
</span></span><span class="line"><span class="cl">}
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">resource</span> <span class="s2">&#34;aws_ssm_parameter&#34; &#34;db_password&#34;</span> {
</span></span><span class="line"><span class="cl"><span class="n">  name</span>             <span class="o">=</span> <span class="s2">&#34;/myapp/db_password&#34;</span>
</span></span><span class="line"><span class="cl"><span class="n">  type</span>             <span class="o">=</span> <span class="s2">&#34;SecureString&#34;</span>
</span></span><span class="line"><span class="cl"><span class="n">  value_wo</span>         <span class="o">=</span> <span class="k">data</span><span class="p">.</span><span class="k">aws_kms_secrets</span><span class="p">.</span><span class="k">example</span><span class="p">.</span><span class="k">plaintext</span><span class="p">[</span><span class="s2">&#34;db_password&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="n">  value_wo_version</span> <span class="o">=</span> <span class="m">1</span>
</span></span><span class="line"><span class="cl">}
</span></span></code></pre><p>stateに値を保存しないので安全だが、代わりに変更検知ができなくなるという問題がある。そこで<code>value_wo_version</code>という数値を手動で管理する。値を変更したらこれを1から2にインクリメントする。Terraformはこのバージョン番号の変更を検知して、プロバイダーに新しい値を送る。</p>
<p>実際にversionを変更してplanすると、こうなる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="c1">  # aws_ssm_parameter.db_password will be updated in-place
</span></span></span><span class="line"><span class="cl">  <span class="err">~</span> <span class="k">resource</span> <span class="s2">&#34;aws_ssm_parameter&#34; &#34;db_password&#34;</span> {
</span></span><span class="line"><span class="cl"><span class="n">      ~ has_value_wo</span>     <span class="o">=</span> <span class="kt">true</span> <span class="err">-&gt;</span> <span class="p">(</span><span class="k">known</span> <span class="k">after</span> <span class="k">apply</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">        id</span>               <span class="o">=</span> <span class="s2">&#34;/tf-write-only-test/db_password&#34;</span>
</span></span><span class="line"><span class="cl"><span class="n">        name</span>             <span class="o">=</span> <span class="s2">&#34;/tf-write-only-test/db_password&#34;</span>
</span></span><span class="line"><span class="cl"><span class="n">      ~ value_wo_version</span> <span class="o">=</span> <span class="m">1</span> <span class="err">-&gt;</span> <span class="m">2</span><span class="c1">
</span></span></span><span class="line"><span class="cl"><span class="c1">        # (12 unchanged attributes hidden)
</span></span></span><span class="line"><span class="cl">    }
</span></span></code></pre><p><code>value_wo_version</code>の変更は表示されるが、<code>value_wo</code>自体の値や変更内容は一切表示されない。</p>
<p>この設計にはいくつかの問題がある。</p>
<ul>
<li>バージョンのインクリメントを忘れると、実際の値が変わっていてもTerraformは変更を検知しない</li>
<li>リソースタイプごとに個別にwrite-only対応が必要で、対応はまだ一部のリソースに限られる</li>
<li><code>_wo</code>サフィックスの命名規則により、既存の属性名とは別の属性が必要になる</li>
</ul>
<p>なお、<a href="https://developer.hashicorp.com/terraform/plugin/framework/resources/write-only-arguments">プラグインフレームワークのドキュメント</a>では、private stateにハッシュを保存して変更を検知するアプローチもベストプラクティスとして紹介されている。ただし、少なくともAWS providerでは現時点ではこの方式は採用されていないようだ。</p>
<hr />
<h2>Carinaのsecret() + decrypt()</h2>
<p>Carinaでは同じことを<code>secret()</code>（<a href="https://github.com/carina-rs/carina/issues/1238">#1238</a>）と<code>decrypt()</code>（<a href="https://github.com/carina-rs/carina/issues/1240">#1240</a>）の組み合わせで実現する。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="nc">awscc.ssm.parameter</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">name</span>  <span class="o">=</span> <span class="s2">&#34;/myapp/db_password&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">type</span>  <span class="o">=</span> <span class="s2">&#34;SecureString&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">value</span> <span class="o">=</span> <span class="nv">secret</span><span class="p">(</span><span class="nv">decrypt</span><span class="p">(</span><span class="s2">&#34;AQICAHh...&#34;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><p><code>decrypt()</code>はapply時にプロバイダーの暗号化サービス（AWS KMS等）を使って復号する。<code>secret()</code>で包むことで、復号後の値はstateにハッシュとしてのみ保存される。</p>
<p><code>secret()</code>の動作は以下の通り。</p>
<ul>
<li><strong>apply時</strong>: <code>secret()</code>をアンラップして実際の値をプロバイダーに送る</li>
<li><strong>state</strong>: 値そのものではなくArgon2idハッシュのみを保存する。ソルトはリソースタイプ・リソース名・属性キーから生成するので、同じ値でもリソースごとに異なるハッシュになる</li>
<li><strong>plan出力</strong>: 値の代わりに<code>(secret)</code>と表示する。変更時は<code>(secret) → (secret)</code>と表示され、値は見えないが変更があったことはわかる</li>
<li><strong>diff</strong>: ハッシュの比較で変更を自動検知する</li>
</ul>
<p>Terraformのwrite-onlyとの違いをまとめると以下の通り。</p>
<ul>
<li>ハッシュをstateに保存することで変更検知を自動化。バージョン番号を手動で管理する必要がない</li>
<li>ハッシュから元の値を復元することはできないので、stateからシークレットが漏洩するリスクもない</li>
<li><code>secret()</code>は組み込み関数なので、プロバイダー側の個別対応は不要。どのリソースのどの属性にも使える</li>
<li>Terraformでは復号用のデータソースを別途定義する必要があるが、Carinaでは属性値に直接書ける</li>
<li><code>decrypt()</code>はプロバイダー非依存の設計で、AWS KMS、GCP Cloud KMS、Azure Key Vaultなどプロバイダーの暗号化サービスに自動的に委譲する予定</li>
</ul>
<hr />
<h2>env()との組み合わせ</h2>
<p>暗号文を<code>.crn</code>ファイルに書きたくない場合は、<code>env()</code>関数（<a href="https://github.com/carina-rs/carina/issues/1239">#1239</a>、実装済み）で環境変数から渡すこともできる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="nc">awscc.rds.db_instance</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">master_password</span> <span class="o">=</span> <span class="nv">secret</span><span class="p">(</span><span class="nv">env</span><span class="p">(</span><span class="s2">&#34;DB_PASSWORD&#34;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><hr />
<h2>設計の違いまとめ</h2>
<p>Carinaのシークレット管理は、<code>secret()</code>と<code>decrypt()</code>の2つの関数で実現する。</p>
<ul>
<li><code>secret(value)</code> — 値をシークレットとしてマーク。stateにはハッシュのみ保存</li>
<li><code>decrypt(ciphertext)</code> — プロバイダーの暗号化サービスで復号</li>
</ul>
<p>これらに<code>env()</code>のような汎用関数を組み合わせることもできる。それぞれが単一の責務を持ち、自由に組み合わせられる。Terraformでは<code>sensitive</code>、<code>ephemeral</code>、write-only、<code>aws_kms_secrets</code>データソースと、歴史的経緯から複数の仕組みが並立している。Carinaは後発の利点を活かして、最初からシンプルな設計にできた、と思う。</p>
<p>ただし、これはあくまで現段階の設計であり、より安全で良い方法が見つかれば変更する可能性はある。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Carinaのplan表示を改善した]]></title>
        <link href="https://mizzy.org/blog/2026/03/22/1/"/>
        <updated>2026-03-22T19:21:17+09:00</updated>
        <id>https://mizzy.org/blog/2026/03/22/1/</id>
        <content type="html"><![CDATA[
<p>Carinaの<code>carina plan</code>コマンドの表示を最近まとめて改善したので、その内容を書いておく。</p>
<hr />
<h2>スナップショットテストの導入</h2>
<p>plan表示の改善を始める前に、まずテストの仕組みを整えた。plan表示は目視で確認するのが面倒だし、変更のたびに既存の表示が壊れていないか確認するのも大変なので、<a href="https://insta.rs/">insta</a>を使ったスナップショットテストを導入した。</p>
<p>仕組みはシンプルで、テスト用の<code>.crn</code>ファイル（とオプションでstateファイル）をfixtureとして用意し、それに対して<code>format_plan()</code>を実行した結果をスナップショットとして保存する。テスト実行時にスナップショットと一致しなければ失敗する。表示を変更した場合は<code>cargo insta review</code>で差分を確認してスナップショットを更新すれば良い。</p>
<p>plan表示はANSIカラーコードを含むので、スナップショットの可読性のためにカラーコードをstripしてからスナップショットに保存している。</p>
<p>これにより、表示の改善を安心してガンガン進められるようになった。実際のスナップショットは<a href="https://github.com/carina-rs/carina/tree/main/carina-cli/src/snapshots">こちら</a>にあるので、どんな表示になるのかはここを見るとわかる。</p>
<hr />
<h2>依存関係のツリー表示</h2>
<p>Terraformの<code>terraform plan</code>はリソースをフラットなリストで表示する。リソース間に依存関係があっても、出力上はすべて同じ階層にフラットに並ぶ。さらにリソース名のアルファベット順でソートされるので、依存関係のあるリソース同士が離れた位置に表示されることもある。結果として、どのリソースがどのリソースに依存しているかは人間が属性値を見て判断する必要がある。</p>
<p>Carinaではリソース間の依存関係をツリー構造で表示するようにした。</p>
<p><img src="/images/2026/03/carina-plan-tree.png" alt="carina plan のツリー表示" /></p>
<p>VPCの下にroute_tableとsubnetがぶら下がっているのが視覚的にわかる。Create、Update、Destroyが混在する場合でも、依存関係の中で何が起きるかがひと目でわかる。</p>
<p><img src="/images/2026/03/carina-plan-mixed.png" alt="Create、Update、Destroyが混在するplan表示" /></p>
<hr />
<h2>read-only属性の表示</h2>
<p>リソースを作成する際、ARNやIDのようなread-only属性はAWS側が自動的に割り当てる。以前のplan表示ではこれらは完全に省略されていたが、Terraformと同様に<code>(known after apply)</code>として表示するようにした。</p>
<p><img src="/images/2026/03/carina-plan-read-only.png" alt="read-only属性の表示" /></p>
<p>これにより、リソース作成後にどんな属性が生えてくるのかがplanの時点でわかるようになった。</p>
<hr />
<h2>デフォルト値の表示</h2>
<p>スキーマにデフォルト値が定義されている属性も、ユーザが明示的に指定していなければplanに表示されていなかった。これを<code># default</code>アノテーション付きで表示するようにした。</p>
<p><img src="/images/2026/03/carina-plan-default-values.png" alt="デフォルト値の表示" /></p>
<p>ユーザが指定した属性、デフォルト値が適用される属性、apply後に確定するread-only属性、という3層が一目でわかる。</p>
<hr />
<h2>未変更属性のサマリ表示</h2>
<p>Update/Replaceの際、変更されない属性がいくつあるかを<code># (3 unchanged attributes hidden)</code>のようなサマリ行で表示するようにした。</p>
<p><img src="/images/2026/03/carina-plan-map-diff.png" alt="未変更属性のサマリ表示" /></p>
<hr />
<h2><code>--detail</code>フラグによる表示レベルの制御</h2>
<p><code>--detail</code>フラグで表示レベルを制御できるようにした。3つのモードがある。</p>
<ul>
<li><strong>full</strong>（デフォルト）: すべての属性を表示。未変更・デフォルト・read-onlyも含む</li>
<li><strong>explicit</strong>: ユーザが<code>.crn</code>ファイルで明示的に指定した属性のみ表示</li>
<li><strong>none</strong>: リソース名と依存ツリーのみ表示</li>
</ul>
<p>デフォルトの<code>--detail full</code>だとこう。</p>
<p><img src="/images/2026/03/carina-plan-detail-full.png" alt="--detail full の表示" /></p>
<p><code>--detail explicit</code>だとこうなる。</p>
<p><img src="/images/2026/03/carina-plan-detail-explicit.png" alt="--detail explicit の表示" /></p>
<p><code>--detail none</code>だとリソース名と依存ツリーのみになる。リソース数が多いときに全体の構造だけさくっと確認したい場合に便利。</p>
<p><img src="/images/2026/03/carina-plan-detail-none.png" alt="--detail none の表示" /></p>
<hr />
<h2>TUI（Terminal UI）の追加</h2>
<p>CLIの表示とは別に、<code>--tui</code>フラグでTUI（Terminal UI）も使えるようにした。ratatuiとcrosstermで実装している。</p>
<p><video src="/images/2026/03/carina-tui.mp4" controls muted playsinline style="max-width:100%"></video></p>
<p>主な機能は以下の通り。</p>
<ul>
<li><strong>ツリービュー + 詳細パネル</strong>: 画面の70%がリソースの依存関係ツリー、30%が選択したリソースの属性詳細。Tabキーでパネル間を切り替える</li>
<li><strong>検索・フィルタ</strong>: <code>/</code>キーで検索モードに入り、リソース名やタイプでフィルタ。Tabキーで補完、<code>n</code>/<code>N</code>でマッチ間を移動</li>
<li><strong>リソース参照ナビゲーション</strong>: 詳細パネルで<code>vpc_id: vpc.vpc_id</code>のようなリソース参照をEnterキーで辿れる。Backspaceで戻る</li>
<li><strong>色分け</strong>: Create（緑）、Update（黄）、Replace（マゼンタ）、Delete（赤）で操作の種類が一目でわかる</li>
</ul>
<hr />
<p>これらの改善は、Terraformのplan表示を参考にしつつ、ツリー表示やTUIのようなCarina独自の機能も加えている。applyする前にリソースの全体像を把握しやすくなった。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Carinaの開発状況（2月下旬〜3月中旬）]]></title>
        <link href="https://mizzy.org/blog/2026/03/20/1/"/>
        <updated>2026-03-20T17:56:56+09:00</updated>
        <id>https://mizzy.org/blog/2026/03/20/1/</id>
        <content type="html"><![CDATA[
<p><a href="https://mizzy.org/blog/2026/02/21/1/">前回の記事</a>ではClaude Codeによるイシュー駆動開発について書いた。あの時点では90個のイシューが登録され67個がクローズされた、というところだったが、あれから約1ヶ月、さらに約280個のイシューがクローズされ、約320個のPRがマージされた。</p>
<p>この1ヶ月でやったことを振り返っておく。</p>
<hr />
<h2>AWS providerの本格整備</h2>
<p>aws providerは以前からあったが、スキーマの手書き部分が多く、テストも不十分だった。この1ヶ月で本格的に整備した。</p>
<p>まず、<a href="https://github.com/aws/api-models-aws">Smithyモデル</a>からスキーマを自動生成する仕組みを入れた（<a href="https://github.com/carina-rs/carina/issues/461">#461</a>）。awscc providerのCloudFormationスキーマからの自動生成と同じ考え方で、AWS SDKの元になっているSmithyモデルから、リソーススキーマとread関数を自動生成する。手書きのスキーマは信用できないので、できるだけ自動生成に寄せていきたい。</p>
<hr />
<h2>Acceptance testの拡充</h2>
<p>AWS providerとAWSCC providerの両方で、acceptance testを大幅に増やした。前回の記事時点ではAWSCC providerの39ファイルだけだったが、AWS providerのテスト追加とシナリオの拡充で172ファイルになった（AWSCC: 120、AWS: 52）。基本的なCRUDに加えて、タグの削除、in-place update、create-before-destroy、属性の削除といったシナリオを整備している。</p>
<p>特に大きかったのは、in-place updateのテストの追加。リソースを作成した後、属性を変更してもう一度applyし、replaceではなくin-place updateが正しく行われることを確認するテスト。これを入れたことで、AWSCC providerのupdate処理にあったバグが大量に見つかった。</p>
<ul>
<li>Cloud Control APIのupdate用patch documentにread-onlyプロパティが含まれていた（<a href="https://github.com/carina-rs/carina/issues/807">#807</a>）</li>
<li>create-onlyプロパティもpatchに含まれていた（<a href="https://github.com/carina-rs/carina/issues/810">#810</a>）</li>
<li>JSON Patchのreplaceではなくaddオペレーションを使うべきだった（<a href="https://github.com/carina-rs/carina/issues/795">#795</a>）</li>
<li>変更のない属性もpatchに含まれていた（<a href="https://github.com/carina-rs/carina/issues/737">#737</a>）</li>
<li>属性の削除がpatchに反映されていなかった（<a href="https://github.com/carina-rs/carina/issues/736">#736</a>）</li>
</ul>
<p>multi-stepテスト（create → update → destroy）のインフラも整備した。テストスクリプトのシグナルハンドリング（<a href="https://github.com/carina-rs/carina/issues/793">#793</a>）、destroy失敗時の検出（<a href="https://github.com/carina-rs/carina/issues/855">#855</a>）、orphanedリソースの防止（<a href="https://github.com/carina-rs/carina/issues/812">#812</a>）など、テストの信頼性を上げるための改善も多い。</p>
<hr />
<h2>Anonymous resourceの識別と置き換え</h2>
<p>Carinaでは、Terraformと異なり、リソースに名前をつけなくてもよい。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="c"># Terraformだと aws_vpc.main のように名前が必要だが、Carinaでは不要</span>
</span></span><span class="line"><span class="cl"><span class="nc">awscc.ec2_vpc</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">cidr_block</span> <span class="o">=</span> <span class="s2">&#34;10.0.0.0/16&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><p>ただし、内部的には<code>.crn</code>ファイルに書かれたリソースとstateに保存されたリソースを突き合わせるためのidentifierが必要になる。Terraformは人間がつけた名前（<code>aws_vpc.main</code>の<code>main</code>の部分）をidentifierとして使うが、Carinaのanonymous resourceにはそれがない。</p>
<p>そこで、スキーマのcreate-onlyプロパティ（作成後に変更できない属性）の値からハッシュを計算してidentifierとしている。ただ、create-onlyプロパティが変更された場合、ハッシュが変わってidentifierも変わる。するとdifferは、<code>.crn</code>ファイルにある「新しいidentifierのリソース」とstateにある「古いidentifierのリソース」を同じリソースだと認識できず、「新しいリソースの作成」と「古いリソースの削除」という無関係な2つの操作として扱ってしまう。本来は「同じリソースの置き換え（Replace）」として認識させる必要がある。</p>
<p>そこで、<code>reconcile_anonymous_identifiers()</code>という仕組みを入れた（<a href="https://github.com/carina-rs/carina/issues/540">#540</a>）。create-onlyプロパティの一部が一致し一部が異なる場合、同じリソースの変更であると判定し、stateにある旧identifierを引き継ぐことでReplaceとして扱う。</p>
<p>さらに、create-onlyプロパティを持たないリソース（EC2 EIPなど）に対しては、全属性の<a href="https://en.wikipedia.org/wiki/SimHash">SimHash</a>（locality-sensitive hash）をidentifierとして使うようにした（<a href="https://github.com/carina-rs/carina/issues/592">#592</a>）。通常のハッシュは1ビットでも入力が変わると出力が大きく変わるが、SimHashは入力の類似度を保存する。属性を1つ変更しても数ビットしか変わらないので、Hamming距離で同一リソースかどうかを判定できる。ただし、tagsのようなMap属性をそのまま1つのfeature（ハッシュの入力単位）としてハッシュすると、tag1つの変更でも相対的な変化が大きくなりすぎて閾値を超えてしまう問題があった。Map/List値を個々のエントリに展開（flatten）してそれぞれ別のfeatureとすることで修正した（<a href="https://github.com/carina-rs/carina/issues/885">#885</a>）。</p>
<p>Terraformにはanonymous resourceの概念がないので、Carina独自の設計判断が多く、バグも出やすい領域だった。</p>
<hr />
<h2>Create-before-destroyの名前衝突回避とカスケード更新</h2>
<p>リソースのReplace（置き換え）を実行するcreate-before-destroyでも、Terraformにはない仕組みを入れた。</p>
<p>Terraformではcreate-before-destroyの際、名前の衝突は人間が<code>name_prefix</code>等で回避する必要がある。Carinaでは、リソースの<code>name_attribute</code>（S3なら<code>bucket_name</code>、IAMなら<code>role_name</code>）をスキーマから自動判定し、replaceの際に一時名を自動生成する（<a href="https://github.com/carina-rs/carina/issues/405">#405</a>）。名前がupdatable（create-onlyでない）なら、旧リソース削除後に本来の名前にリネームする。create-onlyな場合はリネームできないので一時名のまま残る。</p>
<p>依存リソースのカスケードアップデートも入れた（<a href="https://github.com/carina-rs/carina/issues/404">#404</a>）。create-before-destroyでは「新リソース作成→依存リソース更新→旧リソース削除」という順序で実行する必要があるが、この中間ステップの依存リソース更新をdifferが依存グラフから検出してplanに含める。</p>
<hr />
<h2>LSPの強化</h2>
<p>エディタ上での開発体験を良くするために、LSP（Language Server Protocol）まわりを大幅に改善した。</p>
<ul>
<li>プロバイダーブロックの補完（<a href="https://github.com/carina-rs/carina/issues/899">#899</a>）</li>
<li>リソース参照の型ベース補完（<a href="https://github.com/carina-rs/carina/issues/914">#914</a>） — <code>route_table_id</code>に対してRouteTableを返すリソースだけを候補に出す</li>
<li>read-only属性をリソースブロック内の補完候補から除外（<a href="https://github.com/carina-rs/carina/issues/921">#921</a>）</li>
<li>属性ホバーで間違った説明を表示していた問題の修正（<a href="https://github.com/carina-rs/carina/issues/924">#924</a>）</li>
<li>未定義参照の検出強化（<a href="https://github.com/carina-rs/carina/issues/871">#871</a>）</li>
<li>重複letバインディングの検出（<a href="https://github.com/carina-rs/carina/issues/916">#916</a>）</li>
<li>未使用letバインディングの警告（<a href="https://github.com/carina-rs/carina/issues/530">#530</a>）</li>
<li>ネストされたstructブロックの任意深さでの補完・診断（<a href="https://github.com/carina-rs/carina/issues/400">#400</a>、<a href="https://github.com/carina-rs/carina/issues/402">#402</a>）</li>
<li>非ASCII文字によるbyte/charインデックスのずれの修正（<a href="https://github.com/carina-rs/carina/issues/666">#666</a>）</li>
</ul>
<hr />
<h2>コード生成の強化</h2>
<p>CloudFormationスキーマとSmithyモデルからのコード生成器（codegen）に、バリデーション制約のサポートを追加した。文字列長（minLength/maxLength）、配列長（minItems/maxItems）、正規表現パターン、フォーマット制約（int64、URIなど）、デフォルト値、複数制約の合成（pattern + lengthなど）をサポートしている（<a href="https://github.com/carina-rs/carina/issues/628">#628</a>〜<a href="https://github.com/carina-rs/carina/issues/636">#636</a>）。</p>
<p>これにより、<code>carina validate</code>で実際にAWSにリクエストを投げる前に、かなり細かいバリデーションができるようになった。</p>
<hr />
<h2>State管理の改善</h2>
<p>本番運用を見据えたstate管理の改善も進めた。</p>
<ul>
<li>state lockの改善 — TTLを60分に延長し、lock renewalとconditional writeを追加（<a href="https://github.com/carina-rs/carina/issues/857">#857</a>）</li>
<li>アトミックなstate書き込み — ローカルバックエンドのstate書き込みをatomicに（<a href="https://github.com/carina-rs/carina/issues/849">#849</a>）</li>
<li><code>--lock=false</code>オプション — state lockのスキップ（<a href="https://github.com/carina-rs/carina/issues/893">#893</a>）</li>
<li>orphanedリソースの検出 — <code>.crn</code>ファイルから消えたリソースの自動検出・削除（<a href="https://github.com/carina-rs/carina/issues/853">#853</a>）</li>
<li>state refreshコマンド — AWSの実際の状態とstateの同期（<a href="https://github.com/carina-rs/carina/issues/395">#395</a>）</li>
<li>エラーパスでのlock解放保証（<a href="https://github.com/carina-rs/carina/issues/779">#779</a>）</li>
<li>プロバイダースコープのstate identity — マルチプロバイダー構成でのstate破損防止（<a href="https://github.com/carina-rs/carina/issues/854">#854</a>）</li>
</ul>
<hr />
<h2>その他の新機能</h2>
<ul>
<li><strong>フォーマッター</strong> — list-of-mapsをblock構文に変換（<a href="https://github.com/carina-rs/carina/issues/911">#911</a>）</li>
<li><strong>block構文</strong> — <code>List&lt;Struct&gt;</code>属性に対するブロック構文のサポートと、codegenでの自動block_name生成（<a href="https://github.com/carina-rs/carina/issues/381">#381</a>、<a href="https://github.com/carina-rs/carina/issues/759">#759</a>）</li>
<li><strong>前方参照</strong> — 宣言順序に依存しないパーサー（<a href="https://github.com/carina-rs/carina/issues/876">#876</a>）</li>
<li><strong>循環依存検出</strong> — リソース間の循環依存をエラーとして報告（<a href="https://github.com/carina-rs/carina/issues/665">#665</a>）</li>
<li><strong>型認識diff</strong> — differが型情報を持って意味的な比較を行うように（<a href="https://github.com/carina-rs/carina/issues/613">#613</a>）</li>
<li><strong>ordered/unordered list</strong> — AWSCCスキーマのリストに順序付き/順序なしのセマンティクスを追加（<a href="https://github.com/carina-rs/carina/issues/873">#873</a>）</li>
</ul>
<hr />
<h2>リファクタリング</h2>
<p>コードベースが大きくなってきたので、構造的な整理も進めた。main.rs（4000行）、awscc provider.rs（3500行）、differ.rs、diagnostics.rs、completion.rsなどの大きなファイルを分割し、carina-coreからvalidation、resolver、deps、config_loaderなどのモジュールを抽出した（<a href="https://github.com/carina-rs/carina/issues/382">#382</a>〜<a href="https://github.com/carina-rs/carina/issues/392">#392</a>、<a href="https://github.com/carina-rs/carina/issues/673">#673</a>、<a href="https://github.com/carina-rs/carina/issues/787">#787</a>）。</p>
<p>また、CLIやLSPのコードに<code>&quot;aws&quot;</code>や<code>&quot;awscc&quot;</code>といったプロバイダー名が直接書かれていたのを、<code>ProviderFactory</code>トレイト経由で動的に解決するように変更した（<a href="https://github.com/carina-rs/carina/issues/364">#364</a>、<a href="https://github.com/carina-rs/carina/issues/367">#367</a>）。新しいプロバイダーを追加してもCLIやLSPのコードを変更する必要がなくなった。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Claude CodeによるCarinaのイシュー駆動開発]]></title>
        <link href="https://mizzy.org/blog/2026/02/21/1/"/>
        <updated>2026-02-21T17:11:50+09:00</updated>
        <id>https://mizzy.org/blog/2026/02/21/1/</id>
        <content type="html"><![CDATA[
<p><a href="https://github.com/carina-rs/carina">Carina</a>の開発はこれまでコード自体をほとんど見ずにやらせていたので、コード全体の品質や一貫性がどうなっているかは把握していなかった。</p>
<p>そこで、Claude Codeに問題点をGitHubイシューとして登録させ、そのイシューをひとつずつ潰させる、というサイクルを回してみた。イシューの対応はすべて自分でPRをレビューしてからマージしていて、これは自分自身のコードや設計の理解を深める意味もある。10日間回した結果、90個のイシューが登録され、67個がクローズされた。</p>
<hr />
<h2>作業フロー</h2>
<p>具体的な作業フローはこんな感じ。</p>
<ol>
<li><code>/pick-issue</code>スキルを呼ぶと、一番新しいオープンイシューをpickしてくる</li>
<li>Claude Codeがイシューを修正してPRを出す</li>
<li><code>/self-review</code>スキルで5回セルフレビューさせる</li>
<li>セルフレビューの過程でPRに関連する問題が見つかればその場で修正、無関係な問題であれば新たにイシューを登録</li>
<li>自分がPRをレビューしてマージ</li>
<li>1に戻る</li>
</ol>
<p>イシューの優先順位付けは特にやっていない。<code>/pick-issue</code>が一番新しいイシューを拾ってくるので、基本的にはFILO（後入れ先出し）で回している。気になるイシューがあれば<code>/pick-issue 123</code>のように番号指定することもあるが、大半はそのまま最新のものを拾わせている。修正の過程で見つかった新しいイシューが次にpickされやすいので、関連する問題が芋づる式に片付いていく、という副次的な効果もある。</p>
<p>イシューの登録元はセルフレビューだけではなく、コードベース全体のレビューや、acceptance testの実行結果から問題を拾ってイシューに登録させる、ということもやっている。</p>
<p>このフローだと、完全にボトルネックは人間によるレビュー。レビュー対象はPRのコードだけでなく、Claude Codeが立てたplanも含まれる。plan段階でプロジェクトの方針とまったく逆方向の提案をしてくることもあるので、方針がおかしければそこで差し戻す。</p>
<hr />
<h2>Day 1: 23個のイシュー</h2>
<p>まずClaude Codeにコードベース全体のレビューとacceptance testの実行をさせたところ、1日で23個のイシューが登録された。バグ、設計上の問題、不足している機能など、色々なカテゴリのイシューが一気に出てきた。たとえば:</p>
<ul>
<li><code>Effect::Delete</code>にリソース識別子が含まれていない（<a href="https://github.com/carina-rs/carina/issues/135">#135</a>）</li>
<li>IGWのデタッチエラーが握りつぶされている（<a href="https://github.com/carina-rs/carina/issues/136">#136</a>）</li>
<li><code>create_plan()</code>が削除すべきリソースを検出しない（<a href="https://github.com/carina-rs/carina/issues/137">#137</a>）</li>
<li>Structバリデーションがunknown fieldを無視している（<a href="https://github.com/carina-rs/carina/issues/138">#138</a>）</li>
<li>plan fileの保存とapplyが欲しい（<a href="https://github.com/carina-rs/carina/issues/142">#142</a>）</li>
<li><code>name</code>属性をリソース識別子として使うのをやめるべき（<a href="https://github.com/carina-rs/carina/issues/148">#148</a>）</li>
<li>S3バケットの中身が空でないと削除できない（<a href="https://github.com/carina-rs/carina/issues/160">#160</a>）</li>
</ul>
<hr />
<h2>Day 2-3: 修正がさらなる問題を呼ぶ</h2>
<p>初日のイシューを潰し始めると、修正の過程で新たな問題が次々と見つかった。</p>
<p>特に大きかったのが、<code>--detailed-exitcode</code>（<a href="https://github.com/carina-rs/carina/issues/130">#130</a>）の実装と、acceptance testへのapply後plan verify追加（<a href="https://github.com/carina-rs/carina/issues/129">#129</a>）。apply後にもう一度planを実行して差分がないことを確認する仕組みを入れたところ、べき等性の問題が大量に見つかった。</p>
<ul>
<li>enum値の大文字小文字が正規化されない（<a href="https://github.com/carina-rs/carina/issues/174">#174</a>、<a href="https://github.com/carina-rs/carina/issues/176">#176</a>）</li>
<li>AWSから返ってきた値にスキーマにない余分なフィールドが含まれている（<a href="https://github.com/carina-rs/carina/issues/172">#172</a>）</li>
<li><code>List&lt;Struct&gt;</code>の比較が順序依存で、false diffが出る（<a href="https://github.com/carina-rs/carina/issues/171">#171</a>）</li>
</ul>
<p>acceptance testを実際に10アカウント並列で回すと、テストランナー自体のバグも出てきた。ワーカーサブシェルがエラーで黙って停止する問題（<a href="https://github.com/carina-rs/carina/issues/170">#170</a>）や、apply失敗時のリソースクリーンアップ（<a href="https://github.com/carina-rs/carina/issues/180">#180</a>）など。こういった問題もすべてイシューとして登録させて、同じフローで潰していった。</p>
<hr />
<h2>Day 4-6: enumの大掃除</h2>
<p>べき等性の問題を追いかけていくと、根本原因がenum処理の一貫性のなさに行き着いた。</p>
<p>enumの正規化・バリデーション・変換まわりはアドホックなコードが多く、<code>normalize_instance_tenancy</code>、<code>normalize_region</code>のような個別のハードコードされた関数が各crateに散らばっていて、それぞれ微妙に違う挙動をしていた。</p>
<p>ここからenumの大掃除が始まった。</p>
<ul>
<li>複数crateに散らばっていた<code>normalize_*</code>関数を<code>carina-core::utils</code>に集約（<a href="https://github.com/carina-rs/carina/issues/201">#201</a>、<a href="https://github.com/carina-rs/carina/issues/204">#204</a>）</li>
<li><code>get_enum_valid_values()</code>をスキーマ定義から自動生成（<a href="https://github.com/carina-rs/carina/issues/186">#186</a>）</li>
<li>大文字小文字を区別しないenum照合（<a href="https://github.com/carina-rs/carina/issues/217">#217</a>）</li>
<li><code>extract_enum_value</code>パターンの重複排除（<a href="https://github.com/carina-rs/carina/issues/203">#203</a>、<a href="https://github.com/carina-rs/carina/issues/209">#209</a>、<a href="https://github.com/carina-rs/carina/issues/219">#219</a>）</li>
<li>ハードコードされた特殊処理の削除（<a href="https://github.com/carina-rs/carina/issues/199">#199</a>）</li>
</ul>
<p>ひとつ直すと「この関数と同じパターンがあっちにもある」「この関数、もう使われてないのでは」と芋づる式にイシューが出てくる。Claude Codeに「さっき直した関数と同じパターンが他にもないか探して」と指示すると、重複を見つけてイシューを登録し、修正してくれる。</p>
<hr />
<h2>Day 7: APIの引き締め</h2>
<p>enumの大掃除が一段落したところで、今度はAPIの公開範囲の見直しに入った。</p>
<ul>
<li><code>validate_iam_policy_document</code>をprivateに（<a href="https://github.com/carina-rs/carina/issues/231">#231</a>）</li>
<li><code>validate_namespaced_enum</code>を<code>pub(crate)</code>に（<a href="https://github.com/carina-rs/carina/issues/233">#233</a>）</li>
<li>type factory関数を<code>pub(crate)</code>に（<a href="https://github.com/carina-rs/carina/issues/236">#236</a>）</li>
<li><code>validate_*</code>関数のエラーメッセージ形式を統一（<a href="https://github.com/carina-rs/carina/issues/228">#228</a>）</li>
</ul>
<p>この日は13個のイシューが登録され、13個すべてクローズ。登録してすぐ直す、のリズムが回っていた。</p>
<p>同じ日に、コード生成まわりのバグもいくつか見つかった。VPC Endpoint IDの演算子優先順位問題（<a href="https://github.com/carina-rs/carina/issues/244">#244</a>）は、初日に登録されていた<a href="https://github.com/carina-rs/carina/issues/132">#132</a>の派生バグで、5日かけて全箇所を潰した形になる。</p>
<hr />
<h2>Day 8-9: CLIからプロバイダ固有ロジックを追い出す</h2>
<p>初日のレビューで「carina-coreやCLIにプロバイダ固有のロジックが入り込んでいる」（<a href="https://github.com/carina-rs/carina/issues/155">#155</a>）というイシューが登録されていた。ここに本格的に着手。</p>
<p><code>ProviderFactory</code>トレイトを導入して、設定バリデーション、リージョン抽出、プロバイダインスタンス化、スキーマ読み込みを抽象化した（<a href="https://github.com/carina-rs/carina/issues/259">#259</a>）。CLI側にあった<code>&quot;aws&quot;</code>や<code>&quot;awscc&quot;</code>のmatchブロックがなくなり、新しいプロバイダを追加してもCLIのコードを変更する必要がなくなった。</p>
<p>enum処理のうちプロバイダ固有のものも、CLI側からProvider trait側に移動させた（<a href="https://github.com/carina-rs/carina/issues/185">#185</a>、<a href="https://github.com/carina-rs/carina/issues/190">#190</a>）。</p>
<hr />
<h2>全体を振り返って</h2>
<p>10日間のサイクルで実装された主な機能・改善をまとめておく。</p>
<p><strong>新機能:</strong></p>
<ul>
<li>plan fileの保存とapply（<code>carina plan --out=plan.json</code> / <code>carina apply plan.json</code>）</li>
<li>孤児リソースの自動検出・削除</li>
<li><code>--detailed-exitcode</code>フラグ</li>
<li><code>lifecycle { force_delete }</code>メタ引数</li>
<li><code>&lt;attr&gt;_prefix</code>サポート（ランダムサフィックス付きリソース名生成）</li>
<li>unknown fieldのLevenshtein距離によるサジェスト</li>
<li>enumエイリアス（<code>IpProtocol</code>の<code>all</code> → <code>-1</code>など）</li>
</ul>
<p><strong>リファクタリング:</strong></p>
<ul>
<li>リソース識別子の再設計（<code>name</code>属性からの分離）</li>
<li>enumシステムの全面書き直し（17 PR）</li>
<li>型の厳密化（String → 具体的なARN型、リソースID型、enum型など）</li>
<li><code>ProviderFactory</code> traitによるCLI/プロバイダの分離</li>
<li>API公開範囲の引き締め</li>
</ul>
<p><strong>テスト:</strong></p>
<ul>
<li>テスト数: 250 → 454</li>
<li>apply後のplan verifyによるべき等性チェック</li>
<li>テストランナーのシグナルハンドリング、事前認証、クリーンアップ改善</li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Carina awscc providerのacceptance test]]></title>
        <link href="https://mizzy.org/blog/2026/02/11/2/"/>
        <updated>2026-02-11T21:27:18+09:00</updated>
        <id>https://mizzy.org/blog/2026/02/11/2/</id>
        <content type="html"><![CDATA[
<p><a href="https://github.com/carina-rs/carina">Carina</a>のawscc providerにacceptance testを追加した。実際にAWSリソースを作成・削除して、プロバイダーが正しく動作するかを確認するテスト。</p>
<p>Carinaのプロバイダーのコードは全部生成AIにつくってもらっているので、網羅的なテストが重要になる。自分の手でコードを書いていれば、ある程度の品質は把握できるが、生成AIが書いたコードはちゃんとテストしないと信用できない。一方で、こういった地道なacceptance testを手作業で整備・実行・確認するのは正直面倒なので、テストの生成、実行、確認も全部生成AIにやってもらっている。コードを書くのも生成AI、テストを書くのも生成AI、テストを実行して確認して見つかったバグを直すのも生成AI、という構図。</p>
<hr />
<h2>テストの構成</h2>
<p>テストは<code>.crn</code>ファイルとして記述する。リソースタイプごとにディレクトリを分け、パターンごとにファイルを用意している。現時点で20のEC2リソースタイプに対して39のテストファイルがある。</p>
<pre><code>acceptance-tests/
├── ec2_vpc/
│   ├── basic.crn
│   └── with_ipam.crn
├── ec2_subnet/
│   ├── basic.crn
│   ├── ipv6.crn
│   ├── with_dns_options.crn
│   └── with_ipam.crn
├── ec2_route/
│   ├── igw.crn
│   ├── ipv6.crn
│   ├── nat_gateway.crn
│   ├── transit_gateway.crn
│   └── vpc_peering.crn
├── ...
└── run-tests.sh
</code></pre>
<p>テストファイルの中身は通常の<code>.crn</code>ファイルと同じ。たとえば<code>ec2_vpc/basic.crn</code>はこんな感じ。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="c"># EC2 VPC - Basic test</span>
</span></span><span class="line"><span class="cl"><span class="c">#</span>
</span></span><span class="line"><span class="cl"><span class="c"># Tests: cidr_block, enable_dns_support, enable_dns_hostnames, instance_tenancy, tags</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">provider</span> <span class="nv">awscc</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">region</span> <span class="o">=</span> <span class="no">aws.Region.ap_northeast_1</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nc">awscc.ec2_vpc</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">name</span>                 <span class="o">=</span> <span class="s2">&#34;acceptance-test-vpc-basic&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">cidr_block</span>           <span class="o">=</span> <span class="s2">&#34;10.0.0.0/16&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">enable_dns_support</span>   <span class="o">=</span> <span class="kc">true</span>
</span></span><span class="line"><span class="cl">  <span class="nv">enable_dns_hostnames</span> <span class="o">=</span> <span class="kc">true</span>
</span></span><span class="line"><span class="cl">  <span class="nv">instance_tenancy</span>     <span class="o">=</span> <span class="nv">default</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="nv">tags</span> <span class="o">=</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">E</span><span class="nv">nvironment</span> <span class="o">=</span> <span class="s2">&#34;acceptance-test&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><p>テストといっても特別なフレームワークは使っておらず、Carinaの<code>.crn</code>ファイルをそのまま使っている。このファイルに対して<code>validate</code>→<code>apply</code>→<code>destroy</code>を実行して、すべて成功すればOK。</p>
<hr />
<h2>テストランナー</h2>
<p><code>run-tests.sh</code>というシェルスクリプトでテストを実行する。以下のコマンドをサポートしている。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="c1"># AWSクレデンシャル不要</span>
</span></span><span class="line"><span class="cl">./run-tests.sh validate
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># aws-vaultで単一アカウント実行</span>
</span></span><span class="line"><span class="cl">aws-vault <span class="nb">exec</span> myprofile -- ./run-tests.sh plan
</span></span><span class="line"><span class="cl">aws-vault <span class="nb">exec</span> myprofile -- ./run-tests.sh apply
</span></span><span class="line"><span class="cl">aws-vault <span class="nb">exec</span> myprofile -- ./run-tests.sh destroy
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># 10アカウント並列でapply+destroyサイクル</span>
</span></span><span class="line"><span class="cl">./run-tests.sh full
</span></span></code></pre><p><code>validate</code>はAWSクレデンシャル不要で、DSLの構文チェックのみ。<code>plan</code>/<code>apply</code>/<code>destroy</code>は単一アカウントに対して実行する。</p>
<hr />
<h2>10アカウント並列実行</h2>
<p><code>full</code>コマンドは、10個のAWSアカウント（<code>carina-test-000</code>〜<code>carina-test-009</code>）を用意して、テストをラウンドロビンで各アカウントに分配し、並列でapply+destroyサイクルを実行する。</p>
<pre class="chroma"><code><span class="line"><span class="cl">./run-tests.sh full
</span></span></code></pre><p>を実行すると、39個のテストが10アカウントに分配され、各アカウントで4個前後のテストが並列に走る。テストごとにリソースを作成して、正常に作成できたことを確認したらすぐに削除する。</p>
<p>10アカウントを用意している理由は、テスト時間の短縮と、テスト間の独立性の確保。同じアカウントで多くのリソースを同時に作成・削除すると、AWSのAPI rate limitやリソースクォータに引っかかったり、リソース間の依存関係で問題が起きたりする。アカウントを分けることでこれらを回避している。</p>
<hr />
<h2>テストで見つかったバグ</h2>
<p>このacceptance testを実行してみて、いくつかバグが見つかった。</p>
<ul>
<li><a href="https://github.com/carina-rs/carina/issues/75">#75</a> - <code>apply</code>で一部リソースの作成に失敗しても終了コード0を返していた</li>
<li><a href="https://github.com/carina-rs/carina/issues/76">#76</a> - IPAM Poolの削除がCloud Control API経由だとタイムアウトする</li>
<li><a href="https://github.com/carina-rs/carina/issues/77">#77</a> - IPAM PoolのCIDR伝播遅延でサブネット割り当てが失敗する</li>
<li><a href="https://github.com/carina-rs/carina/issues/80">#80</a> - 削除に時間がかかるリソースがあると、依存リソースの削除順序が崩れる</li>
<li><a href="https://github.com/carina-rs/carina/issues/81">#81</a> - <code>ipv4_ipam_pool_id</code>が<code>AwsResourceId</code>型になっていた（正しくは<code>IpamPoolId</code>型）</li>
</ul>
<p>単体テストだけでは見つけられなかったバグが結構出てきたので、やはり実際のAWSに対するテストは重要。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Carinaの型システム強化]]></title>
        <link href="https://mizzy.org/blog/2026/02/11/1/"/>
        <updated>2026-02-11T20:44:44+09:00</updated>
        <id>https://mizzy.org/blog/2026/02/11/1/</id>
        <content type="html"><![CDATA[
<p><a href="https://mizzy.org/blog/2026/02/04/1/">前回のawscc providerの自動スキーマ生成の記事</a>では、CloudFormationスキーマからRustコードを自動生成するとともに、元がString型であっても、descriptionから列挙型であることが推測される場合には、名前空間付き列挙型に変換する仕組みを導入した。</p>
<p>今回も型まわりの強化を進めてみた。</p>
<hr />
<h2>リソースID型の厳密化</h2>
<p>以前は、VPC IDもSubnet IDもSecurity Group IDも、すべて<code>String</code>型として扱っていた。その後<code>AwsResourceId</code>型を導入したが、それだとVPC IDを期待する属性にSubnet IDを渡しても型エラーにならない。</p>
<p>そこで、リソースタイプごとにプレフィックスバリデーション付きの専用型を導入した。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">vpc_id</span><span class="p">()</span><span class="w"> </span>-&gt; <span class="nc">AttributeType</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">AttributeType</span>::<span class="n">Custom</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">name</span>: <span class="s">&#34;VpcId&#34;</span><span class="p">.</span><span class="n">to_string</span><span class="p">(),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">base</span>: <span class="nb">Box</span>::<span class="n">new</span><span class="p">(</span><span class="n">AttributeType</span>::<span class="nb">String</span><span class="p">),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">validate</span>: <span class="o">|</span><span class="n">value</span><span class="o">|</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="k">if</span><span class="w"> </span><span class="kd">let</span><span class="w"> </span><span class="n">Value</span>::<span class="nb">String</span><span class="p">(</span><span class="n">s</span><span class="p">)</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">value</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="n">validate_prefixed_resource_id</span><span class="p">(</span><span class="n">s</span><span class="p">,</span><span class="w"> </span><span class="s">&#34;vpc&#34;</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">}</span><span class="w"> </span><span class="k">else</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="nb">Err</span><span class="p">(</span><span class="s">&#34;Expected string&#34;</span><span class="p">.</span><span class="n">to_string</span><span class="p">())</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">},</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">namespace</span>: <span class="nb">None</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>現時点で以下の12種類の専用型がある。</p>
<table>
<thead>
<tr>
<th>型</th>
<th>プレフィックス</th>
<th>例</th>
</tr>
</thead>
<tbody>
<tr>
<td>VpcId</td>
<td><code>vpc-</code></td>
<td><code>vpc-1a2b3c4d</code></td>
</tr>
<tr>
<td>SubnetId</td>
<td><code>subnet-</code></td>
<td><code>subnet-0123456789abcdef0</code></td>
</tr>
<tr>
<td>SecurityGroupId</td>
<td><code>sg-</code></td>
<td><code>sg-12345678</code></td>
</tr>
<tr>
<td>InternetGatewayId</td>
<td><code>igw-</code></td>
<td><code>igw-12345678</code></td>
</tr>
<tr>
<td>RouteTableId</td>
<td><code>rtb-</code></td>
<td><code>rtb-abcdef12</code></td>
</tr>
<tr>
<td>NatGatewayId</td>
<td><code>nat-</code></td>
<td><code>nat-0123456789abcdef0</code></td>
</tr>
<tr>
<td>VpcPeeringConnectionId</td>
<td><code>pcx-</code></td>
<td><code>pcx-12345678</code></td>
</tr>
<tr>
<td>TransitGatewayId</td>
<td><code>tgw-</code></td>
<td><code>tgw-0123456789abcdef0</code></td>
</tr>
<tr>
<td>VpnGatewayId</td>
<td><code>vgw-</code></td>
<td><code>vgw-12345678</code></td>
</tr>
<tr>
<td>EgressOnlyInternetGatewayId</td>
<td><code>eigw-</code></td>
<td><code>eigw-12345678</code></td>
</tr>
<tr>
<td>VpcEndpointId</td>
<td><code>vpce-</code></td>
<td><code>vpce-0123456789abcdef0</code></td>
</tr>
<tr>
<td>IpamPoolId</td>
<td><code>ipam-pool-</code></td>
<td><code>ipam-pool-0123456789abcdef0</code></td>
</tr>
</tbody>
</table>
<hr />
<h2>IamPolicyDocument型</h2>
<p>IAMロールの信頼ポリシーやインラインポリシーなど、IAMポリシードキュメントはネストされたリストやマップを含むため、<code>Map&lt;String&gt;</code>型では表現できない。そこで<code>IamPolicyDocument</code>型を導入し、構造レベルのバリデーションを追加した。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">iam_policy_document</span><span class="p">()</span><span class="w"> </span>-&gt; <span class="nc">AttributeType</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">AttributeType</span>::<span class="n">Custom</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">name</span>: <span class="s">&#34;IamPolicyDocument&#34;</span><span class="p">.</span><span class="n">to_string</span><span class="p">(),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">base</span>: <span class="nb">Box</span>::<span class="n">new</span><span class="p">(</span><span class="n">AttributeType</span>::<span class="n">Map</span><span class="p">(</span><span class="nb">Box</span>::<span class="n">new</span><span class="p">(</span><span class="n">AttributeType</span>::<span class="nb">String</span><span class="p">))),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">validate</span>: <span class="o">|</span><span class="n">value</span><span class="o">|</span><span class="w"> </span><span class="n">validate_iam_policy_document</span><span class="p">(</span><span class="n">value</span><span class="p">),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">namespace</span>: <span class="nb">None</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>バリデーターは以下をチェックする。</p>
<ul>
<li><code>version</code>が<code>&quot;2012-10-17&quot;</code>か<code>&quot;2008-10-17&quot;</code>であること</li>
<li><code>statement</code>がリストであること</li>
<li>各Statementの<code>effect</code>が<code>&quot;Allow&quot;</code>か<code>&quot;Deny&quot;</code>であること</li>
</ul>
<p>現時点ではまだ基本的な構造チェックのみなので、<code>action</code>や<code>resource</code>、<code>principal</code>のバリデーションなど、今後さらに厳密にしていく予定。</p>
<p><code>.crn</code>ファイルではこんな感じで使う。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">let</span> <span class="nv">flow_log_role</span> <span class="o">=</span> <span class="nc">awscc.iam_role</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">name</span>      <span class="o">=</span> <span class="s2">&#34;flow-log-role&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">role_name</span> <span class="o">=</span> <span class="s2">&#34;flow-log-role&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">assume_role_policy_document</span> <span class="o">=</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nv">version</span> <span class="o">=</span> <span class="s2">&#34;2012-10-17&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="nv">statement</span> <span class="o">=</span> <span class="p">[</span>
</span></span><span class="line"><span class="cl">      <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="nv">effect</span>    <span class="o">=</span> <span class="s2">&#34;Allow&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="nv">principal</span> <span class="o">=</span> <span class="p">{</span> <span class="nv">service</span> <span class="o">=</span> <span class="s2">&#34;vpc-flow-logs.amazonaws.com&#34;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="nv">action</span>    <span class="o">=</span> <span class="s2">&#34;sts:AssumeRole&#34;</span>
</span></span><span class="line"><span class="cl">      <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">]</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><p>Terraformでは、<code>aws_iam_policy_document</code> data sourceを使えば、補完や構文チェックができるが、inline policyの場合はただのJSON文字列なので、補完も構文チェックもできない。Carinaでは<code>IamPolicyDocument</code>型を使うことで、インラインポリシーの記述でも補完と構文チェックを可能にしたいと考えている。</p>
<hr />
<h2>リソース間の型チェック</h2>
<p>リソース参照時の型の不一致も検出できるようにした。</p>
<p>たとえば、こんなコードを書いたとする。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">let</span> <span class="nv">vpc</span> <span class="o">=</span> <span class="nc">awscc.ec2_vpc</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">name</span>       <span class="o">=</span> <span class="s2">&#34;my-vpc&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">cidr_block</span> <span class="o">=</span> <span class="s2">&#34;10.0.0.0/16&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nc">awscc.ec2_vpc</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">name</span>              <span class="o">=</span> <span class="s2">&#34;another-vpc&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">ipv4_ipam_pool_id</span> <span class="o">=</span> <span class="nv">vpc</span><span class="p">.</span><span class="nv">vpc_id</span>  <span class="c"># これは間違い！</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><p><code>ipv4_ipam_pool_id</code>は<code>IpamPoolId</code>型を期待しているが、<code>vpc.vpc_id</code>は<code>VpcId</code>型。以前はどちらも<code>String</code>だったので素通りしていたが、今はバリデーション時に型の不一致としてエラーになる。</p>
<p>型の互換性ルールはシンプルにしている。</p>
<ul>
<li>同じ型 → OK</li>
<li>どちらかが<code>String</code>→ OK（後方互換のため）</li>
<li>異なるCustom型 → エラー</li>
</ul>
<p>このチェックはCLIの<code>validate</code>/<code>plan</code>/<code>apply</code>コマンドで実行されるほか、LSPでもリアルタイムにエディタ上で警告が表示される。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Carina organization移行とアイコン・ドキュメント整備]]></title>
        <link href="https://mizzy.org/blog/2026/02/08/2/"/>
        <updated>2026-02-08T14:12:38+09:00</updated>
        <id>https://mizzy.org/blog/2026/02/08/2/</id>
        <content type="html"><![CDATA[
<p><a href="https://github.com/carina-rs/carina">Carina</a>のリポジトリを個人アカウント（mizzy）から<a href="https://github.com/carina-rs">carina-rs</a> organizationに移した。あわせて、アイコンの作成と<a href="https://carina-rs.github.io/">ドキュメントサイト</a>の公開もやった。</p>
<hr />
<h2>organizationへの移行</h2>
<p>これまで<a href="https://github.com/mizzy/carina">mizzy/carina</a>として個人アカウント配下に置いていたが、<a href="https://github.com/carina-rs/carina">carina-rs/carina</a>に移した。</p>
<p>移行の主な理由は、ドキュメントサイトをGitHub Pagesでホストしたかったから。GitHub Pagesでorganizationサイト（<code>carina-rs.github.io</code>）を使えば、プロジェクト専用のドメインでドキュメントを公開できる。organization名は<code>carina-rs</code>にした。Rustプロジェクトでよくある命名だし、<code>carina</code>という名前はすでに取られていたので。</p>
<p>GitHubのリポジトリ移行機能を使えば、旧URLからのリダイレクトも自動で設定されるので、既存のリンクが壊れる心配もない。</p>
<hr />
<h2>アイコンの作成</h2>
<p>ChatGPTにお願いしてアイコンをつくってもらった。りゅうこつ座（Carina）の船の竜骨と、カノープスをイメージしたデザイン。カノープスはりゅうこつ座のα星で、シリウスに次いで全天で2番目に明るい恒星（太陽を除く）。ただ、日本では南の空の低い位置にしか見えないのでマイナーな印象がある。organizationのアバターと、リポジトリのREADMEのヘッダーにも使っている。</p>
<hr />
<h2>ドキュメントサイト</h2>
<p><a href="https://rust-lang.github.io/mdBook/">mdBook</a>を使ってドキュメントサイトをつくり、GitHub Pagesでホストするようにした。プロバイダのリソースドキュメントはCloudFormationスキーマから自動生成し、それ以外はClaude Codeにつくってもらった。</p>
<p><a href="https://carina-rs.github.io/">Introduction - Carina Documentation</a></p>
<p>現時点では、Carinaの概要や主要機能の説明、クイックスタート、プロバイダのドキュメントなどを載せている。開発が進むにつれて、ドキュメントも充実させていく予定。</p>
<p>mdBookはRust製のドキュメントジェネレーターで、Markdownから静的サイトを生成してくれる。Rust製ツールのドキュメントによく使われているらしい。</p>
<hr />
<h2>関連リンク</h2>
<ul>
<li><a href="https://github.com/carina-rs/carina">carina-rs/carina: A strongly typed infrastructure management tool</a></li>
<li><a href="https://carina-rs.github.io/">Introduction - Carina Documentation</a></li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Carina aws providerとawscc providerに互換性を持たせるのをやめた]]></title>
        <link href="https://mizzy.org/blog/2026/02/08/1/"/>
        <updated>2026-02-08T12:34:04+09:00</updated>
        <id>https://mizzy.org/blog/2026/02/08/1/</id>
        <content type="html"><![CDATA[
<p>以前、<a href="https://mizzy.org/blog/2026/01/29/2/">Carina aws providerとawscc provider</a>という記事で、aws providerとawscc providerのスキーマをCloud Control APIベースで統一し、<code>aws</code>と<code>awscc</code>を置換するだけで切り替えられるようにしている、と書いた。この方針をやめた。</p>
<hr />
<h2>やめた理由</h2>
<p>aws providerはSDKを直接叩くので、SDKのAPI構造に合わせた方が自然だし、パフォーマンスも有利になる。無理にawscc providerと同じスキーマに合わせると、SDKの良さを活かしきれない。</p>
<p>一方、awscc providerはCloud Control APIを使っているので、CloudFormationのスキーマにそのまま合わせるのが最も自然。両者の性質が異なるのに、無理に互換性を持たせようとすると後々無理が出てきそうだな、と思った。</p>
<p>また、仮にプロバイダ間で切り替えたい場合でも、今の時代なら生成AIに変換を任せれば良い。人間が手作業で置換することを前提にスキーマの互換性を維持するより、それぞれのプロバイダに最適な設計にした方が合理的だろう。</p>
<hr />
<h2>変更内容</h2>
<h3>awscc providerの命名規則変更</h3>
<p>awscc providerのリソース名を、CloudFormationのリソースタイプ名に完全に合わせるようにした。</p>
<p>変更前:</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="nc">awscc.vpc</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">name</span>       <span class="o">=</span> <span class="s2">&#34;example-vpc&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">cidr_block</span> <span class="o">=</span> <span class="s2">&#34;10.0.0.0/16&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><p>変更後:</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="nc">awscc.ec2_vpc</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nv">name</span>       <span class="o">=</span> <span class="s2">&#34;example-vpc&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="nv">cidr_block</span> <span class="o">=</span> <span class="s2">&#34;10.0.0.0/16&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><p>CloudFormationでは<code>AWS::EC2::VPC</code>というリソースタイプ名なので、awscc providerでも<code>awscc.ec2_vpc</code>とする。以前は<code>awscc.vpc</code>のようにサービス名を省略していたが、CloudFormationのスキーマから自動生成する際にも、この方が素直にマッピングできる。</p>
<h3>aws providerのスキーマ</h3>
<p>aws providerのスキーマは引き続きCloudFormationのスキーマをベースにするが、awscc providerとの完全な互換性は維持しない。SDKのAPI構造に合わせた方が自然な場合は、そちらに寄せる方針にした。具体的にどう変えるかはまだこれからだが、大きな方針として決めた。</p>
<hr />
<h2>まとめ</h2>
<p>以前は「<code>aws</code>と<code>awscc</code>を置換するだけで切り替えられる」ことを目指していたが、両者の性質が根本的に異なるため、それぞれに適したスキーマ設計をした方がよい、という結論になった。awscc providerはCloudFormationに忠実に、aws providerはSDKに寄せる。ユーザーにとっても、それぞれのプロバイダの特性が明確になる方がわかりやすいはず。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Carina awscc providerの自動スキーマ生成]]></title>
        <link href="https://mizzy.org/blog/2026/02/04/1/"/>
        <updated>2026-02-04T09:40:42+09:00</updated>
        <id>https://mizzy.org/blog/2026/02/04/1/</id>
        <content type="html"><![CDATA[
<p><a href="https://github.com/mizzy/carina">Carina</a>を1週間ほどコードを見ずにClaude Codeにつくらせて、それっぽく動くようにはなったけど、そろそろコードを読んで指示出さないといけないフェーズに入ったかな、と思い、まずはawscc providerのコードを読んでいる。</p>
<p>実際にコードを読んでみると、awscc providerはAWS Cloud Control APを利用しているので、aws-sdk-cloudcontrol crateだけを利用すればいいはずなのに、なぜかaws-sdk-ec2 crateも利用している、といったおかしなことになっていた。</p>
<p>また、プロバイダー用スキーマは、<a href="https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/resource-type-schemas.html">CloudFormation resource provider schemas</a>から自動生成できるはずだがそうなっておらず、Claude Codeが手動？でスキーマを定義していた。（これはそのように指示を出していない自分が悪いのだが。）</p>
<p>というわけで自動生成するようにしてもらった。こんな感じで、スキーマをRustコードの形で自動生成してくれる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="sd">//! internet_gateway schema definition for AWS Cloud Control
</span></span></span><span class="line"><span class="cl"><span class="sd">//!
</span></span></span><span class="line"><span class="cl"><span class="sd">//! Auto-generated from CloudFormation schema: AWS::EC2::InternetGateway
</span></span></span><span class="line"><span class="cl"><span class="sd">//!
</span></span></span><span class="line"><span class="cl"><span class="sd">//! DO NOT EDIT MANUALLY - regenerate with carina-codegen
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">use</span><span class="w"> </span><span class="k">super</span>::<span class="n">tags_type</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">use</span><span class="w"> </span><span class="n">carina_core</span>::<span class="n">schema</span>::<span class="p">{</span><span class="n">AttributeSchema</span><span class="p">,</span><span class="w"> </span><span class="n">AttributeType</span><span class="p">,</span><span class="w"> </span><span class="n">ResourceSchema</span><span class="p">};</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="sd">/// Returns the schema for ec2_internet_gateway (AWS::EC2::InternetGateway)
</span></span></span><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">ec2_internet_gateway_schema</span><span class="p">()</span><span class="w"> </span>-&gt; <span class="nc">ResourceSchema</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">ResourceSchema</span>::<span class="n">new</span><span class="p">(</span><span class="s">&#34;awscc.ec2_internet_gateway&#34;</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">.</span><span class="n">with_description</span><span class="p">(</span><span class="s">&#34;Allocates an internet gateway for use with a VPC. After creating the Internet gateway, you then attach it to a VPC.&#34;</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">.</span><span class="n">attribute</span><span class="p">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="n">AttributeSchema</span>::<span class="n">new</span><span class="p">(</span><span class="s">&#34;internet_gateway_id&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">AttributeType</span>::<span class="nb">String</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="p">.</span><span class="n">with_description</span><span class="p">(</span><span class="s">&#34; (read-only)&#34;</span><span class="p">),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">.</span><span class="n">attribute</span><span class="p">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="n">AttributeSchema</span>::<span class="n">new</span><span class="p">(</span><span class="s">&#34;tags&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">tags_type</span><span class="p">())</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="p">.</span><span class="n">with_description</span><span class="p">(</span><span class="s">&#34;Any tags to assign to the internet gateway.&#34;</span><span class="p">),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>VPCの<code>cidr_block</code>は、CloudFormation resource provider schemaでは<code>string</code>型になっている。</p>
<pre class="chroma"><code><span class="line"><span class="cl">        <span class="s2">&#34;CidrBlock&#34;</span><span class="err">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="nt">&#34;description&#34;</span><span class="p">:</span> <span class="s2">&#34;The IPv4 network range for the VPC, in CIDR notation. For example, ``10.0.0.0/16``. We modify the specified CIDR block to its canonical form; for example, if you specify ``100.68.0.18/18``, we modify it to ``100.68.0.0/18``.\n You must specify either``CidrBlock`` or ``Ipv4IpamPoolId``.&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">            <span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;string&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span><span class="err">,</span>
</span></span></code></pre><p>だが、Carinaは強い型付けを目指しているので、ドメイン固有型である<code>cidr</code>型に変換している。これは指示しなくてもClaude Codeが勝手にそうしてくれた。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="w">        </span><span class="p">.</span><span class="n">attribute</span><span class="p">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="n">AttributeSchema</span>::<span class="n">new</span><span class="p">(</span><span class="s">&#34;cidr_block&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">types</span>::<span class="n">cidr</span><span class="p">())</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="p">.</span><span class="n">with_description</span><span class="p">(</span><span class="s">&#34;The IPv4 network range for the VPC, in CIDR notation. For example, ``10.0.0.0/16``. We modify the specified CIDR block to its canonical form; for exam...&#34;</span><span class="p">),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">)</span><span class="w">
</span></span></span></code></pre><p>VPCの<code>instance_tenancy</code>属性は、CloudFormation resource provider schemaでは<code>string</code>型になっているが、取り得る値は3つに限定されていることがdescriptionから読み取れる。</p>
<pre class="chroma"><code><span class="line"><span class="cl">        <span class="s2">&#34;InstanceTenancy&#34;</span><span class="err">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl"><span class="nt">&#34;description&#34;</span><span class="p">:</span> <span class="s2">&#34;The allowed tenancy of instances launched into the VPC.\n  +  ``default``: An instance launched into the VPC runs on shared hardware by default, unless you explicitly specify a different tenancy during instance launch.\n  +  ``dedicated``: An instance launched into the VPC runs on dedicated hardware by default, unless you explicitly specify a tenancy of ``host`` during instance launch. You cannot specify a tenancy of ``default`` during instance launch.\n  \n Updating ``InstanceTenancy`` requires no replacement only if you are updating its value from ``dedicated`` to ``default``. Updating ``InstanceTenancy`` from ``default`` to ``dedicated`` requires replacement.&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl"><span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;string&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span><span class="err">,</span>
</span></span></code></pre><p>これを何も考えずにRustコードに変換すると、当然のことながら<code>string</code>型になる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="w">        </span><span class="p">.</span><span class="n">attribute</span><span class="p">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="n">AttributeSchema</span>::<span class="n">new</span><span class="p">(</span><span class="s">&#34;instance_tenancy&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">AttributeType</span>::<span class="nb">String</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="p">.</span><span class="n">with_description</span><span class="p">(</span><span class="s">&#34;The allowed tenancy of instances launched into the VPC.  + ``default``: An instance launched into the VPC runs on shared hardware by default, unless y...&#34;</span><span class="p">),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">)</span><span class="w">
</span></span></span></code></pre><p>そこで、コードジェネレーターでdescriptionを解析して取り得る値を抽出し、名前空間付き列挙型に変換するようにしてみた。</p>
<p>こんな感じで、列挙型のバリデーション関数を生成してくれる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">const</span><span class="w"> </span><span class="no">VALID_INSTANCE_TENANCY</span>: <span class="kp">&amp;</span><span class="p">[</span><span class="o">&amp;</span><span class="kt">str</span><span class="p">]</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">&amp;</span><span class="p">[</span><span class="s">&#34;default&#34;</span><span class="p">,</span><span class="w"> </span><span class="s">&#34;dedicated&#34;</span><span class="p">,</span><span class="w"> </span><span class="s">&#34;host&#34;</span><span class="p">];</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">fn</span> <span class="nf">validate_instance_tenancy</span><span class="p">(</span><span class="n">value</span>: <span class="kp">&amp;</span><span class="nc">Value</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nb">Result</span><span class="o">&lt;</span><span class="p">(),</span><span class="w"> </span><span class="nb">String</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">validate_namespaced_enum</span><span class="p">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">value</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="s">&#34;InstanceTenancy&#34;</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="s">&#34;awscc.ec2_vpc&#34;</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="no">VALID_INSTANCE_TENANCY</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>そして、スキーマ定義では、カスタム属性型として定義してくれる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="w">        </span><span class="p">.</span><span class="n">attribute</span><span class="p">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="n">AttributeSchema</span>::<span class="n">new</span><span class="p">(</span><span class="s">&#34;instance_tenancy&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">AttributeType</span>::<span class="n">Custom</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="n">name</span>: <span class="s">&#34;InstanceTenancy&#34;</span><span class="p">.</span><span class="n">to_string</span><span class="p">(),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="n">base</span>: <span class="nb">Box</span>::<span class="n">new</span><span class="p">(</span><span class="n">AttributeType</span>::<span class="nb">String</span><span class="p">),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="n">validate</span>: <span class="nc">validate_instance_tenancy</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="n">namespace</span>: <span class="nb">Some</span><span class="p">(</span><span class="s">&#34;awscc.ec2_vpc&#34;</span><span class="p">.</span><span class="n">to_string</span><span class="p">()),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">})</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="p">.</span><span class="n">with_description</span><span class="p">(</span><span class="s">&#34;The allowed tenancy of instances launched into the VPC.  + ``default``: An instance launched into the VPC runs on shared hardware by default, unless y...&#34;</span><span class="p">),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">)</span><span class="w">
</span></span></span></code></pre>
]]></content>
    </entry>

</feed>
