<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title><![CDATA[Gosuke Miyashita]]></title>
    <link href="https://mizzy.org/atom.xml" rel="self"/>
    <link href="https://mizzy.org/"/>
    <updated>2026-07-04T19:00:00+09:00</updated>
    <id>https://mizzy.org/</id>
    <author>
        <name><![CDATA[Gosuke Miyashita]]></name>
    </author>
    <generator uri="https://github.com/mizzy/nebel/">Nebel</generator>

    <entry>
        <title type="html"><![CDATA[Peithoの発表者ツールをつくっている]]></title>
        <link href="https://mizzy.org/blog/2026/07/04/1/"/>
        <updated>2026-07-04T19:00:00+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/04/1/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/07/03/1/">前回の記事</a>でプレゼンツール<a href="https://github.com/mizzy/peitho">Peitho</a>を紹介した。前回はスライドを書いて生成するところ（Markdownを書いてビルドすると型検査つきでHTMLが出てくる話）が中心だったので、今回は発表するところ、つまり発表者ツールまわりについて書く。</p>
<p>前回、Peithoをつくった動機として「プレゼンツールも自分が使いやすいものがほしくなった」というのを挙げたけど、それがまさにこの部分。</p>
<p><code>peitho present deck.md</code>を実行するとローカルサーバが立ち上がって、スライドと発表者ツールがそれぞれ別のChromeウィンドウで開く。外部ディスプレイがあればそちらにスライドがフルスクリーンで表示され、発表者ツールはメインディスプレイに開く。発表者ツールの見た目はこんな感じ。</p>
<p><img src="/images/2026/07/peitho-presenter-overview.png" alt="Peithoの発表者ツール" /></p>
<p>左に現在のスライドとスピーカーノート、右に次のスライド、タイマーとトラッカー、セクション別の時間を並べたアジェンダ、操作ボタン、という構成。ダーク基調のデザインは<a href="https://claude.ai/design">Claude Design</a>でモックをつくって、それを反映した。以下、各パーツを見ていく。</p>
<hr />
<h2>タイマーとトラッカー</h2>
<p><img src="/images/2026/07/peitho-presenter-timer.png" alt="タイマーとトラッカー" /></p>
<p>発表の予定時間はデッキのfrontmatterに書く（<code>time: 5m</code>、<code>90s</code>、<code>1h30m</code>のような書き方）。それがタイマーの計画時間になっていて、経過時間・残時間・超過時間が表示される。超過すると赤くなって<code>+00:30</code>のように追加表示。</p>
<p>その下の細長い帯がトラッカー。かめ🐢が経過時間（フィルバーの先端と一緒に動く）、うさぎ🐰がスライドの進捗（10枚中3枚目なら30%の位置）。うさぎがかめより先にいれば余裕あり、かめに追い越されていれば遅れ気味、というのがひと目でわかる。</p>
<p>操作ボタンはStart/Pause/Resumeが1個にまとまっていて、押すたびに次のアクションが自動で決まる。</p>
<hr />
<h2>セクション別の時間を並べたアジェンダ</h2>
<p><img src="/images/2026/07/peitho-presenter-agenda.png" alt="アジェンダ" /></p>
<p>デッキのスライドの先頭にコメントでセクションの区切りを書いておくと、発表者ツールにアジェンダが出る。</p>
<pre class="chroma"><code><span class="line"><span class="cl">&lt;!-- {&#34;section&#34;:&#34;Setup&#34;,&#34;time&#34;:&#34;1m&#34;} --&gt;
</span></span><span class="line"><span class="cl"><span class="gh"># スライドはMarkdownで書きたい
</span></span></span></code></pre><p>セクション名と持ち時間はセットで書く決まり。各セクションの計画時間・実績時間・その差分が並んで、今どこにいるかがハイライトされる。</p>
<p>frontmatterの<code>time</code>（デッキ全体の予定時間）とセクション合計がズレているとビルドが止まる。「4分の発表のはずが、セクション合計が5分になっている」みたいな取り違いに、開いた瞬間ではなくビルドの瞬間に気づける。前回書いたスロット契約と同じで、「黙って何かが起きる」経路を残さない、というPeithoの基本方針の延長。</p>
<hr />
<h2>スピーカーノート</h2>
<p><img src="/images/2026/07/peitho-presenter-notes.png" alt="スピーカーノート" /></p>
<p>書き方は<a href="https://marp.app/">Marp</a>や<a href="https://github.com/k1LoW/deck">k1LoW/deck</a>と同じで、スライドの中にHTMLコメントを書くだけ。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="gh"># スライドはMarkdownで書きたい
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">&lt;!-- 自己紹介は10秒で切り上げる。「スライド作りで消耗した経験、ありますよね」と会場に問いかけてから本題へ。 --&gt;
</span></span></code></pre><p>JSON形式のコメントはページ設定、それ以外がスピーカーノート、という使い分け。ノートはビルドの配布物には混ざらないようにしてあって、<code>peitho present</code>のときだけ発表者ツールに読み込まれる。</p>
<hr />
<h2>現在/次スライドのプレビュー</h2>
<p><img src="/images/2026/07/peitho-presenter-next.png" alt="次スライドのプレビュー" /></p>
<p>左の大きい方が現在のスライド、右上の小さい方が次のスライド。実際に配布されているスライドHTMLをそのまま埋め込んでいるので、プレビュー用に別レンダリングされたものではなく本物と同じ見た目。</p>
<hr />
<p>参考:</p>
<ul>
<li><a href="https://github.com/mizzy/peitho">mizzy/peitho</a></li>
<li><a href="/blog/2026/07/03/1/">前回の記事: プレゼンツールPeithoをつくっている</a></li>
<li><a href="https://peitho.gosu.ke/">デモサイト peitho.gosu.ke</a></li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[個人用メモアプリをつくっている]]></title>
        <link href="https://mizzy.org/blog/2026/07/03/2/"/>
        <updated>2026-07-03T21:30:00+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/03/2/</id>
        <content type="html"><![CDATA[
<p>Obsidianの代替として、個人用のメモツールWebアプリをつくりはじめた。リポジトリは<a href="https://github.com/mizzy/memo">mizzy/memo</a>。Cloudflare上で動かしている。</p>
<p><img src="/images/2026/07/memo-desktop.png" alt="memoアプリのデスクトップ表示" /></p>
<hr />
<h2>なぜつくったか</h2>
<p>ObsidianはMarkdownが書けるメモツールとして常用していて、PC上での利用には特に不満はない。ただ、Obsidianの豊富な機能を使いこなしているわけでもなくて、メモを書いて読む、という基本的な使い方しかしていない。</p>
<p>問題はiOSアプリで、挙動が不安定。特に、開いたときにindexingという表示のまま進まないことがよくある。同期まわりの問題だと思うけど、外出先でさっとメモを確認したいときに待たされていらつく。</p>
<p>自分が求めているのは、Markdownで書けてPCとiPhoneで同期ができるメモツールなので、Obsidianを使い続けなくても、自分の使い方だけに特化したシンプルなツールをつくったほうがいいのでは、と考えた。データをサーバ側のDBに置くWebアプリなら、そもそも同期という概念自体が消える。PWAとしてiPhoneのホーム画面に追加すれば、起動もネイティブアプリに近い体験にできる。</p>
<p>あと、<a href="/blog/2026/06/16/1/">ホテルメモサイトをAstroで作り直した</a>ときにもObsidianの利用をやめたのだけど、生成AIのおかげで「既製ツールの不満を我慢しながら使い続ける」より「自分のユースケースだけ満たす専用ツールをさくっとつくる」ほうが早い、という状況になってきていると思う。</p>
<hr />
<h2>技術スタック</h2>
<table>
<thead>
<tr>
<th>Layer</th>
<th>Tech</th>
</tr>
</thead>
<tbody>
<tr>
<td>API</td>
<td>Hono + TypeScript</td>
</tr>
<tr>
<td>Frontend</td>
<td>React + Vite + TypeScript</td>
</tr>
<tr>
<td>Editor</td>
<td>Tiptap (ProseMirrorベース)</td>
</tr>
<tr>
<td>DB</td>
<td>Cloudflare D1 + Drizzle ORM</td>
</tr>
<tr>
<td>Storage</td>
<td>Cloudflare R2 (画像添付)</td>
</tr>
<tr>
<td>Hosting</td>
<td>Cloudflare Workers</td>
</tr>
<tr>
<td>Auth</td>
<td>Cloudflare Access</td>
</tr>
<tr>
<td>Infra</td>
<td>Terraform (stateはR2に保存)</td>
</tr>
</tbody>
</table>
<p>1つのWorkerで静的アセット(SPA)とAPIの両方を配信する、単一Worker構成にしている。当初はPagesとWorkersを分ける構成を考えていたけど、同一オリジンならCORSの設定が不要になるし、認証Cookieも何も考えなくても全リクエストに付いてくる。個人ツールでフロントとAPIを別々に更新したい場面はそもそも少ないので、分離は必要になってからで遅くない、という判断。</p>
<p>認証まわりのコードはアプリ内には一切なくて、前段のCloudflare Accessで自分のGmailアカウントだけを許可している。アプリ本体は認証のことを何も知らないまま、Cloudflareのログイン画面で守られる。個人ツールならこれで十分。</p>
<hr />
<h2>機能</h2>
<p>今のところ実装しているのはこのあたり。</p>
<ul>
<li>メモの作成・編集・一覧・検索</li>
<li>Vault機能(Obsidianと同様、常に1つのVaultを開いてスイッチャーで切り替える)</li>
<li>フォルダ機能(Vault内に任意深さのフォルダ木を持ち、デスクトップではフォルダツリー/メモ一覧/エディタの3ペイン構成)</li>
<li>画像添付(R2にアップロード)</li>
<li>PWA対応(iPhoneのホーム画面に追加して利用)</li>
</ul>
<p>デスクトップはこんな感じ。左上のVaultスイッチャーでVaultを切り替える。</p>
<p><img src="/images/2026/07/memo-vault-switcher.png" alt="Vaultスイッチャー" /></p>
<p>モバイルはこんな感じ。ホームがフォルダツリーで、フォルダを開くとメモ一覧、メモを開くとエディタ、という画面遷移にしている。</p>
<div style="display:flex;gap:12px;">
<img src="/images/2026/07/memo-mobile-home.png" alt="モバイルのフォルダツリー" style="width:calc(50% - 6px);max-width:280px;height:auto;">
<img src="/images/2026/07/memo-mobile-editor.png" alt="モバイルのエディタ" style="width:calc(50% - 6px);max-width:280px;height:auto;">
</div>
<hr />
<h2>デザイン</h2>
<p>見た目がそれっぽくないとモチベーションが上がらない、というのはホテルメモのときと同じなので、今回もビジュアルデザインはClaude Designにお願いした。まず方向性の違う3案(A: 紙とインク、B: ターミナル、C: 夜の書斎)のモックをHTMLでつくってもらって、並べて比較した。</p>
<p><img src="/images/2026/07/memo-design-compare.png" alt="デザイン方向性の比較" /></p>
<p>BとCで迷った結果、Cの雰囲気にBの道具っぽさを足したハイブリッドのD案「夜の書斎×道具」に決めた。ミッドナイトブルーの地にランプの琥珀色、本文はZen Kaku Gothic New、日付などのメタ情報はIBM Plex Mono、ロゴだけFraunces、という使い分けにしている。B案にあったプロンプト風の装飾やstatuslineみたいな、見た目だけターミナルをなぞった要素は、検討の過程で結局外した。</p>
<p>ホテルメモのときは、Claude Designがローカルファイルを直接編集できないので、デザインを直すたびにClaude Designで実装指示書をつくってClaude Codeに渡し直す、というのが面倒だった。今はClaude CodeとClaude Designの連携がしやすくなっていて、Claude Codeからデザインファイルを直接Claude Designのプロジェクトに同期できる。なので今回は、モックや現状UIのスナップショットをリポジトリの<code>design/</code>に置いてClaude Design側と同期し、Claude Designが出してきた改善版デザインをClaude Codeが実装する、という流れで回している。</p>
<hr />
<h2>今後</h2>
<p>まだつくりはじめたばかりなので、しばらくは自分で使いながら不満点を潰していく予定。Obsidianに溜まっている既存のメモの移行もこれから。</p>
<hr />
<p>参考:</p>
<ul>
<li><a href="https://github.com/mizzy/memo">mizzy/memo</a></li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[プレゼンツールPeithoをつくっている]]></title>
        <link href="https://mizzy.org/blog/2026/07/03/1/"/>
        <updated>2026-07-03T19:35:00+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/03/1/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/06/23/1/">前回の記事</a>でPyxis/Argus/Irisという最近つくっているOSSを紹介したけど、もうひとつ新しいものをつくりはじめた。<a href="https://github.com/mizzy/peitho">Peitho</a>というプレゼンテーションツールで、Markdownをsource of truthとして、HTMLネイティブなスライドを生成し、ブラウザでの発表までやる。</p>
<p>ビルドするとどんなスライドになるのかは、デモサイト<a href="https://peitho.gosu.ke/">peitho.gosu.ke</a>で見られる。リポジトリの<code>examples/</code>を<code>peitho build</code>してそのまま配信したもの。発表のほうは手元でChromeで操作するので、これは後述する。</p>
<p><img src="/images/2026/07/peitho-demo-landing.png" alt="Peithoのデモサイト" /></p>
<hr />
<h2>なぜつくったか</h2>
<p>スライドをMarkdownで書く、というのはずっとやっていて、<a href="https://www.deckset.com/">Deckset</a>から<a href="https://github.com/k1LoW/deck">k1LoW/deck</a>へとたどってきた。Decksetはデザインの自由度がなくて、「ここがもっとこうなると見やすくなるのに」というところが変更できない、かゆいところに手が届かない感が不満だった。</p>
<p>k1LoW/deckは「コンテンツとデザインの分離」という思想が最高にかっこいい。Markdownで書いたコンテンツをGoogle Slidesに反映するツールで、内容はgitで管理しつつ、デザインはGoogle Slidesに任せる、という分担がきれいに決まっている。</p>
<p>k1LoW/deckは最高なんだけど、Google Slidesでデザインを手動調整するのは、かゆいところに手が届く反面、面倒さもともなう。これがPeithoをつくりはじめた動機。今なら、ここは生成AIに任せたい。デザイン成果物がHTML/CSSというただのコードなら、AIに書かせることができるし、gitで管理してdiffを取ったりレビューしたりもできる。</p>
<p>なので、k1LoW/deckの思想は受け継ぎつつ、デザインの側をGoogle SlidesではなくHTML/CSSで持つ、という形にした。Peithoの設計は三本柱にしている。</p>
<ol>
<li><strong>コンテンツとデザインの分離</strong> — コンテンツはMarkdown、デザインはレイアウトHTMLとCSS。両者を混ぜない（k1LoW/deckリスペクト）</li>
<li><strong>git管理可能なHTML/CSSレイアウト</strong> — デザイン成果物はただのHTML/CSS。diffが取れ、レビューできる</li>
<li><strong>型検査されるスロット契約</strong> — レイアウトはスロット（タイトル・本文・コードなど、コンテンツの挿し込み口）を持ち、そこに何をいくつ書けるかを宣言する。Markdownの内容がその宣言に合わないときは、黙って捨てられる代わりに行番号とヒント付きのビルドエラーになる</li>
</ol>
<p>もうひとつ、プレゼンツールも自分が使いやすいものがほしくなった、という動機もある。ただ、まだどういう仕様にするかは詰め切れていないので、これからブラッシュアップしていく。</p>
<hr />
<h2>デッキの書き方</h2>
<p>デッキは素のMarkdownで書ける。<code>---</code>で区切ったかたまりが1枚のスライドになり、最も浅い見出しはタイトル、コードブロックはcodeスロット、残りはbodyスロットへ、という規約で自動的に割り当てられる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="gh"># タイトル
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">本文の段落。
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">-</span> リストも
</span></span><span class="line"><span class="cl"><span class="k">-</span> 使える
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">---
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="gh"># 次のスライド
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="s">```rust
</span></span></span><span class="line"><span class="cl"><span class="k">enum</span> <span class="nc">Phase</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="n">Parsed</span><span class="p">,</span><span class="w"> </span><span class="n">Mapped</span><span class="p">,</span><span class="w"> </span><span class="n">Checked</span><span class="p">,</span><span class="w"> </span><span class="n">Rendered</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="s">```</span>
</span></span></code></pre><p><code>peitho build deck.md</code>で<code>dist/</code>に配布物（スライドHTML断片+manifest.json+index.html+CSS）が生成される。manifest.jsonはデッキの目次にあたるファイルで、スライドの順序やキー、各HTML断片のパスを持っていて、index.htmlはこれを読んでスライドを順に表示する。レイアウトとテーマはバイナリに内蔵されたデフォルトがあるので、デッキファイルがひとつあればどこでも動く。差し替えたいときはデッキファイルと同じディレクトリに<code>layouts/</code>と<code>css/</code>を置けば自動検出される、というゼロコンフィグ規約にしている。</p>
<hr />
<h2>レイアウト自身がスキーマ</h2>
<p>デザインの単位はレイアウトで、ただのHTMLファイル。Web Componentsの<code>&lt;slot&gt;</code>を借用した記法で、スロットの名前・受け入れるコンテンツ型・個数（arity）を宣言する。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">section</span> <span class="na">class</span><span class="o">=</span><span class="s">&#34;peitho-slide&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="p">&lt;</span><span class="nt">h1</span><span class="p">&gt;&lt;</span><span class="nt">slot</span> <span class="na">name</span><span class="o">=</span><span class="s">&#34;title&#34;</span> <span class="na">accepts</span><span class="o">=</span><span class="s">&#34;inline&#34;</span> <span class="na">arity</span><span class="o">=</span><span class="s">&#34;1&#34;</span><span class="p">&gt;&lt;/</span><span class="nt">slot</span><span class="p">&gt;&lt;/</span><span class="nt">h1</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="p">&lt;</span><span class="nt">div</span> <span class="na">class</span><span class="o">=</span><span class="s">&#34;body&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="p">&lt;</span><span class="nt">slot</span> <span class="na">name</span><span class="o">=</span><span class="s">&#34;body&#34;</span> <span class="na">accepts</span><span class="o">=</span><span class="s">&#34;blocks&#34;</span> <span class="na">arity</span><span class="o">=</span><span class="s">&#34;0..*&#34;</span><span class="p">&gt;&lt;/</span><span class="nt">slot</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="p">&lt;/</span><span class="nt">div</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="p">&lt;</span><span class="nt">figure</span> <span class="na">class</span><span class="o">=</span><span class="s">&#34;code&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="p">&lt;</span><span class="nt">slot</span> <span class="na">name</span><span class="o">=</span><span class="s">&#34;code&#34;</span> <span class="na">accepts</span><span class="o">=</span><span class="s">&#34;code&#34;</span> <span class="na">arity</span><span class="o">=</span><span class="s">&#34;0..1&#34;</span><span class="p">&gt;&lt;/</span><span class="nt">slot</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="p">&lt;/</span><span class="nt">figure</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;/</span><span class="nt">section</span><span class="p">&gt;</span>
</span></span></code></pre><p>これはPeithoに内蔵されているデフォルトレイアウトそのもの。ポイントは、このHTML自身がスキーマを兼ねているところ。ビルド時にPeithoがこのレイアウトをパースして、「titleはインライン要素がちょうど1個、bodyはブロック要素がいくつでも、codeはコードブロックが0〜1個」という契約を取り出し、Markdownで書いた内容がそれを満たしているかを検査する。</p>
<p>たとえばcodeスロットは<code>arity=&quot;0..1&quot;</code>なので、このレイアウトのスライドにコードブロックを2つ書くとビルドが止まる。</p>
<pre><code>error: slide 2 ('code-slide'), line 7: slot 'code' got 2 item(s), but layout 'title-body-code' allows 0..1
  = help: use a layout with more code capacity or remove one code block
</code></pre>
<p>Rustコンパイラ風に、行番号とhelpつきでエラーになる。<code>('code-slide')</code>は該当スライドに振ったキーで、これは次で説明する。</p>
<hr />
<h2>キー付きper-slide調整</h2>
<p>「このスライドだけコードの背景色を変えたい」みたいな個別調整は、まずMarkdown側で、スライドの先頭にページ設定コメントを書いて安定キーを振る。</p>
<pre class="chroma"><code><span class="line"><span class="cl">&lt;!-- {&#34;key&#34;:&#34;payoff&#34;} --&gt;
</span></span><span class="line"><span class="cl"><span class="gh"># The compiler reviews every caller
</span></span></span></code></pre><p>キーを振ったスライドは、生成されるHTMLの要素に<code>data-slide-key=&quot;payoff&quot;</code>という属性が付く。CSS側では、この属性を目印にして、そのスライドだけにスタイルを当てる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="o">[</span><span class="nt">data-slide-key</span><span class="o">=</span><span class="s2">&#34;payoff&#34;</span><span class="o">]</span> <span class="p">.</span><span class="nc">slot-code</span> <span class="p">{</span> <span class="k">background</span><span class="p">:</span> <span class="mh">#0d2822</span><span class="p">;</span> <span class="p">}</span>
</span></span></code></pre><p>このCSSも<code>peitho build</code>の検査対象になっていて、デッキに存在しないキーを指定しているとビルド時にエラーになる。スライドを消したり書き換えたりしてキーが無くなったのに、それを指定したCSSが参照切れのまま静かに残り続ける、ということもない。</p>
<hr />
<h2>複数レイアウトと型駆動ディスパッチ</h2>
<p>レイアウトは複数持てて、どのスライドにどのレイアウトが適用されるかは次の順で決まる。</p>
<ol>
<li>ページ設定コメント<code>&lt;!-- {&quot;layout&quot;:&quot;cover&quot;} --&gt;</code>による明示指定</li>
<li>レイアウトが1枚だけなら無条件にそれ</li>
<li>複数枚なら、スライドの内容の形（タイトルだけ・本文あり・コードあり等）にスロット契約が一致するレイアウトへ自動で振り分ける</li>
</ol>
<p>3の型駆動ディスパッチは、ちょうど1枚に一致することが条件で、複数一致（曖昧）も0枚一致も黙って解決せずビルドエラーにして明示指定を促す。ここもk1LoW/deckのページ設定の仕組みを参考にしつつ、「黙って何かが起きる」経路を残さないようにした。</p>
<hr />
<h2>peitho present</h2>
<p>ビルドだけでなく発表もカバーしていて、<code>peitho present deck.md</code>でローカルサーバを立ててブラウザを起動する。外部ディスプレイがあればそちらにスライドをフルスクリーン、手元のディスプレイに発表者ビュー（現在/次スライド・ノート・タイマー）を自動配置する。Escで全ウィンドウとサーバがまとめて終了する。Keynote的な発表体験をブラウザでやる、というもの。</p>
<p>実行するとこんな感じ。1画面のMacに仮想ディスプレイを足して、それを外部ディスプレイに見立てた環境での録画。スライドが仮想ディスプレイ側にフルスクリーンで出て、手元に発表者ビューが開き、Escでまとめて終了する。</p>
<p><video src="/images/2026/07/peitho-present.mp4" controls muted playsinline style="max-width:100%"></video></p>
<hr />
<h2>サンプル</h2>
<p>Peithoのリポジトリの<code>examples/</code>に、内容もレイアウト構造もテーマも異なるサンプルを置いていて、<a href="https://peitho.gosu.ke/">デモサイト</a>でそのまま見られる。</p>
<p><em>Keynote</em> — クリーム地+セリフ体+中央寄せ。cover（タイトルのみ）とstatement（タイトル+本文）の2レイアウト構成で、型駆動ディスパッチが内容の形からレイアウトを振り分ける。</p>
<p><img src="/images/2026/07/peitho-keynote.png" alt="Keynoteサンプル" /></p>
<p><em>Code Walkthrough</em> — ターミナル風2カラムのコード解説。codeスロットが<code>arity=&quot;1&quot;</code>なので、毎スライドにコードが必須。シンタックスハイライトは<a href="https://github.com/trishume/syntect">syntect</a>（ArgusやIrisと同じ）でビルド時にかけていて、実行時JSは無く、色はテーマCSSで定義する。</p>
<p><img src="/images/2026/07/peitho-code-walkthrough.png" alt="Code Walkthroughサンプル" /></p>
<p><em>Lightning Talk</em> — ダーク+大型タイポのLT向けデザイン。レイアウトにcodeスロットが無いので、コードを書くとビルドエラーになる。</p>
<p><img src="/images/2026/07/peitho-lightning-talk.png" alt="Lightning Talkサンプル" /></p>
<p>この「コードを書くとビルドエラーになるレイアウト」というのが、レイアウトがスキーマを兼ねることの面白いところだと思っている。レイアウトが「このスライドに何を書けるか」を決めて、それをビルドが守らせる。</p>
<hr />
<h2>実装</h2>
<p>ビルドコアはRust。デッキを表す型が、パース済み（<code>Parsed</code>）→スロット割り当て済み（<code>Mapped</code>）→検査済み（<code>Checked</code>）→レンダリング済み（<code>Rendered</code>）と、パイプラインの段階ごとに分かれていて、レンダラは検査済みの型しか受け取れない。つまり、検査をすっ飛ばしてレンダリングするようなコードは、実行時エラーになるのではなく、そもそもコンパイルが通らない。いわゆるtypestateパターン。Rustのdoctestには「このコードはコンパイルに失敗するはず」を検証できる<code>compile_fail</code>という仕組みがあるので、検査前のデッキをレンダラに渡すコードが実際にコンパイルエラーになること自体もテストで固定している。</p>
<p>一方、<code>peitho present</code>の発表時にブラウザ側で動く部分（スライド表示、キー操作、タイマー、画面間の同期）はTypeScriptで書いている。RustとTypeScriptの両方で、ビルドが出力するmanifest等の同じデータ構造を扱うことになるので、型定義はRust側にだけ置いて、TypeScript側の型は<a href="https://github.com/Aleph-Alpha/ts-rs">ts-rs</a>で自動生成している。手書きの型が2つあるとズレていくので、生成した型がRust側と一致しているかをCIで検査する。</p>
<hr />
<h2>名前の由来</h2>
<p>Peitho（ペイトー）は、力ではなく言葉で人の心を動かすことを司るギリシャ神話の女神から。プレゼンテーションはまさに言葉で人の心を動かす行為なので、ぴったりだと思ってつけた。Carina、Pyxis、Argus、Irisと続いてきた神話シリーズの並びでもある。</p>
<hr />
<h2>今後</h2>
<ul>
<li>実際の発表スライドを公開するサイト（スライドのMarkdownをリポジトリにpushしたら、CIがビルド=契約検査して、そのままサイトで公開されるところまで自動化したい）</li>
<li>スピーカーノートのMarkdown記法（発表者ビューの表示側はできていて、記法だけ未決）</li>
<li>2カラムなど規約で曖昧になるレイアウト向けの、fenced divによる明示スロット記法</li>
<li>プレゼンツールとしての機能充実</li>
</ul>
<p>などなど。</p>
<p>ちなみに、プレゼンツールつくってるけど、次のプレゼンの予定は今のところ特にない。</p>
<hr />
<p>参考:</p>
<ul>
<li><a href="https://github.com/mizzy/peitho">mizzy/peitho</a></li>
<li><a href="https://peitho.gosu.ke/">デモサイト peitho.gosu.ke</a></li>
<li><a href="https://github.com/k1LoW/deck">k1LoW/deck</a></li>
<li><a href="https://www.docswell.com/s/Songmu/K137GV-about-deck">k1LoW/deckの紹介とチュートリアル</a></li>
</ul>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Pyxisの開発: RMSNorm]]></title>
        <link href="https://mizzy.org/blog/2026/07/01/2/"/>
        <updated>2026-07-01T21:10:07+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/01/2/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/07/01/1/">前回の記事</a>でembeddingテーブルルックアップを実装して、トークンIDからベクトルを取り出せるようになった。今回はそのベクトルに対して最初に適用される正規化処理、RMSNormを実装した。対応するPRは<a href="https://github.com/mizzy/pyxis/pull/18">#18</a>。</p>
<hr />
<h2>RMSNormとは</h2>
<p>RMSNormは2019年にBiao ZhangとRico Sennrichが<a href="https://arxiv.org/abs/1910.07467">提案</a>した正規化手法。LLaMA以降のLLM（Mistral、Gemma、Qwen、DeepSeekなど）で広く採用されている。</p>
<p>正規化とは、ベクトルの各要素の値の大きさを一定の基準に揃える処理。学習時に値が大きくなりすぎたり小さくなりすぎたりするのを防ぐために導入された手法で、モデルは正規化が入った状態で訓練されている。推論時にも同じ正規化を適用しないと正しい結果が出ないので、Pyxisでも実装する必要がある。</p>
<p>RMSNormは2つのステップからなる。まず各要素をRMS（二乗平均平方根）で割る。RMSは各要素を二乗して平均を取り、その平方根を取った値。たとえば<code>[1.0, 3.0, -2.0, 4.0]</code>のRMSは、二乗して<code>[1.0, 9.0, 4.0, 16.0]</code>、平均が<code>(1.0 + 9.0 + 4.0 + 16.0) / 4 = 7.5</code>、平方根を取って<code>sqrt(7.5) ≈ 2.74</code>。各要素をこのRMSで割ると<code>[0.37, 1.10, -0.73, 1.46]</code>になる。要素間の比率は保ったまま、正規化後のベクトルのRMSが1.0になる。</p>
<p>次に、学習済みのweightを要素ごとに掛ける。このweightはsafetensorsファイルにテンソルとして保存されているパラメータで、各要素の重要度に応じた調整を行う。</p>
<p>もともとよく使われていたのはLayerNormで、計算式はこうなる。</p>
<pre><code>y[i] = (x[i] - mean(x)) / sqrt(var(x) + ε) * weight[i] + bias[i]
</code></pre>
<p>まず平均を引いて（re-centering）、標準偏差で割って（re-scaling）正規化し、そこに学習済みのweightとbiasを適用する。biasはweightと同様の学習済みパラメータで、足し算でオフセットを加える。RMSNormはLayerNormを簡略化したもので、平均を引く処理とbiasを省いて、二乗平均平方根（Root Mean Square）で割るだけにしている。Zhang &amp; Sennrichの論文では、平均を引く処理はなくても性能に影響しないことが実験で示されていて、計算量を7〜64%削減できるとしている。</p>
<hr />
<h2>計算</h2>
<p>RMSNormの計算式はこうなる。</p>
<pre><code>y[i] = x[i] / sqrt(mean(x²) + ε) * weight[i]
</code></pre>
<ol>
<li>入力ベクトル<code>x</code>の全要素を二乗して平均を取る（= 二乗平均）</li>
<li>それにε（小さい定数、ゼロ除算防止）を足してから平方根を取る（= RMS）</li>
<li>各要素をRMSで割る</li>
<li>学習済みの重み<code>weight</code>を要素ごとに掛ける</li>
</ol>
<p>εはゼロ除算を防ぐための定数で、Qwen3では1e-6が使われている。前のセクションの具体例ではεを省略したが、実際の計算ではRMSに加算される。</p>
<hr />
<h2>実装</h2>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">struct</span> <span class="nc">RmsNorm</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">weight</span>: <span class="nb">Vec</span><span class="o">&lt;</span><span class="kt">f32</span><span class="o">&gt;</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">eps</span>: <span class="kt">f32</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p><code>weight</code>が学習済みパラメータで、<code>eps</code>がε。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">forward</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">x</span>: <span class="kp">&amp;</span><span class="nc">mut</span><span class="w"> </span><span class="p">[</span><span class="kt">f32</span><span class="p">])</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="fm">assert_eq!</span><span class="p">(</span><span class="n">x</span><span class="p">.</span><span class="n">len</span><span class="p">(),</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">weight</span><span class="p">.</span><span class="n">len</span><span class="p">());</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">mean_square</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">x</span><span class="p">.</span><span class="n">iter</span><span class="p">().</span><span class="n">map</span><span class="p">(</span><span class="o">|</span><span class="n">value</span><span class="o">|</span><span class="w"> </span><span class="n">value</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="n">value</span><span class="p">).</span><span class="n">sum</span>::<span class="o">&lt;</span><span class="kt">f32</span><span class="o">&gt;</span><span class="p">()</span><span class="w"> </span><span class="o">/</span><span class="w"> </span><span class="n">x</span><span class="p">.</span><span class="n">len</span><span class="p">()</span><span class="w"> </span><span class="k">as</span><span class="w"> </span><span class="kt">f32</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">factor</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="mf">1.0</span><span class="w"> </span><span class="o">/</span><span class="w"> </span><span class="p">(</span><span class="n">mean_square</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">eps</span><span class="p">).</span><span class="n">sqrt</span><span class="p">();</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">for</span><span class="w"> </span><span class="p">(</span><span class="n">value</span><span class="p">,</span><span class="w"> </span><span class="n">weight</span><span class="p">)</span><span class="w"> </span><span class="k">in</span><span class="w"> </span><span class="n">x</span><span class="p">.</span><span class="n">iter_mut</span><span class="p">().</span><span class="n">zip</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">.</span><span class="n">weight</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="o">*</span><span class="n">value</span><span class="w"> </span><span class="o">*=</span><span class="w"> </span><span class="n">factor</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="n">weight</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><hr />
<p>Qwen3では、トランスフォーマーの各層の中でRMSNormが2回適用される。embeddingで取り出したベクトルが0番目の層に入ると、まずRMSNormで正規化され、アテンション（トークン間の関連度を計算する処理、次回の記事で詳しく書く）に渡される。アテンションの出力は再びRMSNormで正規化され、FFN（フィードフォワードネットワーク、ベクトルを非線形変換する処理）に渡される。それぞれのRMSNormは別の学習済みweightを持っていて、Qwen3ではsafetensorsファイルに以下の名前で格納されている。</p>
<ul>
<li>アテンションの前: <code>model.layers.0.input_layernorm.weight</code></li>
<li>FFNの前: <code>model.layers.0.post_attention_layernorm.weight</code></li>
</ul>
<p>名前に「layernorm」とあるが、実際の処理はRMSNorm。これはLLaMAのアーキテクチャに由来する命名で、Qwen3もこれを踏襲している。名前はモデルの構造をそのまま反映していて、<code>model.layers.0</code>が0番目のトランスフォーマー層、<code>input_layernorm</code>/<code>post_attention_layernorm</code>がその層の中の各RMSNorm、<code>weight</code>がそのパラメータ、を意味する。トランスフォーマー層は複数あり（Qwen3-1.7Bでは28層）、各層がそれぞれ別のRMSNorm weightを持っている。</p>
<p>次回はアテンションの実装について書く。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Pyxisの開発: Embeddingテーブルルックアップ]]></title>
        <link href="https://mizzy.org/blog/2026/07/01/1/"/>
        <updated>2026-07-01T09:05:29+09:00</updated>
        <id>https://mizzy.org/blog/2026/07/01/1/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/06/30/2/">前回の記事</a>でsafetensorsからテンソルデータをf32として読み込めるようになった。今回はそのデータを使った最初の計算として、embeddingテーブルルックアップを実装した。対応するPRは<a href="https://github.com/mizzy/pyxis/pull/17">#17</a>。</p>
<p>embeddingテーブルルックアップとは、入力のトークンIDを浮動小数点のベクトルに変換する処理のこと。LLMの推論パイプラインの入口にあたる。</p>
<hr />
<h2>トークナイザーとBPE</h2>
<p>LLMはテキストをそのまま処理するのではなく、まずトークナイザーでトークンIDの列に変換する。トークナイザーはテキストをトークンに分割して、それぞれに整数のIDを振る。たとえばQwen3のトークナイザーだと「tokenization」は<code>['token', 'ization']</code> → <code>[5839, 2022]</code>のように分割される。「unbelievable」なら<code>['un', 'belie', 'vable']</code> → <code>[359, 31798, 23760]</code>と3つに分かれる。</p>
<p>分割のアルゴリズムとして広く使われているのがBPE（Byte Pair Encoding）。元々は1994年にPhilip Gageがデータ圧縮のアルゴリズムとして<a href="https://dl.acm.org/doi/10.5555/177910.177914">発表</a>したもので、2015年にRico Sennrichらが<a href="https://arxiv.org/abs/1508.07909">ニューラル機械翻訳のためのサブワード分割に応用</a>し、その後GPT-2をはじめとするLLMのトークナイザーにも採用された。最初は1文字ずつ（<code>a</code>, <code>b</code>, <code>c</code>, ...）の辞書から始めて、訓練データ中で隣り合う頻出ペアを繰り返しマージして辞書を大きくしていく。たとえば<code>t</code>と<code>h</code>のペアが最も多ければ<code>th</code>を辞書に追加、次に<code>th</code>と<code>e</code>が多ければ<code>the</code>を追加、という具合に繰り返す。よく出る文字列ほど早くマージされて1トークンになり、珍しい単語はマージが進まず細かいトークンに分割されたまま残る。マージの回数が辞書のサイズを決める。各トークンに0から始まる整数IDが割り当てられる。この辞書に含まれるトークンの総数をvocab_sizeと呼ぶ。</p>
<p>実際のLLMでは、BPEで一から辞書を構築するのではなく、既存の辞書をベースに拡張することもある。<a href="https://arxiv.org/abs/2309.16609">Qwen Technical Report</a>によると、Qwen3ではOpenAIのtiktoken（cl100k_base、約10万トークン）をベースに、中国語の常用漢字・単語や他の言語のトークンを追加し、数字を1桁ずつに分割するようにした結果、トークン数は151643になった。config上のvocab_sizeは151936で、これは151643を128の倍数に切り上げた値。GPUのTensor Coresは行列の次元が64や128の倍数のとき効率が良いので、embeddingテーブルのサイズをそれに合わせている。</p>
<hr />
<h2>Embeddingとは</h2>
<p>トークンIDは単なる整数のインデックスで、数値の大小に意味がない（ID 5839がID 5838より何か「大きい」わけではない）。LLMの推論は浮動小数点の行列演算なので、トークンIDをそのまま入力に使うことはできない。各トークンIDを固定長の浮動小数点ベクトルに変換する必要があって、この変換を行うのがembeddingテーブル。</p>
<p>embeddingテーブルは<code>vocab_size × hidden_dim</code>の二次元行列で、各行がひとつのトークンに対応するベクトルになっている。hidden_dimはモデル内部で使われるベクトルの次元数で、Qwen3-1.7Bでは2048。vocab_sizeが151936、hidden_dimが2048なら、151936個のトークンそれぞれに2048次元のベクトルが割り当てられている。トークンID <code>n</code>の入力に対しては、テーブルのn行目のベクトルをそのまま取り出す。これがembeddingルックアップ。</p>
<hr />
<h2>実装</h2>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">struct</span> <span class="nc">Embedding</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">weights</span>: <span class="nb">Vec</span><span class="o">&lt;</span><span class="kt">f32</span><span class="o">&gt;</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">vocab_size</span>: <span class="kt">usize</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">hidden_dim</span>: <span class="kt">usize</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p><code>weights</code>がembeddingテーブルの本体で、<code>vocab_size × hidden_dim</code>個のf32値が一次元のフラット配列として入っている。前回実装した<code>SafeTensors::tensor_f32()</code>で読み込んだデータがそのまま入る。</p>
<p>ルックアップはこれだけ。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">lookup</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">token_id</span>: <span class="kt">usize</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="kp">&amp;</span><span class="p">[</span><span class="kt">f32</span><span class="p">]</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="fm">assert!</span><span class="p">(</span><span class="n">token_id</span><span class="w"> </span><span class="o">&lt;</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">vocab_size</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">start</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">token_id</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">hidden_dim</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">end</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">start</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">hidden_dim</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="o">&amp;</span><span class="bp">self</span><span class="p">.</span><span class="n">weights</span><span class="p">[</span><span class="n">start</span><span class="o">..</span><span class="n">end</span><span class="p">]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p><code>token_id * hidden_dim</code>で行の先頭位置を計算して、そこから<code>hidden_dim</code>個分のスライスを返す。フラット配列を二次元テーブルとして扱っているだけなので、コピーは発生しない。</p>
<p>たとえばvocab_size=3、hidden_dim=4のテーブルなら、weightsは12個のf32値が並んでいて:</p>
<p><img src="/images/2026/07/embedding-weights.png" alt="embeddingテーブルのweights配列" /></p>
<p><code>lookup(1)</code>は<code>weights[4..8]</code>、つまり<code>[4.0, 5.0, 6.0, 7.0]</code>を返す。</p>
<hr />
<p>embeddingルックアップ自体は単なる配列のインデックスアクセスで、計算らしい計算はない。ただ、これがトランスフォーマーのパイプライン全体の入口になる。次回はこのベクトルに対して最初に適用されるRMSNormの実装について書く。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Pyxisの開発: テンソルデータの読み込み]]></title>
        <link href="https://mizzy.org/blog/2026/06/30/2/"/>
        <updated>2026-06-30T22:01:50+09:00</updated>
        <id>https://mizzy.org/blog/2026/06/30/2/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/06/30/1/">前回の記事</a>でsafetensorsのヘッダーをパースしてテンソルのメタデータ（名前、dtype、shape、オフセット）を取り出すところまで書いた。今回はその次のステップで、実際のテンソルデータをバイト列からf32の数値配列として読み込む部分を実装した。対応するPRは<a href="https://github.com/mizzy/pyxis/pull/16">#16</a>。</p>
<hr />
<h2>SafeTensors構造体の導入</h2>
<p>前回は<code>parse_header</code>関数でヘッダーを読んでメタデータを返すだけだったので、ファイルをすぐ閉じてよかった。今回はテンソルデータの本体も読みに行くので、ファイルへのアクセスを保持し続ける必要がある。そこで<code>SafeTensors</code>構造体を導入して、ファイルアクセスとメタデータをまとめて持つようにした。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">struct</span> <span class="nc">SafeTensors</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">mmap</span>: <span class="nc">memmap2</span>::<span class="n">Mmap</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">data_offset</span>: <span class="kt">usize</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">tensors</span>: <span class="nc">HashMap</span><span class="o">&lt;</span><span class="nb">String</span><span class="p">,</span><span class="w"> </span><span class="n">TensorInfo</span><span class="o">&gt;</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p><code>mmap</code>がファイルの中身へのアクセス。<code>data_offset</code>はヘッダーの終わり（= テンソルデータの先頭）の位置で、8バイトのサイズフィールド + ヘッダーのJSONバイト数。<code>tensors</code>が前回の<code>parse_header</code>で取得していたテンソルのメタデータ（dtype、shape、オフセット）。</p>
<p>前回の<code>parse_header</code>のロジックは<code>SafeTensors::load()</code>メソッドに移動していて、ヘッダーをパースしてメタデータを取り出す処理自体は同じ。</p>
<p>ファイルアクセスには<code>memmap2</code>クレートを使ってメモリマップしている。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="kd">let</span><span class="w"> </span><span class="n">file</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">File</span>::<span class="n">open</span><span class="p">(</span><span class="n">path</span><span class="p">)</span><span class="o">?</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="kd">let</span><span class="w"> </span><span class="n">mmap</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="k">unsafe</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="n">memmap2</span>::<span class="n">Mmap</span>::<span class="n">map</span><span class="p">(</span><span class="o">&amp;</span><span class="n">file</span><span class="p">)</span><span class="o">?</span><span class="w"> </span><span class="p">};</span><span class="w">
</span></span></span></code></pre><p>メモリマップはファイルの中身をプロセスの仮想アドレス空間にマッピングする仕組みで、OSのページキャッシュを介してアクセスされる。ファイル全体を一気にメモリに読まなくても、アクセスしたページだけが物理メモリに載る。モデルファイルは数GBになるので、全部メモリに読み込むのは現実的ではないが、メモリマップなら必要な部分だけが透過的に読み込まれる。</p>
<hr />
<h2>Dtype enum</h2>
<p>前回は<code>dtype</code>を<code>String</code>で持っていたけど、テンソルデータを読むにはdtypeごとにバイト列の解釈を変える必要がある。文字列のまま<code>match</code>するのは型安全でないので、<code>Dtype</code> enumに変えた。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="cp">#[derive(Debug, Clone, Copy, PartialEq, Eq, Deserialize)]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="cp">#[serde(rename_all = </span><span class="s">&#34;UPPERCASE&#34;</span><span class="cp">)]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">enum</span> <span class="nc">Dtype</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">F32</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">F16</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="no">BF16</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">impl</span><span class="w"> </span><span class="n">Dtype</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">element_size</span><span class="p">(</span><span class="bp">self</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="kt">usize</span> <span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="k">match</span><span class="w"> </span><span class="bp">self</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="n">Dtype</span>::<span class="n">F32</span><span class="w"> </span><span class="o">=&gt;</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="n">Dtype</span>::<span class="n">F16</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">Dtype</span>::<span class="no">BF16</span><span class="w"> </span><span class="o">=&gt;</span><span class="w"> </span><span class="mi">2</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p><code>element_size()</code>は1要素あたりのバイト数を返す。F32は4バイト、F16とBF16は2バイト。テンソルデータを読み込むときに、バイト列をこのサイズごとに区切って数値に変換する。バイト長がこのサイズの倍数になっているかのバリデーションにも使う。</p>
<p>safetensorsのフォーマット仕様ではF32/F16/BF16以外にもI64、U8、BOOLなど<a href="https://github.com/huggingface/safetensors/blob/main/safetensors/src/tensor.rs">22種類のdtype</a>が定義されている。Pyxisではさしあたり必要なF32/F16/BF16だけをサポートして、それ以外のdtypeのテンソルはスキップするようにした。他のdtypeは必要になったら対応する。</p>
<hr />
<h2>テンソルデータの読み込み</h2>
<p><code>tensor_f32()</code>メソッドがこのPRの本体。テンソル名を指定すると、そのテンソルのバイト列をdtypeに応じてf32に変換して返す。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">tensor_f32</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">name</span>: <span class="kp">&amp;</span><span class="kt">str</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nc">io</span>::<span class="nb">Result</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="kt">f32</span><span class="o">&gt;&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">info</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">tensors</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="n">name</span><span class="p">).</span><span class="n">ok_or_else</span><span class="p">(</span><span class="o">||</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">io</span>::<span class="n">Error</span>::<span class="n">new</span><span class="p">(</span><span class="n">io</span>::<span class="n">ErrorKind</span>::<span class="n">NotFound</span><span class="p">,</span><span class="w"> </span><span class="fm">format!</span><span class="p">(</span><span class="s">&#34;tensor not found: </span><span class="si">{name}</span><span class="s">&#34;</span><span class="p">))</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">})</span><span class="o">?</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="p">[</span><span class="n">start</span><span class="p">,</span><span class="w"> </span><span class="n">end</span><span class="p">]</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">info</span><span class="p">.</span><span class="n">data_offsets</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">start</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">data_offset</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">start</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">end</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">data_offset</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">end</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">bytes</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">&amp;</span><span class="bp">self</span><span class="p">.</span><span class="n">mmap</span><span class="p">[</span><span class="n">start</span><span class="o">..</span><span class="n">end</span><span class="p">];</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">values</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="k">match</span><span class="w"> </span><span class="n">info</span><span class="p">.</span><span class="n">dtype</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">Dtype</span>::<span class="n">F32</span><span class="w"> </span><span class="o">=&gt;</span><span class="w"> </span><span class="n">bytes</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">chunks_exact</span><span class="p">(</span><span class="mi">4</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">map</span><span class="p">(</span><span class="o">|</span><span class="n">c</span><span class="o">|</span><span class="w"> </span><span class="kt">f32</span>::<span class="n">from_le_bytes</span><span class="p">([</span><span class="n">c</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span><span class="w"> </span><span class="n">c</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span><span class="w"> </span><span class="n">c</span><span class="p">[</span><span class="mi">2</span><span class="p">],</span><span class="w"> </span><span class="n">c</span><span class="p">[</span><span class="mi">3</span><span class="p">]]))</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">collect</span><span class="p">(),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">Dtype</span>::<span class="n">F16</span><span class="w"> </span><span class="o">=&gt;</span><span class="w"> </span><span class="n">bytes</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">chunks_exact</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">map</span><span class="p">(</span><span class="o">|</span><span class="n">c</span><span class="o">|</span><span class="w"> </span><span class="n">half</span>::<span class="n">f16</span>::<span class="n">from_le_bytes</span><span class="p">([</span><span class="n">c</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span><span class="w"> </span><span class="n">c</span><span class="p">[</span><span class="mi">1</span><span class="p">]]).</span><span class="n">to_f32</span><span class="p">())</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">collect</span><span class="p">(),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">Dtype</span>::<span class="no">BF16</span><span class="w"> </span><span class="o">=&gt;</span><span class="w"> </span><span class="n">bytes</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">chunks_exact</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">map</span><span class="p">(</span><span class="o">|</span><span class="n">c</span><span class="o">|</span><span class="w"> </span><span class="n">half</span>::<span class="n">bf16</span>::<span class="n">from_le_bytes</span><span class="p">([</span><span class="n">c</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span><span class="w"> </span><span class="n">c</span><span class="p">[</span><span class="mi">1</span><span class="p">]]).</span><span class="n">to_f32</span><span class="p">())</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">.</span><span class="n">collect</span><span class="p">(),</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">};</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nb">Ok</span><span class="p">(</span><span class="n">values</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>やっていることはシンプルで、ヘッダーに記録されている<code>data_offsets</code>にデータ領域の開始位置を足して、mmap上の該当バイト列をスライスする。あとはdtypeに応じてバイトの解釈を変えるだけ。</p>
<ul>
<li><strong>F32</strong>: <code>chunks_exact(4)</code>で4バイトずつに切り、<code>f32::from_le_bytes</code>でリトルエンディアンのf32に変換する</li>
<li><strong>F16</strong>: <code>chunks_exact(2)</code>で2バイトずつに切り、<code>half::f16::from_le_bytes</code>でF16に変換してから<code>.to_f32()</code>でf32にする</li>
<li><strong>BF16</strong>: 同様に2バイトずつに切り、<code>half::bf16::from_le_bytes</code>でBF16に変換してから<code>.to_f32()</code>でf32にする</li>
</ul>
<p><code>chunks_exact</code>がバイト列を1要素あたりのバイト数（前述の<code>element_size()</code>、F32なら4、F16/BF16なら2）ごとに分割し、<code>from_le_bytes</code>がそのバイト列をリトルエンディアンの数値として解釈する、という二段階の処理。F16とBF16の変換には<code>half</code>クレートを使っている。</p>
<p>この時点ではすべてのテンソルをf32に変換して読み込んでいる。Rustの標準ライブラリにはBF16の型がなく、F16の型（<code>f16</code>）もnightly限定の実験的APIでstableでは使えない。<code>half</code>クレートがBF16/F16の型と算術演算を提供しているが、<a href="https://github.com/starkat99/half-rs/blob/main/src/bfloat.rs">演算の実装</a>はf32に変換してから計算してBF16/F16に戻す、というもの。最初からf32で持てばRustのプリミティブ型の演算がそのまま使えて、変換の往復が不要になる。メモリ効率の面ではBF16のまま持って計算時にf32に変換する方が有利だけど、まずは動くものを作るのが優先なので、ここはシンプルな方を選んだ（後のPRでBF16のまま持つ最適化を入れている）。</p>
<hr />
<p>これで、safetensorsファイルからテンソルの数値データをf32として取り出せるようになった。次回はこのデータを使って、トークンIDからベクトルを引くembeddingテーブルルックアップの実装について書く。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[Pyxisの開発: safetensorsパーサー実装]]></title>
        <link href="https://mizzy.org/blog/2026/06/30/1/"/>
        <updated>2026-06-30T14:40:07+09:00</updated>
        <id>https://mizzy.org/blog/2026/06/30/1/</id>
        <content type="html"><![CDATA[
<p><a href="/blog/2026/06/23/1/">前回の記事</a>でPyxisを軽く紹介した。LLM推論エンジンを自前実装するプロジェクトで、今回はその最初のステップであるsafetensorsパーサーの実装について書く。</p>
<hr />
<h2>safetensorsとは</h2>
<p>LLMの重みファイルのフォーマットはいくつかあるが、HuggingFaceが策定した<a href="https://github.com/huggingface/safetensors">safetensors</a>が量子化されていないモデルの配布フォーマットとして現在のデファクトになっている。名前の通り、従来のPickleベースのフォーマット（PyTorchの<code>.pt</code>ファイル）に比べて安全で、任意コードの実行ができない。</p>
<p>バイナリフォーマットの構造はシンプルで、こうなっている。</p>
<pre><code>[ 8 bytes : header size (u64 LE) ]
[ N bytes : JSON header          ]
[ tensor data                    ]
</code></pre>
<p>最初の8バイトにヘッダーのサイズがリトルエンディアンのu64で入っていて、その後ろにそのサイズ分のJSONが続く。JSONの中に各テンソルのメタデータ（名前、dtype、shape、データのオフセット）が入っている。テンソルの実データはJSONの後ろにそのまま並んでいる。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;__metadata__&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;format&#34;</span><span class="p">:</span> <span class="s2">&#34;pt&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;model.embed_tokens.weight&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;dtype&#34;</span><span class="p">:</span> <span class="s2">&#34;BF16&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;shape&#34;</span><span class="p">:</span> <span class="p">[</span><span class="mi">151936</span><span class="p">,</span> <span class="mi">2048</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;data_offsets&#34;</span><span class="p">:</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">622329856</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre><p><code>dtype</code>はテンソルの要素の数値型。主なものはこんな感じ。</p>
<table>
<thead>
<tr>
<th>dtype</th>
<th>ビット数</th>
<th>説明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>F32</code></td>
<td>32bit</td>
<td>単精度浮動小数点。指数部8bit、仮数部23bit。精度が高いが重い</td>
</tr>
<tr>
<td><code>F16</code></td>
<td>16bit</td>
<td>半精度浮動小数点。指数部5bit、仮数部10bit</td>
</tr>
<tr>
<td><code>BF16</code></td>
<td>16bit</td>
<td>Brain Float 16。Google Brainが考案（現在はGoogle DeepMindに統合）。指数部8bit、仮数部7bit。F32と指数部の幅が同じなので表現できる数値のスケールがF32と同じ（最大値約3.38×10^38、最小値約1.17×10^-38）。精度はF32より低いがスケールを保ったままメモリを半分に抑えられるため、LLMの学習・推論では広く使われる</td>
</tr>
<tr>
<td><code>I8</code></td>
<td>8bit</td>
<td>8ビット整数。量子化済みモデルで使われる</td>
</tr>
</tbody>
</table>
<p>BF16はF32の指数部をそのままに仮数部を23bitから7bitに切り詰めた、いわばF32の軽量版。メモリ使用量はF32の半分になりながら、数値のスケール（桁の範囲）はF32と同じに保てる。LLMの学習では勾配の値が幅広い範囲に散らばるため、スケールを保ったBF16はF32からの置き換えとして相性がよく、最近のモデルはBF16で配布されているものが多い。F16も同じ16bitだが指数部が5bitと狭く、スケールはBF16より大幅に小さい。</p>
<p><code>shape</code>はテンソルの次元数と各次元のサイズで、たとえば<code>[151936, 2048]</code>なら151936行×2048列の行列を表す。</p>
<hr />
<h2>実装</h2>
<p>パーサー本体はこんな感じ。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">fn</span> <span class="nf">parse_header</span><span class="p">(</span><span class="n">path</span>: <span class="kp">&amp;</span><span class="nc">Path</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nc">io</span>::<span class="nb">Result</span><span class="o">&lt;</span><span class="n">HashMap</span><span class="o">&lt;</span><span class="nb">String</span><span class="p">,</span><span class="w"> </span><span class="n">TensorInfo</span><span class="o">&gt;&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">file</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">File</span>::<span class="n">open</span><span class="p">(</span><span class="n">path</span><span class="p">)</span><span class="o">?</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">size_buf</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="mi">0</span><span class="k">u8</span><span class="p">;</span><span class="w"> </span><span class="mi">8</span><span class="p">];</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">file</span><span class="p">.</span><span class="n">read_exact</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span><span class="w"> </span><span class="n">size_buf</span><span class="p">)</span><span class="o">?</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">header_size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kt">u64</span>::<span class="n">from_le_bytes</span><span class="p">(</span><span class="n">size_buf</span><span class="p">)</span><span class="w"> </span><span class="k">as</span><span class="w"> </span><span class="kt">usize</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">header_buf</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="fm">vec!</span><span class="p">[</span><span class="mi">0</span><span class="k">u8</span><span class="p">;</span><span class="w"> </span><span class="n">header_size</span><span class="p">];</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">file</span><span class="p">.</span><span class="n">read_exact</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span><span class="w"> </span><span class="n">header_buf</span><span class="p">)</span><span class="o">?</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">header_json</span>: <span class="nc">HashMap</span><span class="o">&lt;</span><span class="nb">String</span><span class="p">,</span><span class="w"> </span><span class="n">serde_json</span>::<span class="n">Value</span><span class="o">&gt;</span><span class="w"> </span><span class="o">=</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">serde_json</span>::<span class="n">from_slice</span><span class="p">(</span><span class="o">&amp;</span><span class="n">header_buf</span><span class="p">)</span><span class="o">?</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">tensors</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">HashMap</span>::<span class="n">new</span><span class="p">();</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">for</span><span class="w"> </span><span class="p">(</span><span class="n">key</span><span class="p">,</span><span class="w"> </span><span class="n">value</span><span class="p">)</span><span class="w"> </span><span class="k">in</span><span class="w"> </span><span class="n">header_json</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="k">if</span><span class="w"> </span><span class="n">key</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="s">&#34;__metadata__&#34;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="k">continue</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="kd">let</span><span class="w"> </span><span class="n">info</span>: <span class="nc">TensorInfo</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">serde_json</span>::<span class="n">from_value</span><span class="p">(</span><span class="n">value</span><span class="p">)</span><span class="o">?</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">tensors</span><span class="p">.</span><span class="n">insert</span><span class="p">(</span><span class="n">key</span><span class="p">,</span><span class="w"> </span><span class="n">info</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nb">Ok</span><span class="p">(</span><span class="n">tensors</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>ファイルを開いて最初の8バイトを読んでヘッダーサイズを得る、ヘッダーサイズ分のバイトを読んでJSONとしてパースする、<code>__metadata__</code>キーをスキップして各テンソルのメタデータを取り出す、という流れ。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="cp">#[derive(Debug, Clone, Deserialize)]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">pub</span><span class="w"> </span><span class="k">struct</span> <span class="nc">TensorInfo</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">pub</span><span class="w"> </span><span class="n">dtype</span>: <span class="nb">String</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">pub</span><span class="w"> </span><span class="n">shape</span>: <span class="nb">Vec</span><span class="o">&lt;</span><span class="kt">usize</span><span class="o">&gt;</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">pub</span><span class="w"> </span><span class="n">data_offsets</span>: <span class="p">[</span><span class="kt">usize</span><span class="p">;</span><span class="w"> </span><span class="mi">2</span><span class="p">],</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span></code></pre><p>実際にQwen3-1.7BのモデルファイルにこのCLIを通してみると、こんな出力が得られる。</p>
<pre><code>Tensor                                Dtype           Shape               Offsets
---------------------------------------------------------------------------------
lm_head.weight                         BF16  [151936, 2048]          0..622329856
model.embed_tokens.weight              BF16  [151936, 2048]          0..622329856
model.layers.0.input_layernorm.weight  BF16          [2048]  622329856..622333952
...
Total: 311 tensors
</code></pre>
<p>テンソルの名前、dtype、shape、データのオフセット範囲が一覧で確認できる。311個のテンソルが含まれていることがわかる。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[最近つくっているOSS 3つ - Pyxis / Argus / Iris]]></title>
        <link href="https://mizzy.org/blog/2026/06/23/1/"/>
        <updated>2026-06-23T21:03:56+09:00</updated>
        <id>https://mizzy.org/blog/2026/06/23/1/</id>
        <content type="html"><![CDATA[
<p>最近つくっているOSSをまとめて紹介しておく。</p>
<hr />
<h2>Pyxis</h2>
<p><a href="https://github.com/mizzy/pyxis">Pyxis</a>はLLM推論エンジン。<a href="https://github.com/huggingface/candle">candle</a>も<a href="https://github.com/ggml-org/llama.cpp">llama.cpp</a>も使わずに、トランスフォーマーのパイプライン全体（アテンション、KVキャッシュ、RoPE、サンプリング）を自前実装する、というプロジェクト。</p>
<p>つくりはじめたきっかけは、<a href="https://connpass.com/event/395614/">コーディングのためのローカルLLM勉強会 in 福岡</a>に参加して、<a href="https://x.com/monochromegane">@monochromegane</a>さんと<a href="https://x.com/kis">@kis</a>さんと懇親会で話したこと。Carinaのplan表示をわかりやすくしたいという話を@monochromeganeさんとしていたら、生成AIに解説させるというアプローチを提案してもらった。その場ではGitHub ActionsでAIを呼び出す方向で考えていたけど、Carina自体に機能を持たせればいいんじゃないかと思った。また、@kisさんと@monochromeganeさんが推論エンジンについて会話しているのを横で聞いていて「ぜんぜんわからない」と言ったら、@kisさんから「6回実装してみればわかりますよ」と言われた。じゃあ、Carina自体にplan等をAIに解説させる機能を組み込んで、その推論エンジンも自前で実装してしまえ、と思ってつくりはじめた。</p>
<p>今の状態は、safetensorsのパーサーと、テンソル一覧のテーブル表示あたりが動いている段階。safetensorsファイルからテンソルデータをF32/BF16/F16として読み込む部分も実装中。推論エンジンの仕組みを学習する、という目的もあり、Claude Codeに実装と解説をさせ、ひとつひとつ理解しながらなので、亀の歩みではある。</p>
<p>名前は羅針盤座（Pyxis）から。<a href="https://github.com/carina-rs/carina">Carina</a>がりゅうこつ座で、どちらも、現在は4つの星座に分割されたアルゴ座（Argo Navis）の一部にあたる星座。CarinaのplanをAIが解説する、つまり羅針盤が船に方向を示す、という対比でつけた。</p>
<hr />
<h2>Argus</h2>
<p><a href="https://github.com/mizzy/argus">Argus</a>はシンタックスハイライトとgit diff表示を備えたページャー。</p>
<p><img src="/images/2026/06/argus-screenshot.png" alt="Argusのスクリーンショット" /></p>
<p>ファイルを指定して開くCLIツールで、以下のように使う。</p>
<pre class="chroma"><code><span class="line"><span class="cl">argus &lt;file&gt;
</span></span><span class="line"><span class="cl">argus --rev HEAD~1 &lt;file&gt;
</span></span><span class="line"><span class="cl">argus --rev HEAD~3..HEAD &lt;file&gt;
</span></span></code></pre><p>未コミットの変更があるファイルを開くと、diff領域が自動的にハイライトされる。<code>--rev</code>を指定すれば、コミット済みの変更をdiffとして表示することもできる。変更のかたまり（チェンジグループ）間を<code>n</code>/<code>N</code>でジャンプできたり、インクリメンタルサーチが使えたりする。また、word-level diffも実装している。</p>
<p>git diffだと差分周辺しか表示されないけど、それより広い範囲でコードを確認したいことがよくある。そのためにエディタを開いたりlessを使ったりしていたけど、最近コードを書くのはターミナルだけで済んでいるので、そのためにエディタを開くのもだるい。かといってlessはファイルをそのまま表示するだけで、差分が追いにくいし、シンタックスハイライトがないので読みにくい。ターミナルで差分表示するツールは色々あるけれど、どれも自分にはリッチすぎる。シンタックスハイライトと差分表示に対応したページャー程度のものがあればいいな、と思ってつくった。</p>
<p>名前はギリシャ神話の百の目を持つ巨人アルゴス（Argus）から。コードを隈なく見通すツールとしてしっくりくるし、アルゴス自身がアルゴ船を造った船大工でもあるので、<a href="https://github.com/carina-rs/carina">Carina</a>（アルゴ船の竜骨座）との系譜もきれいにつながる。</p>
<hr />
<h2>Iris</h2>
<p><a href="https://github.com/mizzy/iris">Iris</a>はgitのdiff出力にシンタックスハイライトをかけるツール。</p>
<p><img src="/images/2026/06/iris-screenshot.png" alt="Irisのスクリーンショット" /><br />
<code>git config --global core.pager iris</code>と設定すると、すべてのgitサブコマンドのページャーとして使えるようになる。</p>
<p>stdinからunified diffを読んで、変更されたファイルの拡張子から言語を検出し、シンタックスハイライトをかけた状態で出力する。diffのフォーマット自体（ヘッダー、<code>+</code>/<code>-</code>マーカー、ハンク情報）はそのまま残す。</p>
<p>シンタックスハイライトには<a href="https://github.com/trishume/syntect">syntect</a>を使っている。カラーテーマは<code>IRIS_THEME</code>環境変数で指定できて、デフォルトは<code>base16-ocean.dark</code>。</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="nb">export</span> <span class="nv">IRIS_THEME</span><span class="o">=</span><span class="s2">&#34;Solarized (dark)&#34;</span>
</span></span><span class="line"><span class="cl">iris --list-themes  <span class="c1"># 利用可能なテーマ一覧</span>
</span></span></code></pre><p>Argusを使っていて、同じようにgit diff等でもシンタックスハイライトされるといいな、と思ってつくった。こちらも既成ツールはあるけど、どれも見た目がリッチすぎてごちゃついて見えるので、gitの出力そのままにシンタックスハイライトするだけのシンプルなものが欲しくてつくった。</p>
<p>名前はギリシャ神話の虹の女神アイリス（Iris）から。虹の多色性がシンタックスハイライトとかかっているし、irisには眼の虹彩という意味もあるので、色と「見る」の両方に二重にかかっている。百の目を持つArgusと並べると、どちらも「見る／目」のモチーフが揃うのも気に入っている。</p>

]]></content>
    </entry>

    <entry>
        <title type="html"><![CDATA[ホテルメモサイトをAstroで作り直した]]></title>
        <link href="https://mizzy.org/blog/2026/06/16/1/"/>
        <updated>2026-06-16T12:24:46+09:00</updated>
        <id>https://mizzy.org/blog/2026/06/16/1/</id>
        <content type="html"><![CDATA[
<p>去年の8月に<a href="/blog/2025/08/30/1/">ObsidianとQuartz 4でつくる雑なメモサイト</a>というエントリを書いて、<a href="https://hotel-memo.github.io/">ホテルメモ</a>というサイトをObsidian + Quartz 4で運用していたのだけど、これをAstroで作り直した。リポジトリは元と同じ<a href="https://github.com/hotel-memo/hotel-memo.github.io">hotel-memo/hotel-memo.github.io</a>で、サイトのURLも<a href="https://hotel-memo.github.io/">https://hotel-memo.github.io/</a>のまま。</p>
<p>ビフォーアフターはこんな感じ。</p>
<p><em>Before（Quartz 4）</em><br />
<img src="/images/2026/06/hotel-memo-quartz.png" alt="Quartz 4で作っていた頃のホテルメモ" /></p>
<p><em>After（Astro）</em><br />
<img src="/images/2026/06/hotel-memo-astro.png" alt="Astroで作り直したホテルメモ" /></p>
<hr />
<h2>作り直した動機</h2>
<p>ひとつめは、見た目の話。元々Obsidianでメモしていたものをそのまま公開したかったので、Obsidian Publishのクローン的な位置付けのQuartzを選んだ、という流れだった。ただ、別に見た目までObsidian Publishに寄せたかったわけではなくて、特に右サイドのグラフビューは自分のユースケース的に不要なのに、画面の幅をそれなりに占有していてうっとうしかった。コンフィグでいじれる範囲に限界があって、踏み込もうとするとQuartzのコードに手を入れることになりそうで、upstreamと乖離していくのが懸念だった。</p>
<p>もうひとつは、Obsidianを使う必然性が無くなったこと。最初にこの仕組みを作ったときはメモを自分でObsidianに書いていたので、Obsidian → Markdown → Quartzという流れに意味があった。でも今はメモの中身も生成AIに書いてもらうことが多くて、AIが書いたMarkdownを最終的にGitHubのリポジトリに置ければそれでいい、という状態になっている。Obsidianを経由する必要が無い。</p>
<p>それなら、ビルドはもっと素直な静的サイトジェネレータでやって、デザインやレイアウトも自分で決められる方が気持ちいい。ということでAstroで書き直すことにした。</p>
<hr />
<h2>ビジュアルデザインはClaude Designにやってもらった</h2>
<p>雑なメモサイトとはいえ、見た目がそれっぽくないとモチベーションも上がらないので、ビジュアルデザインは<a href="https://www.anthropic.com/news/claude-design-anthropic-labs">Claude Design</a>にやってもらった。</p>
<p>まず最初にClaude CodeにAstroで雑に作ってもらい、Claude Designには「ホテルのワークチェアについてのメモサイトを作りたい」みたいな感じで方向性を伝えつつ、Claude Codeが生成したファイルを渡すと、いくつかデザインのパターンを出してくれる。そこから気に入ったものを選んで、何度かやり取りして方向性を固めていった。</p>
<p>そこからはページ種別ごとに繰り返し作業で、トップページ、地域別ページ、ホテルブランドページ、ホテルページ、部屋ページといった単位でClaude Designにコンテンツやデザインファイルを読み込ませつつデザインを提案してもらい、Claude Code向けの実装指示書を作ってもらって、それをClaude Codeに渡して実装する、という流れ。自分がやり方を知らないだけかもしれないけど、ローカルファイルを直接編集することはできないようなので、「ここを直したい」となるたびにClaude Designで作成した指示書をClaude Codeに渡し直す必要があって、そこは少し面倒だった。</p>
<hr />
<p>参考：</p>
<ul>
<li><a href="https://hotel-memo.github.io/">ホテルメモ</a></li>
<li><a href="https://github.com/hotel-memo/hotel-memo.github.io">hotel-memo/hotel-memo.github.io</a></li>
<li><a href="https://astro.build/">Astro</a></li>
<li><a href="https://www.anthropic.com/news/claude-design-anthropic-labs">Claude Design</a></li>
</ul>

]]></content>
    </entry>

</feed>
